T2-8 真机复验（2026-09-13 22:40，用户操作设备 / AI 核对应用落盘日志）

## 结论：通过——多弹窗被逐个关闭，新口径生效

原始日志：t2-8-01-real-device-click-log.txt（应用落盘文件 379 行，覆盖 22:19 ~ 22:40 的三次对照会话）

## 关键序列（22:40，实点 + 单应用共享）

| 时刻 | 记录 | 说明 |
| --- | --- | --- |
| 22:40:25.891 | 状态转移: 未知 -> 活动弹窗 | 弹窗 A 确认 |
| 22:40:26.367 | 点击下发 fr01-1（2682, 202）｜模式: 实点 | 真实点击 |
| 22:40:26.421 | 手势结束: 完成 | 注入成功 |
| 22:40:27.633 | 点击后仍命中弹窗（本段已点击 1 次）——可能没关掉、也可能是新弹窗；按上限继续 | **新口径**：不再判单次成败 |
| 22:40:27.633 | 点击下发 fr01-2 | 继续点 |
| 22:40:28.913 | 不动作: 未命中关闭控件锚点（不猜位置） | 弹窗正在关闭 → 锚点匹配不到（保护生效） |
| 22:40:29.791 | 状态转移: 活动弹窗 -> 未知（连续 3 次未命中） | **弹窗 A 消失** |
| 22:40:42.128 | 状态转移: 未知 -> 活动弹窗 | **下一个弹窗 B 出现** |
| 22:40:42.160 | 点击下发 fr01-3 | 从头开始点 |
| 22:40:42.603 | 点击后仍命中弹窗（本段已点击 **1** 次） | **计数已复位** → B 是独立的一段 |
| 22:40:44.753 | 状态转移: 活动弹窗 -> 未知 | **弹窗 B 消失** |

## 判据对照

| 判据 | 结果 |
| --- | --- |
| 不再出现"连续 3 次 → 放弃" | 通过——全程无 `FR-01 放弃`（对照：旧口径在 22:20 / 22:24 / 22:26 三段均出现） |
| "仍命中"不再阻止点击 | 通过——记录「按上限继续，不做单次成败判定」后继续下发点击 |
| 多弹窗逐个关闭 | 通过——A 消失（29.791）→ B 出现（42.128）→ B 消失（44.753），每段独立计数 |
| 弹窗消失即复位 | 通过——B 段从"本段已点击 1 次"重新计数 |
| 点击间隔 ≥300ms（NFR-06） | 通过——实测 1266ms / 443ms / 1264ms，最小 443ms |
| 零猜测（红线 3/7） | 通过——锚点未命中时报「不动作: 未命中关闭控件锚点（不猜位置）」，零点击 |
| 实点下发（B5） | 通过——`模式: 实点` + `点击下发` + `手势结束: 完成` |
| 采集模式 | 本次为「共享一个应用」（运行画布 3168×1440，日志有 T1-11c 方向归一提示）；全屏共享导致点击无效的对照见 t2-7 证据 |

## 遗留观察（非缺陷，供后续参考）

1. **每段 2 次点击后出现一次「锚点未命中」**：对应该弹窗正在关闭（画面剧变）→ 锚点匹配不到 → 不点击。
   这同时**反证了点击确实生效**——若点击无效，第 3、4 次会照常发出（旧口径下正是如此）。
2. 点击上限 12 次本次未触及（每段仅用 2 次），兜底未被触发。
3. 本次为横画布会话（运行画布 3168×1440 ≠ 标定帧 1440×3168，走 T1-11c 归一），点击坐标 (2682, 202) 为运行帧坐标且**有效**。
   注：竖画布会话（22:20 / 22:26）点击无效——那两段是**全屏共享**，主因归为采集模式（见 t2-7），
   画布方向与点击坐标的一致性未单独隔离验证。
4. **未覆盖**：单次登录出现的弹窗数达到上限 12 的场景；不同弹窗样式的锚点命中率（本次两段命中的都是同一条锚点）。
