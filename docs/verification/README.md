# 验收证据对照

> 用途：把每条 FR / NFR 与"凭什么说它验收了"的证据对上。里程碑收尾时更新。
> 证据要求：可回溯的命令、报告、日志或截图引用（文件路径 / 提交号）。禁止只写"已验证"。

## 记录规则

1. 状态口径：未开始 / 进行中 / 已验收（有证据）。
2. 误报相关结论必须附样本量 n，并按上界公式表述（见 `../glossary.md`「误报率上界」）。
3. 识别类证据先归档到 `../recognition/`，本表只放链接。

## 总表

| 编号 | 内容摘要 | 状态 | 证据 |
| --- | --- | --- | --- |
| FR-01 | 活动弹窗自动关闭 | 进行中（M2 实现完成：期望集合驱动 + 弹窗 4 条记录标定 + 关闭闭环 B1/B2/B4 演练通过 + 演练/实点开关 B5；**实点已执行**（用户报告多个活动弹窗被逐个关闭），**`MM-Click` 日志证据待补**） | ① T2-2 标定与样式 ① 真机复演：[m2/t2-2/](m2/t2-2/)；② T2-5 唤醒集收窄（只搜活动弹窗）+ 离线负背景验证（农场 0/15、好友面板 0/7、大厅(无弹窗) 0/14、大厅+弹窗 36/15 帧）与真机演练：[m2/t2-5/](m2/t2-5/)；③ 任务卡（T2-1 ~ T2-6）与实点核对状态：[../plans/m2-auto-handling.md](../plans/m2-auto-handling.md) |
| FR-02 | 新手引导 / 新手大厅自动返回 | **已移除**（2026-09-13 用户口径：账号打完一局后两界面不再出现，见 `../requirements.md` §1.3 / §3.1） | — |
| FR-03 | 巡查配置 | 未开始 | — |
| FR-04 | 巡查主流程（10 步） | 未开始 | — |
| FR-05 | 中断与恢复 | 未开始 | — |
| FR-06 | 巡查记录 | 未开始 | — |
| FR-07 | 悬浮窗与操作菜单 | 进行中（M0 壳 + M1 状态文字；操作菜单待在 M2 开放） | A4 可见性 + A5 穿透：[t0-5/](t0-5/)（2026-09-11 真机）；A8 状态文字随识别更新：[t1-7/](t1-7/)（2026-09-12 真机）；`../plans/m0-skeleton.md`、`../plans/m1-recognition.md` |
| FR-08 | 后台运行 | 进行中（M0 壳） | A1 授权状态矩阵：[t0-2/](t0-2/)（2026-09-11 真机核对）；A3 常驻观察：[t0-4/](t0-4/)（2026-09-11 真机）；`../plans/m0-skeleton.md` |
| FR-09 | 前台判定 | 进行中（M0 壳 + M1 采集会话 + 识别循环冻结） | A2 时序核对：[t0-3/](t0-3/)（2026-09-11 真机）；M1-T1-2 会话区分（切走不终止）：[t1-2/](t1-2/)（2026-09-11 真机）；M1-T1-4 识别循环冻结（FR-09 不消耗滞回计数）：[t1-4/](t1-4/)（2026-09-12 真机）；`../plans/m0-skeleton.md`、`../plans/m1-recognition.md` |
| NFR-01 | 单帧处理 < 20ms（P95） | 进行中（**未达标，如实记录**：全扫 7 信号 ≈993ms 推算 / 真机全扫轮 ≈790ms、按状态子集稳态 ≈62ms 推算 / 真机 P95 298.3ms，均 > 20ms） | M1-T1-9 首轮真机基线（P95 2287.6ms）：[t1-9/](t1-9/)（2026-09-12 真机）；M1-T1-10 离线取证 + 真机复审（P95 312.8–327.8ms，较基线提速 ≈7.0–7.3×）：[t1-10/](t1-10/)；T1-10e 后续手段评估（3×3 采样 ×1.39 有漂移 / 剪枝 ×1.06 零漂移，均不采纳）：[research-t1-10e](../recognition/research/research-t1-10e-speedup-evaluation.md)；T1-10f 多信号与分段剖析（7 信号 = 单信号 ≈3.85×，粗搜 ≈79% / 精搜 ≈20%；设备 67 帧 + 7 信号生产路径 P95 38ms → 推算真机 ≈1.0s）：[research-t1-10f](../recognition/research/research-t1-10f-cost-profile.md)；T1-10g 按状态启用子集（状态序列与全扫 0/67 差异；搜 1 个 2.31ms vs 全扫 36.80ms）：[research-t1-10g](../recognition/research/research-t1-10g-subset.md)；T1-13 真机复测（预期驱动口径下的真实构成：子集轮 P95 298.3ms / 全扫轮 ≈790ms，瓶颈为大窗口信号与全扫）：[t1-13/](t1-13/)（2026-09-13 真机）；T1-13b 逐信号耗时拆解（每信号单独计时、不含等待与全扫兜底；最贵 `server_select` 10.03ms / 最便宜 `farm` 0.59ms，倍差 ×17.1；全扫 = 逐信号之和，无额外固定开销）：[research-t1-13b](../recognition/research/research-t1-13b-per-signal-cost.md)（2026-09-13 桌面回放；同日加入**信号级耗时日志**「信号耗时统计: <信号名> 连续 100 次 P95/平均/最大」，**真机逐信号实测 7/7（2026-09-13 两场演示、35 个窗口）**：均值 **×12.4**、区间 **×10.3~15.1**（竖画布会话）/ **×17.0**（首场横画布会话）→ 换算系数由单值 ×27 改为**区间 ×10~×20**（同一信号窗口间可差 2 倍，均值随样本构成浮动；"双峰 = 设备固有"未证实）；最便宜路径 `farm` 真机 6.5~7.4ms + 灰度 16ms ≈ 23ms/轮，**已逼近** 20ms 阈值；取数与回填脚本 `tools/t1-13b-device-signal-cost.ps1`；真机证据：[t1-13b/](t1-13b/)） |
| NFR-02 | 检测间隔自适应（口径经 T1-13 修订：需要响应 ≤0.2s / 确实稳定 ≤1s；间隔 = 两轮之间的休息时间） | 进行中（M1 两档节流；人速取值 200 / 1000ms） | M1-T1-4 稳定档实测（10s 窗口 3–4 轮 ≈3000ms）：[t1-4/](t1-4/)（2026-09-12 真机）；M1-T1-9 真机两档切换日志（1000↔3000ms 成对出现）：[t1-9/](t1-9/)（2026-09-12）；**T1-13 真机复核：200↔1000ms 成对切换且判据标注正确、稳定档周期 = 1000ms 休息 + 单轮耗时（7~8 轮 / 10s）、≈98% 帧被节流跳过**：[t1-13/](t1-13/)（2026-09-13）；全区间最终验收在 M5 |
| NFR-03 | 包体 ≤ 25MB / Android 9.0+ | 未开始 | — |
| NFR-04 | 30 分钟长跑无崩溃 | 未开始 | — |
| NFR-05 | 无意外点击（点击审计） | 进行中（M2 起点击审计层落地：`MM-Click` 对**每次点击与每次拒绝**均留痕——判定 ID / 锚点 / 模式 / 状态 / 前台 / 时间戳；正式验收属 M5 全流程点击审计） | M0 零点击预审：[t0-6/](t0-6/)（2026-09-11）；M2-T2-5 演练段「点击拒绝（演练模式：只识别不下发）」留痕 + 零真实点击：[m2/t2-5/](m2/t2-5/)；**M2 实点段审计日志待补** |
| NFR-06 | 点击间隔 ≥ 300ms | 进行中（M2 统一转发层实现「上次手势**完成** → 本次**开始** ≥ 300ms」硬下限，JVM 单测覆盖；**真机相邻间隔待 T2-3e 实点日志**） | [../plans/m2-auto-handling.md](../plans/m2-auto-handling.md)（T2-3c 点击转发层 + T2-3e 真机核对；真机证据待补） |

## M0-T0-2 授权流核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168
- 核对内容（验收 A1）：

| 截图 | 状态 | 结果 |
| --- | --- | --- |
| [t0-2-01-all-missing.png](t0-2/t0-2-01-all-missing.png) | 全部缺失 | 三项均红色「缺失」+ 各自引导按钮；汇总「还缺：无障碍服务、屏幕采集、通知」 |
| [t0-2-02-one-missing.png](t0-2/t0-2-02-one-missing.png) | 缺一项 | 无障碍「已开启」、通知「已允许」、屏幕采集「未授权」；汇总「还缺：屏幕采集」 |
| [t0-2-03-all-granted.png](t0-2/t0-2-03-all-granted.png) | 全部已授 | 三项均就绪；汇总「全部就绪」 |

- 补充验证：
  - 会话制：强杀进程重开后屏幕采集回到「未授权」（[t0-2-04-session-reset.png](t0-2/t0-2-04-session-reset.png)），符合 ADR-001；
  - 自动刷新：从系统设置页返回（onResume）后状态即时刷新，无需手动操作；
  - 边界发现：vivo 在应用被「强行停止」后会撤销其无障碍授权——界面下次启动正确显示「未开启」并给出引导，符合 FR-08「不得静默失败」。

## M0-T0-3 前台判定核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168
- 目标应用：《王者荣耀》国服（`com.tencent.tmgp.sgame`，设备实装）；原始日志：[t0-3-01-timeline-log.txt](t0-3/t0-3-01-timeline-log.txt)（含操作标记 MM-Marker，与状态日志同一时间源，可直接相邻对比）

| 场景 | 操作发出 | 判定生效 | 延迟 |
| --- | --- | --- | --- |
| 切后台（HOME） | 20:05:55.667 | 20:05:56.010 | **343ms** |
| 热态切到其他应用 | 20:06:21.865 | 20:06:22.313 | **448ms** |
| 热态切回游戏 | 20:06:26.077 | 20:06:27.692 | **1.615s** |
| 熄屏 | 20:07:11.759 | 20:07:12.731 | **972ms** |

- 结论（验收 A2）：
  - 独立信号：判定仅依据无障碍服务的活动窗口包名，与画面识别无任何耦合；
  - 2 秒内生效：实测切换延迟 343ms–1.6s，全部达标；
  - 判定失败即非前台：服务未连接 / 查询失败 / 服务销毁 → 输出「非前台」（JVM 单测覆盖查询失败分支；真机关闭服务期间零误报、零残留信号）。
- 补充说明：
  - 实现为双来源：窗口事件为主（上述延迟均为事件链路），1.5s 周期主动复核兜底防丢事件；
  - 游戏冷恢复期间（如发起于 20:05:58、生效于 20:06:05 的 6.9s 案例）目标应用确实尚未取得焦点——判定「非前台」是正确行为；焦点一取得立即切换（窗口事件驱动）；
  - 锁屏（NotificationShade）保持「非前台」；解锁回游戏即时恢复「前台」；
  - 服务关闭→重开（vivo 界面流程，关闭确认 + 7s 防误触倒计时）：关闭期间无任何 FOREGROUND 误报，重开后恢复正常；
  - 环境事实：王者荣耀绑定登录页由游戏应用自身绘制（非跨应用界面），与 ADR-001 单应用采集口径吻合；设备存在「主应用 / Ⅱ·分身」双实例，冷启动会触发 vivo 实例选择器（选择器界面判定为非前台，正确）。后续真机测试遇实例选择器时，统一选择主应用（「王者荣耀」）继续测试。

## M0-T0-4 常驻服务核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168
- 观察对象：`ResidentService`（前台服务，specialUse 类型；常驻通知「MasterMechanic 守护运行中」）
- 原始记录：[t0-4-03-observation-log.txt](t0-4/t0-4-03-observation-log.txt)；截图：[通知中心](t0-4/t0-4-01-notification-panel.png)、[界面运行状态](t0-4/t0-4-02-ui-running.png)

| 场景 | 观察时长 | 检查点 | 结果 |
| --- | --- | --- | --- |
| 切后台（HOME） | 2 分钟 | +30s / +120s | 存活（isForeground=true，startRequested=true） |
| 熄屏（POWER，mWakefulness=Asleep 确认） | 2 分钟 | +30s / +120s | 存活（同上） |
| 亮屏恢复 | — | 即时 | 存活（同上） |

- 结论（验收 A3）：
  - 服务常驻：切后台 / 熄屏观察期内系统未中止；`types=0x40000000`（specialUse）在 Android 16 上生效；
  - 通知可见：通知中心明确展示「MasterMechanic 守护运行中」（NO_CLEAR 标记，不可随手划掉）；
  - 状态展示：界面「运行状态」与系统通知一致呈现「运行中」，无静默失败。
- 补充说明：
  - 锁屏期间服务同样保持运行（额外存活佐证）；
  - 「停止」按钮路径（context.stopService）未真机复核：锁屏无法操作 UI 且 vivo 拒绝 shell 侧 am stop-service，留待后续复核；
  - 观察结束后服务保持运行（设备守护处于开启状态）。

## M0-T0-5 悬浮窗壳核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168（横屏物理坐标系 3168×1440）
- 观察对象：收起态手柄悬浮窗（`TYPE_ACCESSIBILITY_OVERLAY`，无障碍服务挂载；文字「待命」；右上角 12dp/120dp 停靠）
- 原始记录：[t0-5-08-window-touch-flags.txt](t0-5/t0-5-08-window-touch-flags.txt)（窗口属性摘录）、[t0-5-09-timeline-log.txt](t0-5/t0-5-09-timeline-log.txt)（可见性时间线日志）

可见性三态（验收 A4）：

| 状态 | 截图 | 结果 |
| --- | --- | --- |
| 游戏前台 | [t0-5-01](t0-5/t0-5-01-a4-game-visible.png) | 手柄显示（右上角「待命」） |
| 切后台（HOME） | [t0-5-02](t0-5/t0-5-02-a4-background-gone.png) | 整窗移除，桌面无任何悬浮窗痕迹 |
| 回游戏前台 | [t0-5-03](t0-5/t0-5-03-a4-back-restored.png) | 收起态重建恢复显示 |

- 日志链条：挂载 / 移除与前台信号变化一一对应（20:34:22 回游戏挂载 → 20:37:14 切后台移除 → 20:37:56 回游戏重挂载），窗口事件链路即时生效。
- 穿透（验收 A5）：
  - 系统级：窗口 `fl=NOT_FOCUSABLE NOT_TOUCHABLE`（系统不向该窗口分发焦点与触摸）；窗口几何 176×105（wrap_content 紧凑）；
  - 行为级：在覆盖区注入点击（落点同时位于窗口矩形 [2944,480]-[3120,585] 内与游戏「静音」图标命中区内）→ 游戏图标由「静音（斜线）」切换为「有声（声波）」：点击穿透手柄直达游戏（[点前](t0-5/t0-5-04-a5-tap-before-full.png) → [点后](t0-5/t0-5-06-a5-tap-after-full.png)，局部放大：[前](t0-5/t0-5-05-a5-tap-before-zoom.png) / [后](t0-5/t0-5-07-a5-tap-after-zoom.png)）。
- 补充说明：
  - 安装重装 APK 触发服务重载：断开时移除、重连立即恢复（日志 20:31:03–20:31:04），无残留；
  - 真机环境：游戏登录页右侧图标列与手柄默认停靠区存在窄重叠（约 26px 宽），穿透验证落点即取重叠区；
  - 游戏冷启动再次出现实例选择器，按既定口径选择主应用（「王者荣耀」）后继续。

## M0-T0-6 零点击审计核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）；审计基点：commit 7341c5b（T0-5 完成后，M0 全量代码）
- 原始记录：[t0-6-01-code-audit.txt](t0-6/t0-6-01-code-audit.txt)（代码静态审计）、[t0-6-02-runtime-log.txt](t0-6/t0-6-02-runtime-log.txt)（运行时日志审计）

代码静态审计（结论）：
- 全库 `*.kt` 检索注入类 API（dispatchGesture / performAction / MotionEvent / KEYCODE / shell 执行等三组正则）零调用；唯一命中为 androidTest 模板测试的标准 API（不进入发布 APK）；
- Manifest 权限仅通知与前台服务三类；服务 `canPerformGestures` 为能力声明，M0 对能力零调用；依赖清单无自动化 / 注入类库。

运行期日志审计（结论）：
- 采样场景：游戏前台 + 悬浮窗显示静置 68 秒 → 切后台 → 回游戏；应用进程全量日志（62 行）中业务日志仅「前台状态变化」与「悬浮窗挂载 / 移除」两类，无任何注入动作记录；
- 佐证：采样起止截图（[开始](t0-6/t0-6-03-game-start.png) / [结束](t0-6/t0-6-04-game-end.png)）显示游戏始终停留于登录页——从未被任何点击推进。

补充说明：
- 审计对象为应用自身代码路径与运行日志；验证期间由测试侧注入的点击（如 T0-5 穿透验证的 adb input）属测试行为，非应用发出，已区分；
- M0 六项验收 A1–A6 至此全部通过，计划文件状态置「已完成」（DoD 达成）。

## M1-T1-2 采集会话核对记录（2026-09-11）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168（横屏物理坐标系 3168×1440）
- 核对对象：`CaptureService`（mediaProjection 类型前台服务；常驻通知「屏幕采集中」）
- 原始记录：[t1-2-01-runtime-log.txt](t1-2/t1-2-01-runtime-log.txt)（会话日志时间线）、[t1-2-06-dumpsys.txt](t1-2/t1-2-06-dumpsys.txt)（会话存活性快照）

会话生命周期四场景（验收 A1）：

| 场景 | 时点 | 结果 |
| --- | --- | --- |
| 建立 | 21:19:22.192 | INACTIVE -> ACTIVE（来源: 用户授权会话建立）；帧管线 ~60fps 接收 / 1s 节流处理 |
| 切走（HOME，~2.5 分钟） | 21:20:12–21:22:51 | 无终止日志、dumpsys 会话保持活跃；期间无帧（单应用路径目标不可见时系统空白化，与 ADR-001 一致）；回前台帧恢复 |
| 应用主动停止 | 21:23:35.358 | ACTIVE -> INACTIVE（来源: 应用主动停止）；界面回到「未授权」 |
| 系统侧回收 | 21:26:45.885 | 系统控制中心停止共享 → onStop 回调 → ACTIVE -> INACTIVE（来源: 系统侧回收）；服务 / 通知随之停止 |

界面状态（截图）：[采集中](t1-2/t1-2-02-ui-capture-active.png)、[常驻通知](t1-2/t1-2-03-capture-notification.png)、[终止后未授权](t1-2/t1-2-04-ui-capture-inactive.png)、[系统共享指示条](t1-2/t1-2-05-share-indicator.png)

补充说明：
- 会话制语义完整：每次终止（任一来源）后界面回到「未授权」并给出重新授权入口，符合 ADR-001 与 FR-08「不得静默失败」；
- 单应用共享路径真机确认：系统授权弹窗默认「共享一个应用」，目标应用前台时系统显示「屏幕共享中」指示条；目标不可见时无帧到达但会话不终止；
- 系统侧停止验证期间触发的系统自带录屏属测试侧操作，已停止并确认无残留会话；测试期间 adb 注入的点击属测试行为，非应用发出，已区分；
- 采集期间应用日志仅「会话状态变化」与「帧管线统计」两类，无任何注入动作记录（红线 1 / 2 延续）。

## M1-T1-3 识别引擎核对记录（2026-09-11）

- 核对对象：识别引擎（`recognition` 包：`TemplateMatcher` + `SignalDetector`；纯逻辑，无安卓框架依赖）
- 设备无关性：本层零设备绑定常量（模板 / 搜索窗口 / 阈值全部注入）；以固定种子程序化合成样本在 JVM 离线重跑（真机画面样本的标定采集属 T1-5、回放工具属 T1-6）
- 原始记录：[t1-3-01-jvm-test-record.txt](t1-3/t1-3-01-jvm-test-record.txt)（JVM 测试记录：执行命令 / 18 例明细 / 全量回归 / 验收对照）、[t1-3-02-replay-report.txt](t1-3/t1-3-02-replay-report.txt)（回放比对报告：A2 样本集与比对方法 / 确定性来源）

验收对照：

| 验收 | 内容 | 结果 |
| --- | --- | --- |
| A2 | 同批画面 + 同参数 → 结果完全一致（§5-4） | 三次重跑判定记录逐字段一致；峰值排序含确定性 tie-break |
| A3 | 判定记录四项齐全（§5-1） | 命中标志 / 位置 / 置信度（精确命中 >0.99）/ 最强竞争位置 |
| A4 | 不可信按未识别处理（§5-2） | 判据规则级边界 2 例 + 图像级多处高分 1 例均判 UNRELIABLE |

测试规模：识别包新增 18 例（TemplateMatcherTest 7 + SignalDetectorTest 11）全过；全量回归 32 例零失败（既有套件 14 例无回归）。

补充说明：
- 测试参数（命中线 0.85 / 差距线 0.1 / 峰值间距 5）为测试数据，非生产值；生产参数来自目标设备标定（T1-5）；
- 半透明遮罩场景实测分数约 0.65（远高于噪声峰 ~0.25）：命中线标定时需纳入此类干扰场景；
- 本记录为纯逻辑层证据；真机画面样本的回放比对与误报率统计随 T1-6 工具就绪后补充。

## M1-T1-4 状态机（滞回）核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168（横屏物理坐标系 3168×1440）
- 核对对象：状态机（滞回，纯逻辑）+ 识别循环接入采集帧管线（含 NFR-02 两档节流初版）
- 原始记录：[t1-4-01-jvm-test-record.txt](t1-4/t1-4-01-jvm-test-record.txt)（JVM 测试记录：60 例全量 / T1-4 新增 28 例明细 / A5 对照）、[t1-4-02-runtime-log.txt](t1-4/t1-4-02-runtime-log.txt)（真机运行日志：原始时间线 / 冻结与节流实测）

验收对照（A5）：

| 核对点 | 结果 |
| --- | --- |
| 进入：连续 2 次命中 | JVM 单测通过（singleHitDoesNotEnter / consecutiveHitsEnterState / brokenStreakDoesNotEnter） |
| 离开：连续 3 次未命中 | JVM 单测通过（twoMissesKeepState / threeMissesLeaveToUnknown / hitResetsMissStreak） |
| 未知：连续 3 次未命中任何标志 | JVM 单测通过（staysUnknownWithoutAnyHit；当前为「未知」时不累计离开计数） |
| 非前台冻结不消耗计数（FR-09） | JVM 单测 2 例（进入 / 离开计数各 1）+ 真机冻结 / 恢复 ×3 / ×2 与前台信号一一对应（55ms–0.8s） |

真机运行核对（识别循环运行 + 冻结核对）：
- 识别循环运行：统计窗口 ×9，~60fps 接收、按节流处理、识别轮数与窗口时长吻合；最近帧 1440×3168 为运行时读取（非代码常量）；
- NFR-02 两档节流实测：稳定态 10s 窗口识别 3–4 轮 ≈3000ms 间隔（稳定档），与「稳定时 3~5s」区间相符；冻结轮不调整节流间隔；
- 状态恒「未知」为设计预期（未标定，日志明示「信号 0 个，未标定（无判定）」）；真实标志驱动的状态切换随 T1-5 标定产物补充核对；
- 全程零点击：运行日志仅会话 / 前台 / 悬浮窗 / 冻结 / 统计类，无任何注入动作（红线 1 / 2 延续）。

测试规模：T1-4 新增 28 例（UiStateMachineTest 12 + RecognitionLoopTest 5 + SignalStateMappingTest 5 + RgbaToGrayTest 4 + FrameThrottleTest 新增 2）；全量回归 60 例零失败（既有套件 32 例无回归）。

补充说明：
- 采集端环境事实（记录备查）：设备屏幕约 30s 自动熄灭会触发系统侧回收采集会话，长时核对需保持屏幕常亮；应用进程日志曾出现静默丢失、进程重启后恢复（设备侧环境事实，非应用缺陷）；
- 本次真机范围按任务卡界定为「识别循环运行与冻结核对」；真实标志驱动的状态切换（进入 / 离开 / 未知）观察依赖 T1-5 标定产物，届时补充。

## M1-T1-5 标定核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）
- 核对对象：设备自标定闭环——样本采集 → 标注与产物生成 → 产物加载接入识别（补 T1-4 备注：真实标志驱动的状态切换）
- 原始记录：[t1-5-01-runtime-log.txt](t1-5/t1-5-01-runtime-log.txt)（真机事件时间线）、[t1-5-02-artifact-review.txt](t1-5/t1-5-02-artifact-review.txt)（产物审查）
- 样本与代表帧：`../recognition/samples-index.md`（set-20260912-01）

验收对照（A6）：

| 核对点 | 结果 |
| --- | --- |
| 识别能力在目标设备上建立 | 真机标定 48 帧（启动页）→ 框选「开始游戏」→ 产物：信号 launch_start / 模板 131×37（选框 1:1 灰度裁剪）/ 参数 0.85,0.10,5 |
| 代码零设备绑定参数 | 产物为全部设备相关数值的唯一载体（帧尺寸 / 窗口比例坐标 / 参数均读自产物文件；审查见 t1-5-02，口径延续 T1-3） |
| 更换设备可重建 | 标定流程为设备侧通用流程（无开发设备参数），样本与产物可按同流程重建 |

真机运行核对（真实标志驱动的状态切换，补 T1-4 备注事项）：
- 会话（重）建立加载产物：「标定产物已加载：信号 1 个（标定帧 1440x3168）」；
- 识别命中：「界面状态变化: 未知 -> 启动页（连续 2 次命中（进入））」（08:03:17）；其后至 08:04:36 共 6 个统计窗口持续保持「启动页」；
- 帧尺寸一致性：运行帧 1440x3168 与标定帧一致（无告警）；帧管线 ~60fps 接收、按节流处理、稳定态 3–4 轮/10s；
- 全程零点击：运行记录仅会话 / 录制 / 产物加载 / 冻结恢复 / 状态变化 / 统计类，无任何注入动作（红线 1 / 2 延续）。

补充说明（环境事实，记录备查）：
- 帧画布方向随授权时持机姿态变化：竖持授权 → 1440×3168 画布（横屏游戏内容居中带嵌入）；横持授权 → 3168×1440。标定帧与运行帧需同姿态采集，尺寸不一致时运行期有明确告警（本记录为竖持会话）；小目标先放大画面再框选（已随真机反馈实现）；
- 核对期间该设备曾出现应用进程日志静默丢失（进程重启后恢复，同 T1-4 记录先例）；本记录事件时间线可与设备日志交叉复核。

交互增强轮（真机标定反馈 T1-5e–T1-5j，2026-09-12）：
- 内容：归属状态默认信号名与新手状态拆分（e）、标定页布局聚焦（f）、信号名输入框移除（g）、预览滑页 / 双击全屏 / 画布内全屏图标（h）、预览画布高度占位防闪烁（i）、缩略图联动「过半即同步」（j）；任务卡见 `../plans/m1-recognition.md`。
- 回归：全量 16 套件 112 例零失败；真机（vivo V2463A）逐轮安装核对通过。
- 边界：交互轮仅细化标定页 UI 与操作，产物口径与零设备绑定约束不变（判据同本分节 A6 对照）。

选框锚点外置与视觉修正（真机标定反馈 T1-5k，2026-09-12）：

- 内容：框选改「**外层操作框 + 内层真实选区**」——拉伸手柄与关闭钮挂在外层框上（解决真机反馈的「锚点过大压住边缘、看不清边缘」）；外层框白
  色细线、外扩 16dp、锚点缩小（角 5dp / 边 3.5dp / 关闭钮半径 8dp）；选框与锚点之间的带状区域按整体移动处理；`hitCloseButton` 增「选区内一律不命中」
  保护（窄边距下热区侵入选区时，拖动选框内部不会误清选框）；最大放大倍率 8× → 16×。
- 回归：`FrameEditTest` 30 例（净增 9 例：外框外扩与贴边退化 / 锚点不落在选区内 / 内层角不再是手柄 / 带状区为移动区 / 关闭钮外置且不压选区角 /
  选区内不触发关闭钮）；全量 27 套件 193 例零失败。
- 真机核对：vivo V2463A 全屏框选页截图 [t1-5-03-anchor-outside-selection.png](t1-5/t1-5-03-anchor-outside-selection.png)——外层白色框与全部锚点位于红色
  真实选区之外、✕ 关闭钮在选区左上外侧，底部实时显示选区帧像素范围（本次 574,946 → 894,1106）。
- 边界：仅视图绘制与命中几何调整，产物口径与零设备绑定约束不变（选框比例坐标语义同 A6）；外层边距 / 锚点尺寸 / 最大倍率为视图层常量，可继续按真机手感微调。

标定流程重构（T1-5l，2026-09-12）：

- 内容：① 锚点**全部移除**（框选只留红色真实选框与左上角外置关闭钮，拉伸能力取消，不满意点 ✕ 重框）；② **废除草稿模型**（`CalibrationDraft` / `DraftSignal` 删除，产物即唯一数据源）：框选 + 选归属状态即写入产物（同名信号覆盖），不再有「提取为模板」「保存产物」两步；③ 操作收进**全屏工作台**（画布 + 缩略图挑帧 + 归属状态同页，页内不再有预览大图与缩略图条）；④ 产物可读取 / 可删除（逐条列出状态、模板尺寸、窗口帧像素，可查看模板灰度图详情、删单条、删整体）；⑤ 参数属产物级设置，编辑合法即写入（无产物时随首个信号写入）；⑥ 画布几何与产物不一致时拒绝写入并提示（避免产物混几何）。
- 回归：`CalibrationSignalsTest` 21 例（新增）+ `FrameEditTest` 21 例（按新 API 重写）+ `CalibrationDraftTest` 8 例（随草稿模型删除）；全量 **27 套件 198 例零失败**；`:app:assembleDebug` 通过。
- 离线证据：[t1-5l-01-offline-verification.txt](t1-5l/t1-5l-01-offline-verification.txt)（变更清单 + ①–⑥ 验收对照 + 可复跑命令）。
- **真机核对（2026-09-12 晚，vivo V2463A）**：[t1-5l-12-real-device-verification.txt](t1-5l/t1-5l-12-real-device-verification.txt)——标定页 / 工作台 / 框选 / 覆盖写入 / 模板详情 / 删除单条 / 删除整个产物逐步截图（t1-5l-02 ~ t1-5l-11），产物文件逐字段对照（仅 `farm` 被覆盖、信号数仍 7、几何与参数不变；窗口换算与选区口径自检一致）；核对前后产物 md5 一致（`a30a6422…`，设备产物 = 备份 = 本地副本，核对结束后还原并删除备份）。
- **核对发现并修复的缺陷（如实记录）**：参数编辑写入**落后一次编辑**（界面 0.86 / 产物 `0.800000`）——根因 `applyParams()` 使用组合期快照值（`editedParams` / `artifact`）；修复为实时读取编辑框 + 现读盘上产物，删除信号与工作台写入同步改为「现读盘」；复测同一序列得到 `0.860000` / 改回 `0.850000` 一致（[t1-5l-09](t1-5l/t1-5l-09-param-write-lag-defect.png) / [t1-5l-10](t1-5l/t1-5l-10-param-write-fixed.png)），修复后回归 198 例零失败。
- 边界：产物格式未变（`CalibrationData` 文本编码同 T1-5a），旧产物可直接加载；零设备绑定约束不破（模板 / 窗口 / 参数仍全部来自产物）。

工作台交互定稿（真机反馈 T1-5m，2026-09-12 晚）：

- 内容：①「打开标定工作台」入口移入「标定产物」卡（文案去括号）；②工作台大图占满全屏（黑底等比居中），缩略图条与工具栏绝对定位悬浮底部；③浏览模式滑页切帧、缩略图「过半即同步」（T1-5h/j 口径回填）；④点「框选」进入框选模式——禁滑页 / 隐缩略图，工具栏「✕（放弃：清选框回浏览）｜ 选区信息 ｜ ✓ 写入（需已框选 + 已选归属状态，未选提示不写盘）」，其上为归属状态单选滑动条；✓ 按选中状态写入产物（同名覆盖）后回浏览模式；⑤恢复选框**外层白色细线**（外扩 16dp，仅视觉；锚点仍无，命中区仍「框内移动 / 框外新建 / 左上 ✕」）；⑥双指缩放保留。**写入时机口径由 T1-5l「点状态即落盘」修订为「✓ 确认写入」**（用户定稿）。
- 回归：`FrameEditTest` 23 例（恢复外框几何 2 例）；全量 **27 套件 200 例零失败**；`:app:assembleDebug` 通过。
- 真机核对：[t1-5m-01-verification.txt](t1-5l/t1-5m-01-verification.txt) + 截图 t1-5m-02 ~ t1-5m-08（入口位置 / 浏览模式 / 滑页同步 / 框选模式 / 白色外框 / 状态选中与工具栏 / 写入成功消息）；产物文件对照（仅覆盖信号变化、params 与几何逐字段不变）；核对后产物已还原（md5 与基线一致 `a30a6422…`）。
- 记录的测试侧异常（非代码缺陷）：一轮写入前差距线被误注入 0.17（界面与产物一致，写入与解析正确）；当场纠正并复跑完整写入链路，params 保持 0.850000,0.100000,5 不变。
- 交付后补充（用户反馈，同日）：底部工具栏「框选」改为**图标 + 底部文字**（`ic_frame_select.xml` + `ToolIconButton`），装机后**由用户实机自测，结论「符合预期」**；最终验证：设备产物 md5 仍为基线 `a30a6422…`（自测未污染产物，无需还原）——见 [t1-5m-01 第四节](t1-5l/t1-5m-01-verification.txt)。

## M1-T1-6 回放与指标工具核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168
- 核对对象：回放与指标工具——固定批次样本集（清单 + 画面）→ JVM 离线回放（灰度入库）→ 误报率指标报告
- 样本与报告：`../recognition/samples-index.md`（set-20260912-03）；[report-20260912-t16e-batch01.md](../recognition/reports/report-20260912-t16e-batch01.md)
- 原始记录：[t1-6-01-diagnostics.txt](t1-6/t1-6-01-diagnostics.txt)（逐样本判定与分数诊断表）

验收对照（A7）：

| 核对点 | 结果 |
| --- | --- |
| 负样本 ≥60 | 85 帧（半透明遮罩 18 / 动背景 4 / 相似多目标 10 / 普通 53） |
| 三类干扰分别统计 | 报告分列，未合并成一个总通过率 |
| 结论以上界公式表述 | 零误报上界 1 − 0.05^(1/n)：遮罩 ≤0.1533 / 动背景 ≤0.5271 / 相似多目标 ≤0.2589 / 普通 ≤0.0550；合计 85 帧 0 误报 |
| 正样本参照 | 命中 18/19（唯一未命中为「开始游戏」按下高亮瞬态帧，属点击后瞬态、不在识别时机范围，如实记录） |
| 可复现 | 同数据 + 同参数运行 3 次，判定记录逐字段一致（无差异） |

批次与工具说明：
- 批次 104 帧由两次真机采集合并固定（换区巡查全链路 + 大厅日常操作），清单逐帧标注（正样本含目标信号与期望位置，负样本三类分列）；逐帧分类笔记随批次存于本地 `app/build/replay-work/`；
- 识别信号 / 模板 / 搜索窗口 / 阈值全部来自目标设备标定产物（calib-v1＝2026-09-12 真机标定）；工具与清单零设备绑定常量（口径延续 T1-3 / T1-5）；
- 灰度入库后重解码与原始帧逐位等值（单测 `ReplayToolTest`）；报告生成由全量解码改为逐帧流式（批次规模不受测试堆限制，结果等价）；
- 本次为纯离线（JVM）回放，不涉及真机运行；零点击口径不受影响（红线 1 / 2 延续）。

## M1-T1-7 演练模式与状态展示核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168
- 核对对象：演练模式（只识别、不点击）下界面状态信号 → 悬浮窗文字逐态更新（`UiStateSignal` → `FloatingWindow`）
- 原始记录：[t1-7-01-runtime-log.txt](t1-7/t1-7-01-runtime-log.txt)（完整演练日志 13:26:22–13:36:54；20 个测试侧锚点 MM-Marker 与状态日志同一时间源，可相邻对比）

验收对照（A8）：

| 核对点 | 结果 |
| --- | --- |
| 悬浮窗文字随识别状态更新 | 「未知 → 启动页」13:29:48（[t1-7-03](t1-7/t1-7-03-floating-start-page.png)）；「启动页 → 未知」13:31:23（[t1-7-05](t1-7/t1-7-05-floating-unknown.png)）；会话重建后再次「未知 → 启动页」13:36:25（[t1-7-07](t1-7/t1-7-07-floating-start-page-rebuilt.png)） |
| 滞回行为可见 | 测试侧点「换区」后界面已切换，悬浮窗在连续 3 次未命中判据满足前保持「启动页」（[t1-7-04](t1-7/t1-7-04-hysteresis-transition.png)），随后按判据离开（t1-7-05） |
| 会话终止重置 | 应用主动停止 → 「启动页 → 未知（采集会话终止）」13:36:54；系统侧回收后悬浮窗仍挂载、显示「未知」（[t1-7-06](t1-7/t1-7-06-session-dropped-persist.png)） |
| 演练模式明示 | 授权页「运行方式：演练（只识别、不点击）」常驻卡片（[t1-7-02](t1-7/t1-7-02-run-mode-card.png)、[t1-7-08](t1-7/t1-7-08-session-stopped-ui.png)） |
| 零点击 | 演练全程应用日志仅会话 / 前台 / 悬浮窗 / 状态 / 统计类，无任何注入动作记录；全部点击（含「换区」）均为测试侧 adb 注入并以 MM-Marker 标注，属测试行为，非应用发出（红线 1 / 2、NFR-05 延续审计） |

补充说明（环境事实，记录备查）：
- 帧画布方向：首轮会话授权时游戏已在前台（横屏姿态）→ 画布 3168×1440，运行期如实告警「尺寸不一致」（设计行为，见 T1-5 记录）；按流程在 MM 应用前台（竖持）重建后画布 1440×3168 与标定产物一致，无告警；
- 共享路径与前台联动：选择「共享一个应用」完成建会话后，系统将共享目标（游戏）自动带回前台，识别循环随之恢复（13:36:18）；游戏进程被测试侧重启后 vivo 回收共享会话（ACTIVE -> INACTIVE，来源：系统侧回收），重启游戏再次出现「主应用 / Ⅱ·分身」实例选择器，按既定口径选择主应用（同 T0-3 先例）；
- 会话重建期间无障碍服务曾短暂断开重连（13:29:39–40，来源：重置 / 已连接），识别循环冻结一轮不消耗滞回计数（FR-09 行为正确，见日志）；
- 状态信号变化日志含判据（「连续 2 次命中（进入）/ 连续 3 次未命中（离开）」），悬浮窗文字经主线程编组更新，与会话 / 前台信号同模式。

## M1-T1-9 性能基线核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168（标定帧一致）
- 核对对象：单帧处理耗时统计（连续 100 帧满窗；T1-9a/b）真机基线
- 原始记录：[t1-9-01-runtime-log.txt](t1-9/t1-9-01-runtime-log.txt)（真机运行日志摘录，13:56–14:47 四轮迭代）、[t1-9-02-findings.txt](t1-9/t1-9-02-findings.txt)（中间发现记录）

满窗统计（4 条，生效口径为最后一条）：

| 时刻 | 输出 | 说明 |
| --- | --- | --- |
| 14:08:15.999 | P95 2202.7ms，平均 2011.1ms，最大 2239.8ms | 统计首版（无分段） |
| 14:13:16.785 | P95 2274.6ms，平均 2053.1ms，最大 2308.6ms | 统计首版（无分段） |
| 14:35:14.496 | 总 P95 2291.2ms（灰度/识别 null） | 分段首版（null 缺陷，当日修复） |
| **14:43:55.376** | **总 P95 2287.6ms（灰度 52.7ms / 识别 2241.2ms），平均 2012.8ms，最大 2361.7ms** | **最终有效数据** |

- 结论：首轮真机基线与 NFR-01 阈值（20ms）相差约 114 倍，**如实记录为未达标**；构成上识别（模板匹配）占 98%、灰度占 2.3%，瓶颈明确。
- 桌面同源探针（JVM，同产物同帧）：灰度全帧 ≈5.7ms、匹配（全窗 NCC）≈45ms；设备/桌面慢约 48–50×。
- 附带核对：节流两档真机成对切换（1000↔3000ms）与帧管线统计节奏正常（NFR-02 初版证据）；识别命中与状态变化链路正常（「未知 -> 启动页」多处复现）。
- 环境事实（记录备查）：① 帧流静默停止——发现 2（用户换区 / 游戏重启场景下「共享一个应用」帧流不再投递且无回调；帧线程存活但无帧，悬浮窗维持「启动页」外观无异常提示）——已登记后续任务（帧流静默检测）；② 标定帧池 62 文件即停为预期行为（录制开关未开）；③ 分段统计 null 缺陷当日修复（三统计器逐帧全量记录后再判满窗）。
- 整改：进入 T1-10（任务卡见 `../plans/m1-recognition.md`）。

## M1-T1-10 性能达标优化离线取证（2026-09-12）

- 对象：T1-10a 匹配优化（SAT 位置统计量 + 交错半采样粗搜 + 代表点全精度精搜）、T1-10b 窗口化灰度与帧线程优先级、T1-10c 离线回归与提速取证
- 原始记录：[t1-10/](t1-10/)——优化前后逐帧诊断（各 104 样本）+ [对照结论文档](t1-10/t1-10-03-offline-regression-summary.txt)

| 核对点（T1-10c） | 结果 |
| --- | --- |
| 判定零漂移（固定批次 104 帧） | 结论不一致 0；≥0.5 分档最高分差 0、位置漂移 >2px 为 0 |
| 尾部差异（如实记录） | 最高分最大差 0.0007、竞争分/差距最大差 0.0276（均为低分尾部） |
| 桌面提速（识别耗时 ×104 帧） | P50 74→10ms、P95 79→11ms、平均 74.88→10.12ms（≈7.4×） |
| A7 指标复跑 | 报告与 T1-6e 逐字段一致（零误报结论不变） |
| 单测 | 优化一致性（穷举对照）/区域灰度/窗口映射/端到端等价新增用例全过；全量 170 例 0 失败（24 套件，临时探针删除后复跑） |

- 真机复审（T1-10d）：已执行（2026-09-12 真机，见下节）。

## M1-T1-10d 真机复审核对记录（2026-09-12）

- 设备：vivo V2463A / Android 16（API 36）/ 1440×3168（与标定帧一致）
- 核对对象：T1-10 整改构建真机演练——连续 100 帧满窗 P95（A9 达标线 < 20ms）+ 节流两档切换节奏
- 原始记录：[t1-10-04-real-device-verification.txt](t1-10/t1-10-04-real-device-verification.txt)（两会话日志摘录 + 满窗统计 + 采集方法）；截图：[会话 1](t1-10/t1-10-05-launch-page-session1.png)、[会话 2](t1-10/t1-10-06-launch-page-session2.png)（游戏「启动页」+ 悬浮窗「状态: 启动页」）

满窗统计（会话 1 ×2 + 会话 2 ×1，结果一致）：

| 输出时刻 | 总 P95 | 灰度段 | 识别段 | 平均 | 最大 |
| --- | --- | --- | --- | --- | --- |
| 15:54:46.395 | 315.8ms | 20.6ms | 297.6ms | 292.8ms | 324.8ms |
| 15:59:47.078 | 312.8ms | 21.4ms | 294.3ms | 298.4ms | 314.9ms |
| 16:08:29.513 | 327.8ms | 27.6ms | 308.0ms | 304.5ms | 376.2ms |

- 结论（A9）：P95 312.8–327.8ms，**未达标**（差约 15.6–16.4 倍），如实记录；A9 维持未勾选。
- 整改效果：较 T1-9 基线（2287.6ms）提速 ≈7.0–7.3×（识别段 ≈7.3–7.6×、灰度段 ≈1.9–2.6×）；瓶颈仍在识别段（≈94%）；设备/桌面识别倍差 ≈27–28×（旧实现 ≈48–50×）。
- 节流两档（NFR-02 初版）：两会话成对复现——1000→3000（稳定轮）→ 3000→1000（变化 / 过渡轮，随「未知 → 启动页」命中）→ 1000→3000（稳定轮）。
- 帧管线：稳定态 10s 窗口接收 535–583 帧 / 处理 3–5 帧，统计连续无中断（会话 1 77 条 / 会话 2 30 条）；会话 1 建立瞬间含 1 轮 FR-09 冻结（随前台切换恢复，不消耗滞回计数）。
- 后续：按任务卡备注评估 3×3 采样 / 部分和剪枝（另行记录）。

## M1-T1-10e 后续提速手段评估（3×3 采样 / 部分和剪枝，2026-09-12）

- 对象：T1-10 备注的后续手段（2×2 采样不足以达标后评估 3×3 采样与部分和剪枝）；纯离线 JVM 探针，不改生产代码。
- 结论文档：[research-t1-10e-speedup-evaluation.md](../recognition/research/research-t1-10e-speedup-evaluation.md)
  （含方法、数据、机理分析与不采纳理由、结构性建议）
- 复跑与数据：`.\gradlew.bat ':app:testDebugUnitTest' --tests '*SpeedupProbeTest*' --rerun`
  → `app/build/replay-work/out/t1-10e-speedup.txt`（工作目录不在版本库）
- 结果（固定批次 51 有效帧，信号 launch_start）：

| 手段 | 平均(ms) | 提速比 | 结论不一致帧 | best 位置不一致帧 | best 分数最大差 |
| --- | --- | --- | --- | --- | --- |
| 基线（采样2 网格2 K64） | 10.27 | ×1.00 | 0 | 0 | 0.000000 |
| 3×3 采样 | 7.39 | ×1.39 | 0 | 7 / 51 | 0.001118 |
| 部分和剪枝 | 9.69 | ×1.06 | 0 | 0 | 0.000000 |
| 3×3 采样 + 剪枝 K32 | 6.06 | ×1.70 | 0 | 7 / 51 | 0.001118 |

- 自校验：复刻基线 ≡ 生产 `TemplateMatcher.findPeaks`，不一致 0 帧（探针与生产口径一致）。
- **决定：均不采纳**（3×3 有位置漂移、剪枝收益过小；最优组合推算真机 P95 仍 ≈184ms，距 20ms 差约 9 倍）。
- **A9 判定：M1 内不勾选，如实记录**；NFR-01 最终验收在 M5（同 T1-10 任务卡备注）。

## M1-T1-11 画布方向适配（离线 + 真机证据，2026-09-12）

> 非 A1–A9 验收项（真实使用可用性修复）；T1-11 全切片（a–d）完结。

- 决策：[ADR-007](../decisions/ADR-007-画布方向适配.md)（识别层「按画面区运行时归一」，不改采集画布）。
- 几何测量与结论：[../recognition/research/research-canvas-orientation.md](../recognition/research/research-canvas-orientation.md)
  （无旋转；横 → 竖 = ×2.2 + 平移 1256；竖画布内容带 655 行 @ y1256 实测一致；真机横画布会话样本复核：带内相关 0.7419、锚点残差 0,1px）。
- 实现：`CanvasGeometry` / `FrameNormalizer`（纯逻辑）+ `RecognitionLoop` 检测前归一 + 采集层窗口区域映射 + 产物带入几何；运行期日志由「识别结果可能不可用」改为「方向不同，已归一并继续识别」。
- 真实帧离线复演（生产路径）：竖画布帧 1440×3168 → `MATCHED 0.9989 @651,1744`；横画布帧 3168×1440 归一后 →
  `MATCHED 0.9516–0.9538 @651,1744`（**位置残差 0px**）；两会话均进入「启动页」。
- **真机核对（T1-11d）**：[t1-11d/](t1-11d/)——三条会话：竖画布回归 `未知 -> 启动页`（17:57:08，无方向提示）；
  **横画布 3168×1440** 会话 `未知 -> 启动页`（18:05:58）且日志为 info「方向不同…已按画面区几何归一后继续识别」，
  全程无降级告警；同批录制横画布样本 37 帧（[samples-index set-20260912-05](../recognition/samples-index.md)）。
  证据件：[t1-11d-11 真机取证](t1-11d/t1-11d-11-real-device-verification.txt)、[t1-11d-12 运行日志](t1-11d/t1-11d-12-runtime-log.txt)、
  [竖画布会话截图](t1-11d/t1-11d-07-session1-launchpage.png)、[横画布会话截图](t1-11d/t1-11d-10-session2-landscape-launchpage.png)。
- 单测：新增 13 例（几何 7 / 归一 5 / 探针真实帧 1）；全量 27 套件 184 例零失败。
- 复跑：`.\gradlew.bat ':app:test'`（几何 / 归一单测 + `CanvasOrientationProbeTest` 探针）。

## M1-T1-13 预期驱动口径真机复测（2026-09-13）

> 非 A1–A9 验收项（识别口径收敛为「预期驱动」+ 节流人速化）；**T1-13 七条判据全部通过**。

- 任务卡：[../plans/m1-recognition.md](../plans/m1-recognition.md) §T1-13；详细验收 + 原始日志：
  [t1-13a 报告](t1-13/t1-13a-device-expected-driven.md)、[t1-13a 原始日志](t1-13/t1-13a-device-log.txt)
  （35 行，09-13 07:44:20 ~ 07:46:27）。
- 设备 / 会话：vivo V2463A / Android 16；画布 1440×3168（与标定帧一致，未触发方向归一分支）；标定 7 信号。

| 判据 | 结果 |
| --- | --- |
| 取消按需补扫 | 未命中轮仍只搜子集：`启动页 -> 未知（连续 3 次未命中（离开））；本轮搜索 「launch_start」` |
| 取消周期兜底全扫 | 静置 10 个统计窗口的全扫轮数**全为 0** |
| 保留「状态未知 → 全扫」 | 3 次 `本轮搜索 全部信号` **全部**对应 `未知 -> X` |
| 降档判据 = 确实稳定 | `200ms -> 1000ms（确实稳定轮）` / `1000ms -> 200ms（变化 / 过渡 / 未达预期轮）` 成对出现，静置期无抖动 |
| 日志自证搜索范围 | 转移轮输出本轮信号名；统计行输出「实际参与匹配 N 次（M 轮：子集 X / 全扫 Y）」 |
| 状态序列符合预期 | `启动页 →（连续 3 次未命中）→ 未知 →（全扫，连续 2 次命中）→ 选择服务器`，与任务卡 ⑦ 完全一致 |
| 零点击 | 会话内无动作类日志（纯识别路径，红线 1 / 2 延续） |

- 实测数据：静置 **7~8 轮 / 10s**（稳定档周期 = 1000ms 休息 + 单轮耗时）；子集轮 P95 **298.3ms**（平均 283.4ms）；
  全扫轮单轮 ≈**790ms**（未知态 2 轮 1.973s 反推，与推算 993ms 同量级）；被动切换总延迟 ≈**3.5s**（预期代价）；
  接收 415~507 帧 / 10s → 处理 7~8 帧（**≈98% 帧被节流跳过**）。
- 同批口径变更（需求已同步）：NFR-02 改为「需要响应 ≤0.2s / 确实稳定 ≤1s；间隔 = 两轮之间的休息时间」；
  FR-04 第 2 步条件化为「仅起点为农场 / 好友的农场时」，并写明「大厅发起 = 农场发起流程的子集」；§7-7 关闭。
- 未覆盖：大厅 / 活动弹窗状态的稳态耗时（本次路径为启动页 ↔ 选择服务器）；流程驱动（有明确预期）场景的
  步骤延迟（M1 无编排，M2 落地后补测，当前推算 ≈0.9s / 步）。
- 复跑：`.\gradlew.bat ':app:testDebugUnitTest' ':app:assembleDebug'` 全量通过（2026-09-13）；
  真机取证：`adb logcat -d -b all --pid=<MM 进程> | Select-String 'MM-'`。
