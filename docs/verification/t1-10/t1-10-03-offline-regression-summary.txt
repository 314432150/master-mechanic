# T1-10 性能达标优化 — 离线回归与提速取证（2026-09-12）

> 归档件：优化前后逐帧诊断（同批次、同标定产物）+ 判定一致性对照 + 桌面提速统计 + 复现命令。
> 对应任务卡：`docs/plans/m1-recognition.md` T1-10a / T1-10b / T1-10c；真机核对（T1-10d）待设备在线后追加。

## 口径

- 固定批次：set-20260912-03（104 帧；批次与 A7 同源）
- 标定产物：calib-v1（命中线 0.85 / 差距线 0.1 / 峰值间距 5）
- 判定记录字段：结论（MATCHED / UNRELIABLE / 未命中）× 最高分@位置 × 竞争分@位置 × 识别耗时
- 语义约束：对判定起作用的分数全部为全精度值（粗搜仅用于筛候选；精搜/判定输入零变化），详见 T1-10a 设计

## 归档文件

| 文件 | 内容 |
| --- | --- |
| t1-10-01-diagnostics-baseline.txt | 优化前（全窗 NCC 穷举）逐帧诊断，104 样本 + 表头 |
| t1-10-02-diagnostics-optimized.txt | 优化后（SAT + 采样粗搜 + 全精度精搜）逐帧诊断，104 样本 + 表头 |
| t1-10-03-offline-regression-summary.txt | 本文件：对照统计与复现命令 |

两诊断文件均由回放测试 ReplayBatchRunTest.diagnoseBatch 生成（同批次、同产物、同入口）。

## 判定一致性对照（逐帧逐字段）

- 样本数：104
- 结论不一致：**0**（MATCHED / UNRELIABLE / 未命中 全部一致）
- 位置漂移（≥0.5 分档）超过 2px：**0**
- 最高分最大差：**0.0007**（frame-0002906694947.png，低分尾部）
- ≥0.5 分档最高分最大差：**0**（强分帧逐帧完全一致）
- 竞争分最大差：**0.0276**（frame-0002907330919.png：0.4166 → 0.3890，低分尾部）
- 差距（最高−竞争）最大差：**0.0276**（同上帧）
- 说明：全部差异集中在低分尾部；与判定直接相关的字段（结论 / 强分 / 位置）零漂移。

## 桌面提速（识别耗时列 ×104 帧，JVM 热身后）

| 指标 | 优化前 | 优化后 | 加速 |
| --- | --- | --- | --- |
| min | 70ms | 10ms | 7.0× |
| P50 | 74ms | 10ms | 7.4× |
| P95 | 79ms | 11ms | 7.2× |
| max | 125ms | 12ms | — |
| 平均 | 74.88ms | 10.12ms | 7.4× |

同期桌面探针分解（T1-9 现场探针，优化前）：灰度全帧 ≈5.7ms；匹配（全窗 NCC）≈45ms——识别段为绝对大头，优化方向正确。

## A7 指标复跑

- 优化后重跑报告与 T1-6e 报告逐字段一致（判定记录不变 → 误报指标不变：85 负样本 0 误报，正样本 18/19）。
- 复现命令：`.\gradlew.bat ':app:testDebugUnitTest' --tests '*ReplayBatchRunTest*' --rerun`

## 单测证据（与数据同批次提交）

- TemplateMatcherOptimizedTest：优化实现与穷举参考在「单峰 / 重复目标 / 遮罩 / 窗口边界 / 弱纹理（判定口径）」逐用例一致；边界与确定性覆盖。
- RgbaToGrayTest（区域转换 4 例）：区域灰度与全帧转换在区域内逐位一致；越界裁剪 / 重叠幂等 / 空区域。
- RecognitionLoopTest（windowRegions / 端到端等价）：窗口区域映射与标定一致；窗口化灰度与全帧灰度端到端判定记录逐字段一致。
- 全量回归（删除临时探针文件后复跑）：170 例 / 0 失败（24 套件）。

## 复现路径

1. 回放诊断：`.\gradlew.bat :app:test --tests '*ReplayBatchRunTest*' --rerun`（输出 app/build/replay-work/out/diagnostics.txt）
2. 对照：见本目录两诊断文本；对照脚本口径为逐帧逐字段比对（结论 / 最高分 / 竞争分 / 位置 / 差距）

## 真机核对（T1-10d，待执行）

- 前置：设备（vivo V2463A）在线；安装含 T1-10a/b 的构建。
- 方法：演练模式长跑；收集「单帧处理耗时统计」满窗输出（灰度段 / 识别段 / 总段 P95）与节流两档切换日志。
- 达标线：总段 P95 < 20ms（NFR-01）；节流节奏保持（NFR-02 初版）。
- 桌面参考：识别段 10–12ms；设备/桌面存在固有倍差（旧实现实测约 48–50×，新实现待实测）。
- 结果如实记录并追加至本目录；若超标按任务卡备注评估后续手段（3×3 采样 / 部分和剪枝等，另行记录）。
