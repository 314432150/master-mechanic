# 进度日志

> 活文档：**只追加**。状态口径：未开始 / 进行中 / 已完成 / 已暂停。
> 每条记录含日期、结论、证据链接；历史条目不删改，更正用新条目说明。

## 里程碑总览

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| M0 | 最小链路骨架（授权 / 前台判定 / 服务壳 / 悬浮窗壳） | 已完成 |
| M1 | 识别闭环（状态机、判定审计、回放、标定） | 进行中 |
| M2 | 自动异常处理（FR-01 / FR-02） | 未开始 |
| M3 | 巡查配置 + 悬浮窗完整体验（FR-03 / FR-07） | 未开始 |
| M4 | 巡查主流程（FR-04 / FR-05 / FR-06） | 未开始 |
| M5 | NFR 加固与验收 | 未开始 |

## 下一步

1. M1 收尾（顺序：① T1-11 画布方向动态适配 → ② T1-10 后续手段评估 → ③ 勾选 A9）：T1-9 基线、T1-10a/b/c 离线整改与 T1-10d 真机复审（2026-09-12：P95 ≈313–328ms，未达标，如实记录）均已归案；**①T1-11 画布方向动态适配（已完成：a 测量 / b 定稿 ADR-007 / c 实现与单测 / d 真机核对）**：结论「无旋转，等比缩放 + 居中平移」→ 按「画面区运行时归一」实现（离线复演残差 0px）→ 真机三条会话核对通过（竖画布回归 + 横画布 3168×1440 会话均 `未知 -> 启动页`，无降级告警），真机横画布样本 37 帧归档；横竖画布均正确识别，不再依赖用户旋转锁定；②T1-10 后续手段评估（3×3 采样 / 部分和剪枝）；③A9 勾选（M1 DoD 唯一剩余项）
2. 标定补充排期（`plans/m1-recognition.md` T1-8，非 M1 验收项）：M2 前置（活动弹窗多样式 / 新手引导 / 新手大厅）与 M4 前置（巡查状态）分批标定；「选择服务器」（T1-8a）设备在手时可先行，复演「启动页 ↔ 选择服务器」转移
3. 并行推进 R-1、R-2 研究任务的样本收集
4. T1-5l 标定流程重构与 T1-5m 工作台交互定稿**实现 + 离线验证 + 真机核对全部完成**（2026-09-12，证据 `verification/t1-5l/`）；期间修复「参数编辑写入落后一次编辑」缺陷（T1-5l 真机核对发现）。
5. ②T1-10e 后续提速手段评估**已完成**（2026-09-12）：3×3 采样 ×1.39（位置漂移 7/51 帧）、部分和剪枝 ×1.06（零漂移）、组合 ×1.70——**均不采纳**（最优组合推算真机 P95 ≈184ms，距 20ms 仍差约 9 倍）；结论与结构性建议见 `recognition/research/research-t1-10e-speedup-evaluation.md`。
7. 新增 T1-10f 多信号成本与分段剖析（2026-09-12，用户追问驱动）：**既有真机性能数据全部只有单信号**
   （设备已 7 信号，从未测过多信号）——桌面 7 信号 = 单信号 ≈3.85×，推算真机或 ~1.1s/帧；分段显示
   **粗搜 ≈79% / 精搜 ≈20% / 前缀和 ≈1%**（推翻精搜瓶颈假设）。新方案清单见
   `recognition/research/research-t1-10f-cost-profile.md`（按状态启用信号子集 / 收紧窗口 / 多尺度粗搜 /
   工程侧降分配）。**下一步建议：先补 7 信号真机基线（装机 + 用户长跑），再定优化顺序。**
9. 完成 T1-10g 按状态启用信号子集（2026-09-12 深夜，用户指出"实际只需搜一个信号"后启动）：新增
   `ActiveSignalSelector`（未知/转移探测/周期兜底→全扫，稳定态→自身信号+附加集）+ `SignalDetector` 子集判定
   （**子集外信号不产生判定记录**，不是记为未命中）+ `RecognitionLoop` 按需补扫（未命中当轮补扫其余信号，
   消除转移延迟）。实测（设备 67 帧 + 7 信号产物，生产路径）：**状态序列与全扫 0/67 差异**；
   **搜 1 个信号 2.31ms vs 全扫 36.80ms（≈1/16）**；采集图整体 ×1.29（76% 帧触发全扫：30% 未知 + 补扫 + 周期），
   真实停留已知状态时 ≈62ms/帧（真机推算，对比 ≈993ms）。全量单测 214 例零失败；证据
   `recognition/research/research-t1-10g-subset.md` + `out/t1-10g-subset-savings.txt`。待办：真机复核、
   确认"登录→大厅前"附加集、period 调整、与收紧窗口组合。
10. ③**A9 已定稿（2026-09-13）**：**不勾选、如实记录**——间隔项已达标（1000/3000ms）；耗时项未达标
    （全扫 ≈993ms、子集稳态 ≈62ms，均 > 20ms，差约 3×）；NFR-01 / NFR-02 最终验收在 M5（与 T1-9 / T1-10
    任务卡既有备注一致）。验收清单与 `verification/README.md` 已注明差距与后续方向（收紧搜索窗口 ≈2× 可进一步逼近）：按评估结论 M1 内 A9 **不达标、不勾选（如实记录）**，NFR-01 最终验收在 M5（T1-10 任务卡既有备注）；若接受此结论，M1 收尾只剩 T1-8 标定补充（设备在手可先做「启动页 ↔ 选择服务器」转移复演）与产物归档复演，以及是否把「按状态启用信号子集 / 多尺度粗定位」登记为后续性能任务

> **交接提醒（2026-09-12）**：`app/build/replay-work/` 是回放工作区（固定批次 `set-20260912-03`：104 帧 + 清单，另含标定产物副本、原始帧池与诊断出参）——**`gradlew clean` 会将其整体清空**，回放回归（`ReplayBatchRunTest`）依赖之，勿随意 clean；若已被清，需从设备重新导出（帧池位于应用私有目录 `files/calibration/frames`）。

## 阻塞与待确认

| 事项 | 出处 | 状态 |
| --- | --- | --- |
| 拜访完成后如何返回自己的农场/大厅 | requirements §7-1 | R-1 研究中 |
| 如何确认已进入目标好友的农场 | requirements §7-2 | R-2 研究中 |
| 帧流静默停止检测（换区 / 游戏重启场景下帧流不再投递且无回调，悬浮窗无异常提示） | T1-9 发现 2（`verification/t1-9/`） | 待登记任务（建议随 FR-08 加固纳入后续里程碑） |
| 标定流程重构：全屏页统一承载「挑帧 / 框选 / 归属状态 / 直接保存产物」，取消页内预览大图与「提取为模板」步骤 | T1-5l（2026-09-12 用户提出） | **已关闭（T1-5l + T1-5m 完成）**：锚点移除 / 草稿模型废除 / 全屏工作台 / 产物可查看可删除 / 参数编辑合法即写入 / 几何不一致拒绝写入；T1-5m 按用户定稿改为「✓ 确认式写入」并重排工作台（全屏大图 + 底部悬浮条 + 白色外框恢复）。单测 200 例零失败 + 真机核对通过（含修复「参数写入落后一次编辑」缺陷），证据 `verification/t1-5l/`（t1-5l-01/12、t1-5m-01 及截图），产物核对后已还原 |
| 画布方向动态适配（横画布会话下帧尺寸与竖屏标定产物不一致、识别全失效；现有保障仅为「授权时 MM 竖屏前台」流程约定，对真实用户环境不可控） | T1-10d 真机复审（`verification/t1-10/`）+ 2026-09-12 用户口径 | **已关闭（T1-11 完成）**：a 测量 / b 定稿（ADR-007 按画面区运行时归一）/ c 实现与单测 / d 真机双方向核对（竖画布回归 + 横画布 3168×1440 会话均 `未知 -> 启动页`，无降级告警）；证据 `verification/t1-11d/`、`recognition/research/research-canvas-orientation.md`；真机横画布样本 37 帧见 samples-index set-20260912-05 |

## 日志

### 2026-09-11
- 建立 `AGENTS.md` 与 `docs/` 文档体系骨架（本条目之后的所有变更按工作流记录）。
- `requirements.md` 迁移入 `docs/requirements.md`，同步更新 `AGENTS.md` 与 `docs/README.md` 的引用。
- 完成 T0-1：四条 ADR 状态为「接受」（[ADR-001](decisions/ADR-001-画面采集手段.md)、[ADR-002](decisions/ADR-002-输入注入途径.md)、[ADR-004](decisions/ADR-004-悬浮窗实现途径.md)、[ADR-005](decisions/ADR-005-前台判定途径.md)）；`architecture.md` 开放决策表已同步；ADR-002 确定「间隔 ≥300ms」唯一实现点为执行层点击转发器（口径：前次手势完成 → 下次手势开始不低于 300ms）。
- 完成 T0-2 授权流代码：三项关键授权（无障碍 / 屏幕采集会话 / 通知（Android 13+））状态展示与一键授予引导；`:app:test` 通过（AuthorizationSummaryTest 4 例 + 模板 1 例）、`:app:assembleDebug` 通过；真机核对（授权矩阵截图）待执行。
- 完成 T0-2 真机核对（vivo V2463A / Android 16）：授权三态矩阵（全缺 / 缺一项 / 全部已授）与会话制重置验证通过，证据见 `verification/t0-2/`；边界发现：vivo 强停应用后会撤销其无障碍授权，界面正确提示「未开启」（符合 FR-08 不静默失败）。
- 修订 ADR-001（采集范围）：新增第 6 条决策（Android 14+ 引导单应用、低版本回退整屏、识别层不依赖跨应用画面）；悬浮窗入画后果改为双路径表述；授权卡采集文案同步补充选应用引导。
- 完成 T0-3 前台判定：无障碍服务接入「窗口事件 + 1.5s 周期复核」双来源信号，判定失败一律非前台；`:app:test` 通过（ForegroundEvaluatorTest 5 例全过）；真机时序核对（vivo V2463A，王者荣耀实机切换）延迟 343ms–1.6s，证据见 `verification/t0-3/`。
- 完成 T0-4 服务壳：常驻前台服务（`ResidentService`，specialUse 类型）+ 常驻通知 + 界面「运行状态」卡片（启动 / 停止 + 通知不可见警示）；真机观察（vivo V2463A）切后台 / 熄屏各 2 分钟服务保持存活、通知常驻可见，证据见 `verification/t0-4/`；范围界定：真实采集会话（mediaProjection 类型服务 + 虚拟显示）顺延至采集层任务，详见任务卡备注。
- 完成 T0-5 悬浮窗壳：无障碍悬浮窗收起态手柄（`TYPE_ACCESSIBILITY_OVERLAY`，整窗不可触摸 / 不可聚焦恒穿透，右上角停靠），可见性由前台信号驱动——前台挂载、非前台整窗移除、回前台收起态重建；`:app:test` 回归通过、`:app:assembleDebug` 通过；真机核对（vivo V2463A + 王者荣耀）：三态可见性截图 + 覆盖区点击穿透对比（游戏静音图标状态切换）+ 窗口 NOT_TOUCHABLE 属性与几何证据，证据见 `verification/t0-5/`。
- 完成 T0-6（验收 A6）零点击审计：①代码静态审计——全库 `*.kt` 检索注入类 API（dispatchGesture / performAction / MotionEvent / KEYCODE / shell 执行等）零调用（唯一命中为 androidTest 模板测试的标准 API，不进入发布 APK）；②真机运行采样（守护场景静置 + 前后台切换）——应用进程全量日志仅「前台状态变化」与「悬浮窗挂载 / 移除」两类，无注入动作记录；起止截图确认游戏始终停留登录页未被推进。M0 六项验收 A1–A6 全部通过，DoD 达成，计划文件状态置「已完成」。证据见 `verification/t0-6/`。
- 里程碑流转：M0 经 PR #1 合入 `main`（merge commit d3c5c53），自 `main` 新建 `stage/m1`；建立 M1 计划 `plans/m1-recognition.md`（识别闭环：采集会话 / 识别引擎 / 状态机 / 标定 / 回放与指标 / 演练模式；验收清单 A1–A9；任务卡 T1-1 ~ T1-7，其中 T1-1 = 落地 ADR-003 识别技术路线，先行）。
- 完成 T1-1（ADR-003）：接受识别技术路线——标志检测用归一化模板匹配（局部搜索窗口 + 多模板，输出分数 / 位置 / 竞争位置，天然支撑 §5-1 / §5-2 判据），名称定位走文字识别方向（首次实现名称定位的任务中验证，验证前相关步骤保持「停下并提示」）；模板 / 窗口 / 阈值全部来自标定产物（零硬编码）。已同步决策索引、architecture 开放决策表；M1 状态置「进行中」。
- 完成 T1-2（验收 A1）：采集层——采集会话。`CaptureService`（mediaProjection 前台服务）承载 VirtualDisplay + ImageReader 帧管线（~60fps 接收、1s 节流处理，T1-2 固定值）；会话状态经 `CaptureSessionSignal` 统一输出（建立 / 应用主动停止 / 系统侧回收，系统侧终止经 MediaProjection.Callback.onStop），授权界面「采集中」与停止入口由信号驱动；真机核对五场景通过：建立 → 维持 → 切走约 2.5 分钟不终止（dumpsys 确认会话活跃、回前台帧管线恢复）→ 应用主动停止 → 系统侧回收；全程零点击。M0-T0-4 顺延项完结，证据见 `verification/t1-2/`。
- 完成 T1-3（验收 A2 / A3 / A4）：识别引擎（纯逻辑）——标志检测 → 判定记录 → 可信性判定落于 `recognition` 包（帧数据 / 模板 / 比例搜索窗口 / 匹配参数 / 判定记录），基于归一化模板匹配（NCC）：判定输出「命中标志、位置、置信度、最强竞争位置」四项齐全（§5-1），不可信判据（多处高分 / 最高与次高差距过小）按未识别处理（§5-2）；无安卓依赖、零设备常量（模板 / 窗口 / 阈值全部注入）。JVM 单测新增 18 例全过（定位 / 亮度对比度不变性 / 遮罩容忍 / 窗口换算 / 峰值抑制 / 判定边界 / 三次重跑逐字段一致），全量回归 32 例零失败；证据见 `verification/t1-3/`。

### 2026-09-12
- 完成 T1-4（验收 A5）：状态机（滞回）——§2.1 全状态 + §2.2 滞回（进入连续 2 次命中 / 离开连续 3 次未命中 / 连续未命中任何标志 → 未知）+ 非前台冻结不消耗连续计数（FR-09）；识别循环接入采集帧管线（帧 → 灰度 → 判定 → 状态机 → 日志），NFR-02 两档自适应节流初版随之落地（变化 1000ms / 稳定 3000ms，冻结轮不调整）。JVM 单测新增 28 例全过（滞回时序 / 冻结 / 信号映射 / 帧适配 / 节流动态调整），全量回归 60 例零失败；真机核对（vivo V2463A）：识别循环运行、冻结 / 恢复 ×3 / ×2 与前台信号一一对应、稳定态间隔实测 ≈3000ms（NFR-02 稳定档）、状态恒「未知」（未标定设计预期）、全程零点击；证据见 `verification/t1-4/`。真实标志驱动的状态切换观察随 T1-5 标定产物补充（任务卡备注既定）。真机期间排查两起设备侧环境事实（应用进程日志静默丢失后重启恢复、屏幕超时熄灭触发系统侧回收采集会话），已记录备查。
- 完成 T1-5（验收 A6）：标定——产物模型与文本编解码（纯逻辑）、应用侧采集与标注（帧样本录制 → 挑帧 → 框选 / 命名 / 设参 → 保存产物）、采集会话建立时加载产物接入识别循环；标注交互按真机反馈增强（双指缩放 / 平移 + 选框拖动 / 拉伸，纯运算单测覆盖）。真机标定（vivo V2463A，启动页）：48 帧样本 → 框选「开始游戏」→ 产物（frame=1440x3168、signal launch_start、模板 131×37、参数 0.85/0.10/5）→ 会话重建加载产物 → 真实标志驱动识别命中「未知 -> 启动页」（连续 2 次命中进入，其后 6 个统计窗口持续保持），补 T1-4 备注事项；产物审查确认零设备绑定（帧尺寸 / 窗口 / 参数均读自产物文件）。JVM 单测 T1-5 新增 44 例（产物编解码 / 标注工作区 / 标注几何 / 模板提取等），全量回归 104 例零失败；证据见 `verification/t1-5/`，样本索引见 `recognition/samples-index.md`。真机核对期间记录环境事实：帧画布方向随授权时持机姿态变化（标定 / 运行需同姿态），应用进程日志曾静默丢失（进程重启恢复）。
- 完成 T1-5 交互增强轮收尾（真机标定反馈 T1-5e–T1-5j，任务卡见 `plans/m1-recognition.md`）：e 归属状态绑定默认信号名 + 「新手大厅 / 新手引导」拆分 + 框选改全屏画布（关闭钮清选框）；f 布局聚焦（缩略图方块化、说明入 ⓘ、预览限高同屏）；g 信号名输入框移除（名称由归属状态默认名决定）；h 预览滑页切帧（缩略图随动）+ 双击进全屏 + 画布内全屏图标；i 预览画布高度占位（切帧不塌缩、加载 / 失败态内嵌）；j 缩略图联动改「过半即同步」（消除定格滞后）。全量回归 16 套件 112 例零失败；真机（vivo V2463A）逐轮安装核对通过；核对记录补入 `verification/README.md` T1-5 分节。
- T1-6e 素材准备：第二批真机样本（换区巡查全链路 62 帧）从设备帧池拉取归档至 `app/build/replay-work/all/`（前批 43 帧移存 `all-batch1-43f/`）；逐帧清点核实链路：启动页（392区）→ 大厅 + 弹窗轮播 → 农场 / 好友面板 → 设置 → 退出登录确认框 → 切区（418区）→ 新角色登录播片 → 新手引导（「开局送英雄！」）→ 新手大厅（「新手豪华登录礼」）→ 大厅；FR-02 两状态的界面标志线索经用户口径核实并登记 `recognition/samples-index.md`（set-20260912-02，含 2 张代表帧入库）；命名口径：状态名沿用 §2.1 / 术语表，活动文案仅作标志模板素材。T1-6e 待办：固定批次清单标注（负样本 ≥60、三类干扰分列）与首份指标报告。
- 完成 T1-6e（验收 A7）：固定批次采集与首份指标报告——两次真机采集合并为固定批次 `set-20260912-03`（104 帧：新批 62 + 前批 42，排除 1 帧画布过渡帧），逐帧清单标注（positive 19 / neg-mask 18 / neg-motion 4 / neg-similar 10 / neg-plain 53；负样本合计 85 ≥ 60）→ JVM 离线回放（灰度入库，重解码逐位等值）→ 指标报告：全部类别零误报（上界：遮罩 ≤0.1533 / 动背景 ≤0.5271 / 相似多目标 ≤0.2589 / 普通 ≤0.0550），正样本命中 18/19（唯一未命中为「开始游戏」按下高亮瞬态帧，属点击后瞬态、不在识别时机范围，如实记录）；同数据 + 同参数运行 3 次判定记录逐字段一致。工具侧修复大批次内存模式（报告生成由全量解码改为逐帧流式，批次规模不受测试堆限制，结果等价）。逐帧分类期间复核修正 1 帧误标（切区流程中的选择服务器页由「启动页」修正为 neg-similar）。T1-6 全部切片（a–e）完结。证据见 `verification/README.md`（M1-T1-6 分节），样本索引 `recognition/samples-index.md`（set-20260912-03），报告 `recognition/reports/report-20260912-t16e-batch01.md`，A7 勾选。
- 完成 T1-7（验收 A8）：演练模式与状态展示——①界面状态信号 `UiStateSignal`：识别结论独立对外输出（变化日志含判据、订阅通知；会话重建 / 终止重置「未知」）；②悬浮窗收起态文字随状态更新（「状态：<识别结论>」，重建以当前值初始化、帧线程更新编组回主线程）；③授权页常驻「运行方式：演练（只识别、不点击）」卡片。本切片为平台层展示与信号接线，未新增 JVM 单测；全量回归 22 套件 148 例零失败。真机演练（vivo V2463A，王者荣耀实机，13:26–13:36）：悬浮窗文字逐态核对——「未知 -> 启动页」13:29:48 →「启动页 -> 未知」13:31:23（中间含滞回过渡帧：界面已切换、连续 3 次未命中判据满足前保持原状态）→ 游戏进程被测试侧重启致系统回收会话（悬浮窗仍挂载显示「未知」）→ 会话重建后再现「未知 -> 启动页」13:36:25 → 应用主动停止「启动页 -> 未知（采集会话终止）」13:36:54；全程零点击（应用日志仅会话 / 前台 / 悬浮窗 / 状态 / 统计类，全部点击为测试侧 adb 注入并经 MM-Marker 标注）。环境事实记录备查：首轮会话因授权时游戏已在前台产生 3168×1440 画布并如实告警（按流程竖持重建修正）；vivo 分身实例选择器按既定口径选主应用；共享目标建会话后系统自动带回前台。证据见 `verification/t1-7/`（8 件：完整日志 + 运行方式卡片 + 历次悬浮窗截帧），A8 勾选。
- 完成 T1-8 任务卡登记（非 M1 验收项）：标定补充（其余状态标志产物）按功能依赖分 a/b/c 三批移交 M2/M4 前置，见 `plans/m1-recognition.md`；M1 完成定义（A1–A9）不受影响。
- 完成 T1-9（A9 前置）：性能基线测量手段与首轮真机数据——耗时统计器（连续 100 帧满窗输出 P95/平均/最大，nearest-rank）+ 帧管线分段接入（灰度段 / 识别段）+ 节流档切换日志；真机核对（vivo V2463A，14:03–14:47 四轮迭代）首轮基线 **P95 2287.6ms（灰度 52.7ms / 识别 2241.2ms），与 NFR-01 阈值差约 114 倍，如实记录为未达标**；瓶颈为朴素 NCC 全窗滑动（识别段占 98%）；桌面同源探针：匹配 ≈45ms、灰度 ≈5.7ms（设备/桌面 ≈48–50×）。附带发现：帧流静默停止（换区 / 游戏重启场景无回调，已登记）；标定帧池 62 文件即停为预期；分段统计 null 缺陷当日修复。证据见 `verification/t1-9/`（日志摘录 + 发现记录）。
- 完成 T1-10a/b/c（A9 整改，离线部分）：匹配优化（搜索窗口 SAT 位置统计量 + 交错半采样粗搜 + 代表点全精度精搜，判定输入分数保持全精度）+ 窗口化灰度（仅转换信号窗口区域）+ 帧线程优先级；固定批次回归（104 帧）判定零漂移（结论 0 不一致、强分差 0、位置漂移 0，尾部最大差 0.0007/0.0276 如实记录）；桌面识别耗时 P50 74→10ms / P95 79→11ms（≈7.4×）；A7 指标复跑逐字段一致；单测全量 170 例 0 失败（24 套件，临时探针文件删除后复跑）。证据见 `verification/t1-10/`。剩余 T1-10d 真机核对随后执行（见下条）。
- 完成 T1-10d 真机复审（A9 整改验证，2026-09-12）：vivo V2463A 演练长跑，两会话、三条满窗一致——总 P95 315.8 / 312.8 / 327.8ms（灰度 20.6–27.6ms / 识别 294.3–308.0ms），**未达标（差约 15.6–16.4 倍），如实记录**；较 T1-9 基线提速 ≈7.0–7.3×；节流两档切换（1s↔3s）与「未知→启动页」命中两会话成对复现；帧管线统计连续无中断（会话 1 77 条 / 会话 2 30 条）；全程零点击。证据见 `verification/t1-10/`（t1-10-04 真机取证 + 两会话截图）。**后续手段评估（3×3 采样 / 部分和剪枝）待启动**；A9 维持未勾选。
- 登记 T1-11 画布方向动态适配（研究先行，2026-09-12 用户口径）：明确当前方案重大局限——运行画布方向由建会话瞬间的系统显示方向决定，横画布（3168×1440）下帧尺寸与竖画布标定产物不一致、识别全失效；现有保障仅为「授权时 MM 竖屏前台」流程约定（隐含「旋转锁定竖屏」前提），用户关闭锁定等真实环境不可控。目标：不依赖用户设置与姿态，横竖画布均正确识别。排期：先于 T1-10 后续手段评估（3×3 采样 / 部分和剪枝）；任务卡见 `plans/m1-recognition.md`。
- 完成 T1-11d（真机核对，非 A1–A9 验收项）：vivo V2463A 三条会话——A 竖画布 1440×3168 回归（无方向提示）`未知 -> 启动页` 17:57:08；B「共享一个应用 → 王者荣耀」时系统先回 MM 再切游戏 → 建会话仍为竖画布（**环境事实：画布方向取决于服务读尺寸那一刻的显示方向**）；**C 横画布 3168×1440**——因 vivo 会回写旋转设置（强制 `user_rotation` 无效），改用 `wm size 3168x1440` 临时把逻辑显示置横屏建会话（等效真实触发条件），建会话后立即 `wm size reset` 并复查设备设置已还原；C 会话日志出现新 info「运行画布 3168x1440 与标定帧 1440x3168 方向不同…已按画面区几何归一后继续识别（T1-11c）」+ `未知 -> 启动页` 18:05:58，全程**无「识别结果可能不可用」降级告警**，同批录制横画布样本 37 帧（3168×1440）。另记录环境事实：横画布会话下静态启动页的帧到达率明显低于竖画布会话（3–4 帧/10s 对 537–557 帧/10s），识别轮次与判定不受影响；重装 APK 会重置无障碍授权（vivo，同 T0-2）。证据：`verification/t1-11d/`（t1-11d-11 真机取证 + t1-11d-12 运行日志 + 双会话截图）；样本归档 `recognition/samples-index.md` set-20260912-05；探针按真机横画布样本复跑（旋转 0° 唯一成立、缩放 2.2、带内相关 0.7419、锚点残差 0,1px）。T1-11 全切片（a–d）完结。
- 完成 T1-11b/c（非 A1–A9 验收项）：方案定稿 + 实现与单测——**定稿：按画面区的运行时归一**（不改采集画布，识别前把运行帧摆回标定几何），取舍与备选方案见 [ADR-007](decisions/ADR-007-画布方向适配.md)（`architecture.md` 开放决策表与新增不变量 5 已同步；候选 D「采集层固定画布」的否决理由含红线 4 与识别成本 ×23）。实现：`CanvasGeometry`（画面区 contain 模型 + 标定 ↔ 运行帧换算，含采样外扩 / 确定性取整）、`FrameNormalizer`（按信号窗口 ROI 重采样归一；画面区一致时零拷贝快路径，常见竖画布会话开销不变）、`RecognitionLoop` 检测前归一、采集层窗口区域改为「标定窗口映射到运行帧」、`CalibrationData.toLoop()` 带入几何；运行期日志由「帧尺寸不一致，识别结果可能不可用」改为「运行画布与标定帧方向不同，已按画面区归一并继续识别」（不再降级）。验证：单测新增 12 例（`CanvasGeometryTest` 7：以 T1-11a 真机实测值为锚；`FrameNormalizerTest` 5：恒等快路径 / 归一后判定一致 / 过渡帧不误报 / 确定性）+ 探针真实帧生产路径 1 例——**横屏取证帧 3168×1440 经生产路径 `MATCHED 0.9538 @651,1744`，对竖画布原生 `0.9989 @651,1744` 残差 0px，两会话均进入「启动页」**；全量 27 套件 184 例零失败。余 T1-11d 真机双方向复演（当前无设备连接）。
- 完成 T1-11a（研究先行，非 A1–A9 验收项）：画布方向几何对照测量——用既有归档两向帧（竖画布标定帧 1440×3168 + 横屏取证 3168×1440）在 JVM 离线探针（`CanvasOrientationProbeTest`，可复跑）测得精确映射：**无旋转，等比缩放 + 居中平移**——比例 = 两画布宽之比（1440/3168 = 0.4545），竖画布内容带高 655（等比嵌入期望 654.55）、上留白 1256 / 下留白 1257（左右无留白，与「横屏内容等比缩放居中嵌入」模型一致）；锚点核验（标定模板 launch_start）归一化后命中 0.9749 @651,1745，对竖画布原生 0.9989 @651,1744，**位置残差 0,1 px**；其余三个旋转角锚点分数 ≤0.28（旋转假设被双重排除：内容带几何 + 锚点分数）。同时量化失效机理：产物搜索窗口按整幅比例换算后纵向偏约 247px（横画布下窗口 y 比例 0.556 对应内容带内 0.773）。结论与探针原始输出：`recognition/research/research-canvas-orientation.md`（含目视对照图 `t1-11a-band-compare.png`）；测量输入帧对登记为 `recognition/samples-index.md` set-20260912-04；T1-11b 取舍输入（候选 A 运行时归一 / B 产物多方向变体 / C 只换算窗口 + 判据）已写入计划。局限如实记录：横侧输入为 adb 截屏而非横画布采集会话帧，真机横画布会话样本待补；样本仅覆盖启动页一个界面。全量单测回归 25 套件 171 例零失败（新增探针 1 例）。
- 完成 T1-5k 选框锚点外置与视觉修正（真机标定反馈，非 A1–A9 验收项）：框选改为「**外层操作框 + 内层真实选区**」两框——拉伸手柄与关闭钮挂在外层框上、内层框才是真实选区（解决真机反馈的「锚点过大压住边缘、看不清边缘」）；按反馈二次收紧视觉：外层框改**白色细线**、外扩 26dp → **16dp**、锚点缩小（角 7→5dp / 边 5→3.5dp / 关闭钮半径 11→8dp）、**最大放大倍率 8× → 16×**；选框与锚点之间的带状区域按整体移动处理（想抓锚点差几像素不会清掉选框）；`hitCloseButton` 增「选区内一律不命中」保护（窄边距下热区侵入选区时，拖动选框内部不会误清选框）。`FrameEditTest` 由 21 例增至 30 例（外框外扩与贴边退化 / 锚点不落在选区内 / 内层角不再是手柄 / 带状区为移动区 / 关闭钮外置且不压选区角 / 选区内不触发关闭钮），全量 27 套件 193 例零失败；改动已装机（vivo V2463A）。登记 T1-5l（后续迭代，**未实现**）并写入「阻塞与待确认」：全屏页统一承载「挑帧 / 框选 / 归属状态 / 直接保存产物」，取消页内预览大图与「提取为模板」步骤（实现前需确认直接保存语义与草稿模型去留）。
- 设备侧观察（2026-09-12，标定产物扩充，待归档与复演）：用户已在真机标定产物中补至 **7 个信号**——`server_select` / `popup_close` / `hall` / `farm` / `friend_farm` / `friend_list` / `launch_start`（`frame=1440x3168`、`params=0.85/0.10/5` 未变），即 T1-8a/b/c 各批的标定工作已在设备侧完成；**尚未做产物归档、逐信号真机状态复演与性能复测**。性能影响需注意：每帧匹配量与信号数近似线性（各信号窗口各扫一遍），NFR-01 现状（1 信号 P95 ≈313–328ms，差 16 倍）在 7 信号下会显著恶化——T1-10 后续手段评估（3×3 采样 / 部分和剪枝）与「按状态启用信号子集」的必要性上升，建议纳入下一次真机测量。
- 完成 T1-5l 真机核对 + T1-5m 工作台交互定稿（真机反馈，2026-09-12 晚）：**T1-5l 真机核对**（vivo V2463A）——标定页 / 工作台 / 框选 / 覆盖写入 / 模板详情 / 删除单条 / 删除整个产物逐步截图（t1-5l-02 ~ t1-5l-11），产物文件逐字段对照（仅覆盖信号变化、params 与几何不变、删空回无产物），核对前后产物 md5 一致（`a30a6422…`，本地字节级副本恢复机制启用一次）；**期间发现并修复缺陷：「参数编辑写入落后一次编辑」（界面 0.86 / 产物 0.80）**——根因 `applyParams()` 使用组合期快照值，修复为实时读取编辑框 + 现读盘上产物（删除信号与工作台写入同步改「现读盘」），复测一致（t1-5l-09 / t1-5l-10），修复后回归 198 例零失败。**T1-5m 工作台交互定稿**（用户参考系统相册给出 5 条改进）：①工作台入口移入产物卡（去括号）；②工作台大图占满全屏、缩略图条与工具栏绝对定位悬浮底部；③浏览模式滑页切帧、缩略图过半同步；④「框选」进入框选模式（禁滑页 / 隐缩略图；工具栏 ✕ 放弃 ｜ 选区信息 ｜ ✓ 写入；上方归属状态单选滑动条；✓ 需已框选、未选状态提示不写盘；成功后回浏览）；⑤恢复选框外层白色细线（外扩 16dp 仅视觉，锚点仍无）；⑥双指缩放保留。**写入时机口径由「点状态即落盘」修订为「✓ 确认写入」**（用户定稿，任务卡 T1-5m）。验证：`FrameEditTest` 23 例（恢复外框几何 2 例），全量 **27 套件 200 例零失败**；真机逐项截图（t1-5m-02 ~ t1-5m-08）+ 产物文件对照（仅覆盖信号变化）+ 核对后产物已还原（md5 与基线一致）。核对期间记录测试侧异常两次（如实）：一次差距线输入被误注入 0.17（界面与产物一致，非代码缺陷，当场纠正并复跑写入链路排除）；一次设备侧备份文件消失（改用本地字节级副本恢复成功——备份必须同时落本地的教训入档）。证据：`verification/t1-5l/t1-5l-12-real-device-verification.txt`、`t1-5m-01-verification.txt` 及同目录截图。
- 完成 T1-10e 后续提速手段评估（3×3 采样 / 部分和剪枝，2026-09-12，纯离线）：新增探针 `SpeedupProbeTest`
  （测试源集，可复跑）——先以「复刻基线 ≡ 生产实现（不一致 0 帧）」自校验，再在固定批次 51 有效帧上量化：
  3×3 采样 ×1.39 但 **best 位置 7/51 帧漂移**（结论不变、best 分数最大差 0.0011）；部分和剪枝 ×1.06 且
  **语义零漂移**（结论 0、位置 0、best 分数差 0）；组合 K32 ×1.70（漂移同 3×3）。剪枝首版曾漏掉剩余行
  均值修正项导致误剪（9/51 帧结论改变），修正后消失（教训入结论文档）。**结论：均不采纳**——收益仅
  1.1~1.7×，最优组合推算真机 P95 ≈184ms，距 20ms 仍差约 9 倍；剪枝收益低的机理是精搜候选本就是高分邻域、
  缺少可剪的「明显不匹配」样本。结构性建议（按状态启用信号子集 / 收紧搜索窗口 / 多尺度粗定位 / 算法替换 /
  需求口径确认）写入结论文档待排期。全量单测 27 套件 201 例零失败。证据
  `recognition/research/research-t1-10e-speedup-evaluation.md` + 探针输出
  `app/build/replay-work/out/t1-10e-speedup.txt`。**A9 判定待确认**：建议 M1 内不勾选并如实记录
  （NFR-01 最终验收在 M5，同 T1-10 任务卡备注）。
- T1-5m 收尾（2026-09-12 晚，用户反馈迭代）：底部工具栏「框选」按钮改**图标在上 + 文字在下**（新增 `ic_frame_select.xml`，`ToolIconButton` 可复用于后续工具栏项），构建 + 单测（27 套件 200 例零失败）后装机交付并**由用户实机自测（结论：符合预期）**——最终验证：设备产物 md5 仍为基线 `a30a6422…`，自测过程未污染产物，无需还原；本轮用户自测未留截图证据（如实记录，见 `t1-5m-01` 第四节）。**工作方式变更（用户口径）：真机交互测试改由用户执行，我做到「构建 + 装机」为止，只负责最终验证与证据归档。**
