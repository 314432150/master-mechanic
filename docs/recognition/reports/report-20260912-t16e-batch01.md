# 回放指标报告：t16e-batch01

- 样本集：set-20260912-03
- 采集设备：vivo V2463A / Android 16
- 采集场景：换区巡查全链路固定批次（T1-6e batch01）：启动页/大厅弹窗/农场/好友面板/设置/退出确认/切区登录/新手引导，共 104 帧
- 报告日期：2026-09-12
- 参数集版本：calib-v1（命中线 0.850000 / 差距线 0.100000 / 峰值间距 5）
- 运行次数：3

## 口径与公式

- 误报定义：负样本上出现任一信号命中（MATCHED）即为该样本误报；不可信（UNRELIABLE）按未识别处理，不计误报。
- 零误报上界：n 个样本 0 次误报 → 真实误报率上界 = 1 − 0.05^(1/n)（95% 置信）。
- 负样本总量下限 60；不足时不得声称「零误报」。
- 三类干扰分别统计，不得合并成一个总通过率。

## 样本量

- 负样本合计：85（达到口径下限 ≥ 60）
- 正样本合计：19

## 三类干扰分列（不得合并）

| 类别 | 样本量 n | 误报数 | 点估计 / 上界 |
| --- | --- | --- | --- |
| 半透明遮罩（干扰①） | 18 | 0 | 上界 0.153318 |
| 动背景（干扰②） | 4 | 0 | 上界 0.527129 |
| 相似多目标（干扰③） | 10 | 0 | 上界 0.258866 |
| 普通负样本 | 53 | 0 | 上界 0.054955 |
| 合计（仅供样本量核对，不作为总通过率结论） | 85 | 0 | — |

## 正样本（参照）

- 命中 18 / 19

## 可复现比对

- 运行次数：3
- 逐字段比对：逐字段一致（无差异）

## 复现命令

```text
.\gradlew.bat ':app:testDebugUnitTest' --tests '*ReplayBatchRunTest*' --rerun
```
