# 样本集索引

> 规则见 `README.md`。样本集按采集日期版本化；代码永不内嵌样本数据（§5-5）。

## set-20260912-01 — 启动页标定样本（T1-5c）

| 项 | 内容 |
| --- | --- |
| 采集设备 | vivo V2463A / Android 16（API 36） |
| 采集日期 | 2026-09-12 |
| 场景 | 《王者荣耀》启动页（「开始游戏」按钮、区服信息、「退出」等） |
| 数量 | 48 帧（每秒 1 帧，仅目标前台时录制） |
| 帧画布 | 1440×3168（竖持授权会话；横屏游戏内容居中带嵌入，见代表帧） |
| 命名 | frame-<补零毫秒数>.png（字典序 = 时间序） |
| 用途 | T1-5c 标定输入：框选「开始游戏」→ 模板 launch_start（131×37，选框 1:1 灰度裁剪） |
| 样本位置 | 采集设备应用私有目录（files/calibration/frames/）；本仓存代表帧 |
| 代表帧 | [t1-5-launch-start-frame.png](samples/t1-5-launch-start-frame.png)（frame-0002892990467） |

备注：帧画布方向随授权时持机姿态变化（本集为竖持会话）；标定帧与运行帧需同姿态采集，
否则帧尺寸不一致（运行期有告警、识别不可用）。真实标志框选在小目标场景下需先放大画面
（T1-5d 交互），选区以比例坐标记录、与画布尺寸解耦。

## set-20260912-02 — 换区巡查全链路样本（T1-6e 候选池）

| 项 | 内容 |
| --- | --- |
| 采集设备 | vivo V2463A / Android 16（API 36） |
| 采集日期 | 2026-09-12 |
| 场景 | 完整巡查链路：启动页（微信392区）→ 大厅 + 活动弹窗轮播 → 农场 / 好友面板 → 设置 → 退出登录确认框 → 切区到微信418区 → 启动页 → 新角色登录播片 → 新手引导 → 新手大厅 → 大厅（卧龙山青椒 Lv.6） |
| 数量 | 62 帧（每秒 1 帧，仅目标前台时录制） |
| 帧画布 | 1440×3168（竖持授权会话；横屏游戏内容等比缩放居中嵌入，同 set-01） |
| 命名 | frame-<补零毫秒数>.png（字典序 = 时间序） |
| 用途 | T1-6e 固定批次候选池（清单标注待做）；FR-02 两状态标志标定输入；FR-04 换区链路（退出登录 → 换区 → 登录）素材 |
| 样本位置 | 采集设备应用私有目录（files/calibration/frames/）；本地批次备份 `app/build/replay-work/all/`（前批 43 帧移存 `all-batch1-43f/`）；本仓存代表帧 |
| 代表帧 | [t1-6e-tutorial-guide-frame.png](samples/t1-6e-tutorial-guide-frame.png)（frame-0002907401000）、[t1-6e-tutorial-hall-frame.png](samples/t1-6e-tutorial-hall-frame.png)（frame-0002907404451） |

标志线索（标定输入，2026-09-12 会话核实）：

| 状态（信号名） | 界面标志线索 | 证据帧 |
| --- | --- | --- |
| 新手引导（tutorial_guide） | 「开局送英雄！」金色大字横幅 + 英雄头像排 | frame-0002907401000、frame-0002907402008、frame-0002907403022（连续 3 帧）；进入前瞬态 frame-0002907399560（横幅未出现） |
| 新手大厅（tutorial_hall） | 「新手豪华登录礼」标题（左上角「新手大厅」字样） | frame-0002907404451、frame-0002907405466、frame-0002907406480（连续 3 帧） |
| （FR-04 中间态参考） | 「您确定要退出登录吗？」确认框（取消 / 确定） | frame-0002907389148、frame-0002907390162 |

备注：状态名沿用 §2.1 与术语表，不改名；「开局送英雄」「新手豪华登录礼」为当期活动文案，仅作标志模板素材，不进入状态命名。新手引导界面左上角疑似存在返回控件及其点击后行为，待 FR-02 标定时放大确认。

## set-20260912-03 — 固定批次（T1-6e 首份指标报告输入）

| 项 | 内容 |
| --- | --- |
| 采集设备 | vivo V2463A / Android 16（API 36） |
| 采集日期 | 2026-09-12 |
| 构成 | 104 帧 = 新批 62 帧（set-20260912-02）+ 前批 42 帧（排除 1 帧画布过渡帧；前批原始 43 帧移存 `all-batch1-43f/`） |
| 场景 | 换区巡查全链路 + 大厅日常操作：启动页（多版本）/ 选择服务器 / 大厅 + 活动弹窗轮播 / 农场 / 好友面板 / 设置 / 退出登录确认 / 切区登录 / 新手引导 / 新手大厅 |
| 类别分布 | positive 19 / neg-mask 18 / neg-motion 4 / neg-similar 10 / neg-plain 53（负样本合计 85，达到口径下限 ≥60） |
| 帧画布 | 1440×3168（竖持授权会话；横屏游戏内容等比缩放居中嵌入，同 set-01 / set-02） |
| 用途 | T1-6e 固定批次：JVM 离线回放（灰度入库）→ 首份误报率指标报告（A7 证据） |
| 批次位置 | 应用工程 `app/build/replay-work/set-20260912-03/`（`manifest.txt` + 104 帧灰度图；原始帧 `raw/`；本地不入库） |
| 报告 | [report-20260912-t16e-batch01.md](reports/report-20260912-t16e-batch01.md)（运行 3 次判定逐字段一致；全部类别零误报，上界见报告） |
| 清单标注 | 正样本信号统一 launch_start，期望位置为模板（131×37）落位 651,1744 的比例坐标；负样本三类干扰分列（mask / motion / similar） |

备注：批次为固定一批（§5-4），收录后不再增删；重跑比对以同清单 + 同参数集（calib-v1）为准。已知边界：1 帧正样本（「开始游戏」按下高亮瞬态）未命中——属点击后瞬态、不在识别时机范围；1 帧曾误标（切区流程中的选择服务器页）已在清单中修正为 neg-similar。
