# M2 计划：自动异常处理（FR-01 / FR-02）

| 项 | 内容 |
| --- | --- |
| 状态 | 口径已确认（2026-09-13），**待开工**；首个动作 = T2-2 标定补充（需真机，用户执行） |
| 目标 | 常驻守护下程序自主处理活动弹窗（FR-01）与新手引导 / 新手大厅（FR-02）：识别 → 点击 → 验证，全程零猜测点击 |
| 前提 | M1 已交付（识别闭环 + 7 信号标定产物 + 画布方向归一）；**FR-02 标志与活动弹窗多样式标定（T1-8b）必须先补齐** |
| 对应需求 | FR-01、FR-02、§2.1（活动弹窗 / 新手大厅 / 新手引导）、§3.1、§4 红线 1 / 2 / 3 / 5 / 6 / 7、NFR-02、附录「推进顺序」第 2 步 |

## 范围边界

- **做**：预期集合驱动落地（识别层口径）、FR-01 / FR-02 的判定与动作下发、「识别 → 点击 → 验证」闭环、
  「动作锚点」角色字段与产物 v2、点击转发层（相邻点击 ≥ 300ms 的唯一实现点）、演练 / 实点切换。
- **不做**：巡查主流程与名称定位（M3 / M4）、悬浮窗完整交互（M3）、性能达标（NFR-01 移交 M5）。

## 验收清单（草案，待用户确认）

- [ ] B1 弹窗闭环：活动弹窗命中 → 关闭控件点击 → 下一轮不再命中；连续 3 次仍命中 → 放弃本次点击并记录（FR-01 失败处理）
- [ ] B2 新手闭环：新手引导 / 新手大厅标志命中 → 优先点返回按钮，识别不到则点固定返回位置 → 下一轮不再命中（FR-02）
- [ ] B3 优先级：同帧同时命中时先处理弹窗（FR-01 优先级高于 FR-02）
- [ ] B4 顺序：新号阶段「先引导、后活动弹窗」（0~N 个），全部处理完才进大厅（用户口径 2026-09-13）
- [ ] B5 零猜测点击：锚点未标定或识别不可信 → 不点击、停下并提示（红线 3 / 7）
- [ ] B6 点击节流：相邻点击 ≥ 300ms 由统一转发层保证（红线 6）
- [ ] B7 动作后验证：每次点击必须由下一轮识别确认，未确认不进入下一步（红线 5）
- [ ] B8 演练 / 实点切换：演练下只识别不点击（延续 M1 能力），实点需显式开启

## 任务卡（草案）

### T2-1 预期集合驱动落地（识别层口径切换）

- 目标：`ActiveSignalSelector.select(state, round)` → `select(expected)`；空集合 = 不搜；「未知」= 预期集合内连续 3 次未命中
- 验收：M1 演练路径改为显式声明全集后行为不变（固定批次 `set-20260912-03` 判定零漂移）；单测覆盖「空集合 / 子集 / 全集」三态
- 证据：单测 + 固定批次回放报告
- 备注：预期集合的需求依据为 FR-04「进入下一步的验证」列（步 1 = {农场, 好友的农场, 大厅}；步 6 = {大厅} ∪ {活动弹窗}）

### T2-2 FR-01 / FR-02 标定补充（T1-8b 并入，前置）

- 目标：标定 `popup_close` 多样式 +「关闭 / 跳过」文字标志、新手引导标志、新手大厅标志
- 验收：逐信号真机复演命中；产物同源灰度裁剪（红线 4）
- 证据：产物 + 复演日志（`docs/verification/`）
- 捷径：停在新手引导弹窗（未标定界面）会走未知态全扫 → 约 90 秒补齐全部信号窗口

### T2-3 动作锚点角色字段 + 点击转发层

- 目标：产物 v2 角色字段（标志 / 锚点；同一元素两种角色须为两条独立记录）、按当前状态启用、标定页支持该类型；点击转发层强制 ≥ 300ms
- 验收：单测（角色解析 / 按状态启用 / 间隔节流）+ 真机一次真实点击核对（用户执行）
- 证据：单测 + 真机日志与截图

## 移交自 M1 的未结事项

| 事项 | 出处 | 处置 |
| --- | --- | --- |
| NFR-01 未达标（真机子集 P95 298.3ms / 全扫轮 ≈790ms） | M1 A9、`verification/t1-13/` | 移交 M5 最终验收；候选手段「收紧 4 个大窗口信号（≈2×）/ 多尺度粗定位」登记为 M5 性能任务候选 |
| T1-12a 判据「①判定明细 ⑤子集生效」未取证 | `verification/t1-12/` | T1-13 已补日志（转移轮判定明细 / 统计行实际搜索信号），可随时补测闭环 |
| 帧流静默停止检测（换区 / 游戏重启无回调） | T1-9 发现 2 | 建议随 FR-08 加固纳入后续里程碑 |
| R-2 如何确认「已进入目标好友的农场」（`requirements.md` §7 第 1 项） | `requirements.md` §7 | M4 前置，按研究任务继续 |
| 退出登录入口完整路径 / 服务器列表滚动处理 / 同一区服多好友（§7 第 2~4 项） | `requirements.md` §7 | M3 / M4 实现时按当前假设执行 |
| T1-8c 巡查状态标定（大厅 / 农场 / 好友列表 / 好友的农场） | `plans/m1-recognition.md` T1-8c | M4 前置，随 M1 流转并入后续计划 |

## 已确认口径（2026-09-13 用户定稿）

1. **弹窗族只出现在「启动页 → 进入大厅」之间**：活动弹窗、新手引导、新手大厅都不会在农场 / 好友列表 / 好友的农场出现
   → 该阶段之外（含常驻守护待命期间）**不做弹窗族检查**。`requirements.md` FR-01 触发行与 §2.1 活动弹窗行已同步
   （原「常驻守护下常驻每轮检查」已删除）。
   实现含义：守护待命期的预期集合为空 = 不搜；进入该阶段的判断由 T2-1 设计时给出方案（不得引入全扫兜底）。
2. **先演练、后实点**：真实点击**由用户执行**——用户操作设备并逐步告知当前界面状态，我负责核对识别结果、
   动作序列与判定证据；AI 不代用户点真实界面。
3. **复现账号已就绪**：新手引导 / 新手大厅用新号复现；**服务器列表会显示账号等级**，可据此确认所用小号是否为未过引导的新号。
4. **性能手段留 M5**：M2 不做窗口收紧 / 多尺度粗定位等优化；若某轮耗时影响动作节奏，如实记录并交由 M5。

## 演练 → 实点的推进方式（工作约定）

| 阶段 | 谁操作设备 | 我做什么 | 证据 |
| --- | --- | --- | --- |
| 演练（离线） | 我 | 改代码 + 单测 + 固定批次回放 | 回放报表镜像进 `docs/verification/m2/` |
| 真机演练（只识别不点击） | 用户走界面并告知状态 | 装包、读日志核对命中与判定 | 日志 + 截图 |
| 真机实点 | 用户走界面并逐步告知状态 | 核对「识别 → 点击 → 验证」闭环、节流与失败处理 | 日志 + 截图 |

- 实点必须在演练段全部通过（B5~B7）后开启，且**每次会话开始前由用户明确指示「这次实点」**。
- 实点期间任一判据不成立（如动作后未验证、点击间隔不足）→ 立即停止并回到演练段定位。
