# M2 计划：自动异常处理（FR-01 活动弹窗）

| 项 | 内容 |
| --- | --- |
| 状态 | 口径已确认（2026-09-13 含 FR-02 移除），**待开工**；首个动作 = T2-2 标定补充（需真机，用户执行） |
| 目标 | 常驻守护下程序自主处理活动弹窗（FR-01）：识别 → 点击 → 验证，全程零猜测点击 |
| 前提 | M1 已交付（识别闭环 + 7 信号标定产物 + 画布方向归一）；**活动弹窗多样式 +「关闭 / 跳过」文字标志标定（T1-8b 收窄版）必须先补齐** |
| 对应需求 | FR-01、§2.1（活动弹窗）、§3.1、§4 红线 1 / 2 / 3 / 5 / 6 / 7、NFR-02、附录「推进顺序」第 2 步 |

## 范围边界

- **做**：预期集合驱动落地（识别层口径）、FR-01 的判定与动作下发、「识别 → 点击 → 验证」闭环、
  「动作锚点」角色字段与产物 v2、点击转发层（相邻点击 ≥ 300ms 的唯一实现点）、演练 / 实点切换。
- **不做**：~~FR-02 新手引导 / 新手大厅~~（2026-09-13 用户口径移除，见「已确认口径」第 5 条）、
  巡查主流程与名称定位（M3 / M4）、悬浮窗完整交互（M3）、性能达标（NFR-01 移交 M5）。

## 验收清单

- [ ] B1 弹窗闭环：活动弹窗命中 → 关闭控件点击 → 下一轮不再命中；连续 3 次仍命中 → 放弃本次点击并记录（FR-01 失败处理）
- [ ] B2 零猜测点击：锚点未标定或识别不可信 → 不点击、停下并提示（红线 3 / 7）
- [ ] B3 点击节流：相邻点击 ≥ 300ms 由统一转发层保证（红线 6）
- [ ] B4 动作后验证：每次点击必须由下一轮识别确认，未确认不进入下一步（红线 5）
- [ ] B5 演练 / 实点切换：演练下只识别不点击（延续 M1 能力），实点需显式开启

> 编号变更（2026-09-13）：原 B2（新手闭环）、B3（FR-01 优先级高于 FR-02）、B4（先引导后活动弹窗）
> 随 FR-02 移除而删除，其余条目上移；下文「演练段全部通过（B2 ~ B4）」按现编号理解。

## 任务卡

### T2-1 预期集合驱动落地（识别层口径切换）——**已完成（2026-09-13）**

- 目标：`ActiveSignalSelector.select(state, round)` → `select(expected)`；空集合 = 不搜；「未知」= 预期集合内连续 3 次未命中
- 验收：M1 演练路径改为显式声明全集后行为不变（固定批次 `set-20260912-03` 判定零漂移）；单测覆盖「空集合 / 子集 / 全集」三态
- 证据：单测 + 固定批次回放报告
- 备注：预期集合的需求依据为 FR-04「进入下一步的验证」列（步 1 = {农场, 好友的农场, 大厅}；步 6 = {大厅} ∪ {活动弹窗}）
- **实现（2026-09-13，本轮）**：
  - 新增 `decision/ExpectedSignals.kt`：期望集合来源接口（`expected(known)` 取本轮信号名、`onRound(hits)` 供阶段推进，
    默认空实现）；三种实现——`ALL`（**显式声明全集** = 演练 / 回归口径）、`NONE`（不搜）、`FixedExpectedSignals`（固定集合），
    以及 **`PopupPhaseExpectedSignals`**（FR-01 弹窗阶段源，由状态—信号规则构造）。
  - `ActiveSignalSelector` 收敛为「期望集合 ∩ 已标定信号名」：**剔除未标定的期望名**（不能把"没有的能力"记成搜索量）、
    **空集原样返回**；删除 `fromRules` 与「按状态自动选子集 / 状态未知即全扫 / 补扫 / 周期兜底」全部历史路径。
  - `RecognitionLoop`：构造参数 `selector` → `expectedSignals`（默认 `ALL`）；**空集合 = 不搜**（不产生判定记录与
    耗时样本，对状态机如同不存在）；`RoundResult.searched` 由可空集合改为**非空集合**（空集 = 不搜，原 `null` = 全扫语义取消）；
    前台轮末调用 `onRound(hits)`——**用本轮原始命中推进阶段，不等滞回结论**（等 2 轮会让刚弹出的弹窗在本轮与下一轮都搜不到）。
  - **阶段口径（见「已确认口径」第 6 条；入口标志 2026-09-13 晚扩为两个，需求条文待用户确认）**：守护待命搜
    **入口标志** =「启动页」∪「活动弹窗」（全部样式记录，共 4 条）；命中任一 → 弹窗期
    {启动页 ∪ 服务器列表 ∪ 活动弹窗 ∪ 大厅}；命中大厅 → 收回待命（收回后仍守着入口标志）。
    `CalibrationData.toLoop()` 默认注入该源；演练 / 离线回归走 `toLoop(ExpectedSignals.ALL)`。
  - 运行日志：新增 `本轮搜索集合变化: 「…」 /（本轮不搜）`（集合一变就记一行，真机可直接读出阶段推进）；
    统计行由「子集 / 全扫」改为「**单信号 N 轮 / 多信号 N 轮 / 不搜 N 轮**」。
- **验证（2026-09-13）**：全量单测 231 例 0 失败（30 套件）+ `assembleDebug` 通过——新增 `PopupPhaseExpectedSignalsTest` 8 例、
  重写 `ActiveSignalSelectorTest`（选择规则 4 例 + 循环集成 6 例）；固定批次 `set-20260912-03` 回放报表重跑与归档
  `recognition/reports/report-20260912-t16e-batch01.md` **逐行一致（Compare-Object 无差异）**。
- **证据边界（如实记录）**：回放报表走的是检测层（`SignalDetector` 直接出判定），**不经过 `RecognitionLoop`**，
  因此「零漂移」在报表层只能证明检测 / 映射零改动；"显式全集 = 每轮全搜 = 改动前的全扫轮" 由循环层单测
  `fullSetSearchesEveryCalibratedSignal` / `fullSetExpectationKeepsOldBehaviour` 承担。改动前默认口径是
  「按状态子集 + 未知全扫」，与全集的轮次差异（旧口径下当前状态已知时换界面要等 3 轮未知才认）**如实保留**，
  不宣称"与旧默认行为完全一致"。

### T2-2 FR-01 标定补充（T1-8b 收窄版，前置）

- 目标：标定 `popup_close` 多样式 +「关闭 / 跳过」文字标志 —— 活动弹窗的关闭控件位置与样式随活动变化，需逐样式覆盖
- 验收：逐信号真机复演命中；产物同源灰度裁剪（红线 4）
- 证据：产物 + 复演日志（`docs/verification/`）
- 待标定样式（离线核对既有样本，`set-20260912-02` idx10/11/12/57/58/59）：① 限时点券·宝箱版 ② 限时点券·多头像版 ③ 万象棋·紫白卡片版——三种样式的关闭入口均为内容带右上角「×」与右下角「今日不再弹出」；设备现有产物 `popup_close` 窗口落在**右下角**（`0.653472,0.567866,0.957639,0.591540`，即「今日不再弹出」一带），右上角 × 尚未覆盖
  - **产物现状（2026-09-13 用户标定后）**：`ACTIVITY_POPUP` 4 条 = `popup_close`（右下「今日不再弹出」146x25）+
    `popup_close2/3/4`（右上角「×」，模板 41x41 / 46x46 / 39x38，窗口彼此几乎重合）——右上角 × 已覆盖；
- 捷径：停在**任一未标定界面**会走未知态全扫 → 约 90 秒补齐全部信号窗口；**须在 T2-1 切换前使用**
  （切成 `select(expected)` 后不再有「未标定即全扫」这条路径）——**T2-1 已于 2026-09-13 落地，该捷径关闭**；
  用户在切换前已完成标定（产物 `ACTIVITY_POPUP` 现挂 4 条记录：`popup_close` = 右下「今日不再弹出」，
  `popup_close2/3/4` = 右上角「×」的三种样式，三窗口几乎重合）
- **写入方式：方案 A 同状态多条记录（2026-09-13 用户拍板）**——框选写入按归属状态默认名**追加**，
  重名自动追加序号（`popup_close` → `popup_close2` → …），各条记录保有自己的搜索窗口与模板；
  同一状态的多条记录合成一条状态规则（任一命中即该状态命中，§2.1）。
  理由：一条记录只对应一个元素 / 一个点击位置（T2-3 锚点），且 T1-13b 已证明窗口面积是成本主杠杆，
  合并成一条只能把窗口扩到覆盖两处。修正误标 = 产物清单中删除该条后重标。
  备选 B（同名多模板、窗口取并集）**不采纳**：覆盖语义丢失、窗口膨胀。
- **前置缺口已解决（2026-09-13 本轮实现）**：原写入路径固定「信号名 = 状态默认名 + 同名整体覆盖」→
  新增 `CalibrationSignals.nextName(current, base)`（纯逻辑，默认名空闲即用、否则取序号最小的未占用名）；
  `CalibrationScreen.writeSignalFromSelection` 改为追加写入；文案 `calibration_signal_added`
  「已新增信号：…（该状态现有 N 条记录）」取代原「已写入 / 已覆盖」两条。
  产物格式与识别侧**未改动**（`state=` 行多信号名、`SignalStateMapping` 多对一、`ActiveSignalSelector`
  子集选择均原生支持多条记录）；单测 5 例（命名 4 + 同状态多记录规则/窗口独立性 1），全量单测 + `assembleDebug` 通过。
- 备注：新手引导 / 新手大厅标志**不再标定**（FR-02 已移除）；`samples-index.md` 中那两批样本仅作历史留存
- 备注（性能影响）：同状态记录数为 N 时，「搜该状态」一轮成本 ≈ N 条信号的耗时之和（真机 `popup_close`
  单条约 69ms，3 条约 200ms）→ M5 需连同窗口收紧一并评估（真实运行时该状态多数轮只命中 1 条，但匹配成本按全部条目计）。
- **真机执行进度**（用户操作、我只读取证）：① 逐样式停稳后框选写入（记录自动落 `popup_close2`…）——**已完成**；
  ② 停在弹窗界面复演 + 截图日志归档 ——**样式 ① 已完成（2026-09-13 14:17 会话）**；③ 证据归档 `docs/verification/m2/t2-2/`。
- **复演结果（样式 ① 限时点券·宝箱版，2026-09-13）**：证据 `docs/verification/m2/t2-2/`（说明 `t2-2-07-verification.txt`）。
  - T2-1 期望集合驱动真机生效且**三态齐全**：待命 `launch_start` 1 条 → 命中启动页当轮扩为 7 条 →
    `启动页 -> 活动弹窗`（连续 2 次命中）；弹窗期稳定 7 条/轮（真机日志 L6/L10~L26）；悬浮窗显示「状态：活动弹窗」。
  - 逐信号归因（离线同源帧，探针 `CalibrationReviewProbeTest`）：弹窗帧上 **4 条 popup 记录全部命中**
    （0.88~1.00），离开弹窗后大厅帧上四条全部 NOT_MATCHED（≤0.39）→ **无残留误报**。
  - **三条 × 记录的性质（2026-09-13 用户澄清）**：是**三种样式各自的 ×**，故三条都保留。样式 ① 上三条同时命中
    0.99~1.00、位置相差 ≤3px 属预期（说明三种样式的 × 视觉一致）。**待样式 ②③ 出现时再逐条复演**，确认各自命中来源；
    在此之前不做删减（删掉即失去覆盖，多留一条只是多一次小窗口匹配）。
  - **未闭合项**：样式 ② 限时点券·多头像版、③ 万象棋·紫白卡片版 未复演（需其出现时再取证）；
    「命中大厅 → 收回待命」未取证（本次未走到大厅阶段）。**不阻塞 T2-3 / B1**。

### T2-3 动作锚点角色字段 + 点击转发层

- 目标：产物 v2 记录带「角色」（**标志 / 锚点**；同一元素两种角色必须是两条独立记录）、锚点按**当前状态**启用、
  标定页支持选择角色；点击转发层强制相邻点击（手势**完成** → 下次**开始**）≥ 300ms
- 需求锚点：§2.1「界面标志 / 动作锚点」、FR-01、§4 红线 1 / 2 / 5 / 6 / 7、ADR-002、NFR-05
- 验收：单测（角色解析 / v1 兼容 / 按当前状态启用 / 门禁与间隔节流）+ 真机一次真实点击核对（用户执行）
- 证据：单测 + 真机日志与截图（`docs/verification/m2/t2-3/`）

**切片（按依赖顺序）**

- **T2-3a 产物 v2 角色字段** —— **已完成（2026-09-13，提交 `6b3584d`）**：`SignalRole { MARKER, ANCHOR }`；
  `signal=<名>|<角色>|<窗口>`；新增 `anchor=<UiState>|<锚点名>[,<锚点名>…]` 声明锚点的**归属状态**；
  `state=` 行语义不变（只列标志）。**v1 产物仍可读**（全部按标志解析、锚点为空），写入一律 v2——
  设备上现有 10 信号产物无需重标（重标代价高，格式升级不该强迫用户重做已标定内容）。
  校验：角色与所在行不匹配 / 同一条记录被多处引用即拒收；**锚点必须有归属状态**（没有归属 = 永远不会被启用）；
  标志允许暂不归属（离线回放工具的"只有信号没有规则"产物沿用）。
  证据：`CalibrationCodecTest`（v1 兼容 / 角色往返 / 版本与角色不匹配拒收）、
  `CalibrationModelTest`（角色守卫 / 只有锚点的规则 / 仅标志回放产物）、`CalibrationSignalsTest`（角色分流与改角色）。
- **T2-3b 锚点定位（按当前状态启用）** —— **已完成**：`decision/AnchorLocator.kt`——只有「当前状态」规则的锚点参与匹配
  （其余不搜：省成本，也不会点到别的界面）；命中给出**点击点 = 匹配区域中心**，并把标定坐标按画面区反算回**运行帧坐标**；
  不可信（红线 7）按未命中处理；锚点不参与状态判定、不进期望集合；另提供 `windowRegions`（采集层需把锚点窗口一并纳入，
  否则锚点在"只转换了标志窗口"的帧上读到全零像素而静默失败）。`CalibrationData.toAnchorLocator()` 提供同源几何的入口。
  证据：`AnchorLocatorTest` 7 例（中心点 / 按状态启用 / 多处高分不点 / 2 倍画面区坐标回落 / 采集区域 / 空定位器 / 构造守卫）。
- **T2-3c 点击转发层** —— **已完成**（纯逻辑核 + 安卓薄适配）：`action/ClickGate.kt`（门禁 + 节流，纯逻辑）、
  `action/ClickForwarder.kt`（门禁 → 审计 → 注入 → 手势结束回填）、`action/ClickDispatch.kt`（进程内唯一入口，
  服务连接时安装、断开即卸载）、`service/AccessibilityGestureInjector.kt`（`dispatchGesture` 短行程 stroke）、
  `service/ClickAuditLog.kt`（TAG `MM-Click`，含**拒绝**原因的审计）。
  门禁顺序固定：**演练模式 → 非前台 → 状态未知 → 串行（有手势在途）→ 间隔不足**；
  间隔按 ADR-002 以单调时钟计量"上次手势**完成** → 本次手势**开始** ≥ 300ms"，硬下限写在 `ClickGate` 里（调小即抛）。
  模式默认**演练**，`ClickDispatch.enableLive()` 是唯一进入实点的路径（B5）。
  证据：`ClickGateTest` 9 例（四条拒绝理由 + 判定顺序 + 取消同样计入间隔 + 硬下限）、
  `ClickForwarderTest` 5 例（坐标与审计链 / 拒绝不动手 / 同步失败解除在途 / 间隔由层自保证 / 取消上报未完成）。
- **T2-3d 标定页角色选择** —— **已完成（2026-09-13）**：写入按角色落库，**默认名按角色分流**
  （标志 `popup_close` / 锚点 `popup_close_anchor`，重名仍走序号追加）；产物清单与模板详情显示角色；
  写入提示按**同角色**计数（标志数 / 锚点数各自累计，不混着数）。
  证据：`CalibrationSignalsTest` 31 例（新增 3 例）+ `:app:test -PfastTests` 253 例 0 失败 + `assembleDebug` 通过。
  首版交互（角色 chip 与状态 chip 同行）按 T2-3f 修订，见下。
- **T2-3f 标定页交互修订：角色分层 + 外框拉伸** —— **已完成（2026-09-13，用户真机反馈后当天修订）**：
  ① **角色不再与状态同级**：底栏拆两行、各带前缀标签——「归属状态」行（chip，横向滚动）+
  「写入为」行（**分段控件**，与控制同级的 chip 在形态上区分开）；✓ 按钮直接写角色名
  （`✓ 写入标志` / `✓ 写入锚点`）；画布选框颜色随角色变（标志红 / 锚点青），做到"所见即所写"。
  ② **恢复外框拉伸**：T1-5l 去手柄时把拉伸能力一起删了（真机反馈：白线拖不动）。按用户口径**恢复能力但不画手柄**——
  新增 `FrameHit.Resize`（八向）+ `FrameEditMath.resize`；命中带 = 选框外一整圈（宽 = 白线外扩量 ×2、白线居中，
  贴内线也算带内），**完全落在选框之外** → 框内仍只移动、更远处仍是新建；最小边长 = 模板最小边长
  （`MIN_TEMPLATE_PX` 按帧尺寸换算），clamp 在帧界内；从关闭钮拖出超过滑动阈值后按起点重判命中
  （左上角那一格因此归拉伸带，不再是死区）。
  证据：`FrameEditTest` 32 例（净增 9 例 = 拉伸带命中 6 + 拉伸运算 5，其中 3 例为按新口径改写）、
  `:app:test -PfastTests` **262 例 0 失败** + `assembleDebug` 通过。真机界面核对随 T2-3e 一起做（用户执行）。
- **T2-3g 「写入为」改为多选：一次框选写两条记录** —— **已完成（2026-09-13，真机反馈后当天修订）**：
  用户口径「有些元素可以同时作为角色和状态，目前是单选，只能选一个」→ 同一个元素既要判状态又要点击时，
  不必再框两次、写两次。角色行两个角色**各自独立勾选**：勾两个 = 一次写入**两条记录**
  （§2.1：一条记录仍然只有一个角色），两条**共用本次提取的模板与搜索窗口**（只解码 / 提取一次）；
  名字由 `CalibrationSignals.namesFor` 分配（角色顺序固定「标志 → 锚点」，同批内互相避让，已占名照旧追加序号）；
  ✓ 按钮与写入提示都显示 `标志 + 锚点`（`calibration_signal_added` 改为按角色分别报数）；
  画布上双角色 = **外圈红 + 内圈青**（双框线），一眼看出这一框会写成两样；
  两个都不勾时 ✓ 置灰、行尾提示「至少选一个角色」（不制造"点了没反应"的哑状态）。
  实现注意：material3 1.4.0 起 `MultiChoiceSegmentedButtonRow` 是 `@Deprecated(HIDDEN)`（只留二进制兼容），
  但配套的多选 `SegmentedButton` 重载仍是公开 API —— 用 `MultiChoiceRowScopeBridge` 把 `RowScope`
  桥接成空的标记接口 `MultiChoiceSegmentedButtonRowScope` 即可继续用（外观与单选分段控件一致）。
  证据：`CalibrationSignalsTest` 37 例（新增 6 例 = 角色顺序 / 双角色命名 / 已占名让位 / 非法入参 /
  一次写两条共用几何 / 按角色计数）、`:app:test -PfastTests` **268 例 0 失败** + `assembleDebug` 通过。
  **当天第二次真机反馈修订（T2-3g′，2026-09-13）**：
  ① **BUG：一个都不勾时底栏变高** —— 根因：分段按钮内部按 `weight` 抢占整行剩余宽度，我把它与该行右侧的
  红色提示塞进了同一个 `Row` → 提示被挤成 0 宽、逐字换行，肉眼看不见却把整行撑高（用户截图里那条提示确实不见了）。
  修法：**这一行只放分段控件**（顺带补 `end = 12dp` 内边距）——这一行此后不再承载任何文字，"缺什么"的提示见 T2-3h。
  ② **初始改为一个都不勾**（原先默认勾「标志」）：避免顺手把标志写进去，逼一次明确选择。
  ③ **文案一致性**：选项叫「标志 / 锚点」，旁边就不该说「先选角色」——统一改为「先选标志或锚点」
  （`calibration_workbench_confirm_none` / `calibration_role_required` / `calibration_need_selection`）；
  「角色」只作为二者的统称保留在代码与文档里。
  ④ ✓ 按钮文案：一个都不勾时只显示「✓ 写入」（置灰），不再附加「先选标志或锚点」的文字提示。
  ⑤（同日修订）"一个都不勾 = 中性白"**弃用**：白框与外层命中带白线 `#CCFFFFFF`、左上关闭钮白底糊在一起，
  真机上分不清框在哪 → 先还原为标志红，随后按用户决策定为**琥珀黄 `#FFC107`**（"还没决定写什么"）。
  三色定稿：一个都没勾 = 琥珀黄 / 只勾锚点 = 青 / 含标志 = 红（两个都勾 = 红圈 + 内圈青）。
  验证：`:app:test -PfastTests` 268 例 0 失败 + `assembleDebug` 通过；界面真机核对（切换勾选状态时底栏高度不变）
  并入 T2-3e。
- **T2-3h 写入前置条件提示 + 框选顶部说明** —— **已完成（2026-09-13，用户反馈后当天做）**：
  目标：① 进框选模式后**顶部就有**一句操作说明（不必点 ⓘ 翻长文案）；② 不满足写入条件时**直接说清还差什么**。
  验收：框选模式下顶部常显一句短说明（`fillMaxWidth` + 允许换行，`maxLines=2`）；「还差：…」**独立一行 + 全宽 +
  居中**，逐项列出缺失（多缺则顿号并列），全齐时转白显示"条件已齐，可以写入"；`enabled` 与提示行共用同一判据
  （含**归属状态**——原先漏判，导致状态没选时按钮可点、点了才在顶部报错）。
  首版踩坑（同日真机反馈）：两行都写 `maxLines=1 + softWrap=false` 想"防换行"，结果 `softWrap=false` 下文字**既不
  换行也不截断、直接顶出屏幕**；「还差」行还塞在 ✕ 与 ✓ 之间只剩窄条 → 改为 **`fillMaxWidth` + 允许换行 +
  说明文字独占一行**（与选区信息行同一教训：**工具栏行不放说明文字**）。
  另：无选区时不再显示「尚未框选」（用户指出是废话），但**保留该行占位**（不换行空格），保证画出选框时底栏高度不跳。
  同日追加口径：**选区坐标行移到「归属状态」上方居中**（原位置连占位一起删除）；可绘制区域限制（顶/底栏实测
  高度 → 画布外侧 padding）**已撤销**，画布恢复 `fillMaxSize`。**写入条件改为点 ✓ 后提示**（顶部条报
  「还差：…」一次报全），✓ 按钮**始终可点**（不置灰），底部工具条行距收紧（选中行 / 坐标行 4→2dp、
  工具行 8→4dp）。**「写入为」改名「角色」**（标签 + 帮助文案）；缺条件**改为深色弹窗**（标题「还不能写入」，
  正文「还差：…」琥珀黄），判据 `missingWriteConditions()` 由弹窗与写入入口共用。
  证据：`:app:test -PfastTests` 268 例 0 失败 + `assembleDebug` 通过；真机界面核对并入 T2-3e。
  口径说明：**不做"点击后再提示"**——置灰的 `Button` 收不到点击事件（Compose 语义），要有点击反馈就必须让它可点，
  那等于放行一次注定失败的写入；"缺什么"属于**前置状态**，应该在按钮旁边常显。
- **T2-3e 真机核对** —— **实点已执行（2026-09-13 晚，用户操作），日志证据待核**：用户报告「**所有弹窗都被点完了**」，
  即一次登录的多个活动弹窗被逐个自动关闭（也说明多弹窗场景**没有卡在"连续 3 次"上限**，否则会停在某个弹窗上）。
  **待补证据**：`MM-Click` 日志——允许下发记录、判定 ID（`fr01-N`）审计链、相邻间隔 ≥ 300ms、
  「动作后验证」序列、以及**是否出现「验证未通过 ×3 → 放弃」**（用于判定多弹窗是否无缝衔接）。
  **未完成日志核对前不勾选 B3 / B5**（如实记录，不用口述替代证据）。
  备注：红线 2 只承认三类合法来源（不能为测试新增"标定页点一下"的第五类），故本核对只能随 FR-01 真实点击一起做。

### T2-4 FR-01 弹窗闭环：判定与动作下发（B1）——**进行中（2026-09-13）**

目标：把「识别 → 点击 → 验证」串成一次**有界重试**的闭环。活动弹窗确认命中后，用已标定的关闭控件锚点下发一次
点击；点完必须由**下一轮识别**确认弹窗消失，未确认不得再点；连续 3 次仍命中即放弃并记录（requirements FR-01
失败处理：「宁可漏关，不可错点」）。

验收（对应 B1 / B2 / B4）：

1. 弹窗**确认**（滞回结论 = 活动弹窗）且锚点命中 → 下发一次点击（来源 FR-01，坐标 = 锚点匹配中心，ADR-002）；
2. **动作后验证**（红线 5）：点击轮之后的下一轮必须重新判定——弹窗不再命中 = 闭环成功并复位；确认前不得再次点击；
3. **失败处理**：连续 3 次「点击后仍命中」→ 放弃本次点击，此后不再点击，直到弹窗消失才复位；
4. **零猜测**（B2、红线 3/7）：未标定锚点 / 锚点未命中 / 判定不可信 → 不点击，日志说明原因（不静默）；
5. 非前台（冻结轮）→ 不点击、**不推进**重试计数（FR-09；非前台时"弹窗还在不在"无从判断）；
6. **演练模式**（B5）：判定与审计照记（日志写出「本应点击哪里」），但**不推进重试计数、不进入等待验证**——
   否则演练长跑会在 3 轮后"放弃"，且放弃的是从未真正发出的点击。

证据方式：`PopupCloseControllerTest` 单测（纯逻辑，覆盖上述 6 条）+ 真机演练日志（`MM-Capture` 的 FR-01 行 +
`MM-Click` 审计）；实点段由用户执行，随 T2-3e 一起核对。

实现要点：

- 决策本体 = `action/PopupCloseController`（纯逻辑、无安卓依赖，可 JVM 重跑）：每轮一次 `onRound`，输入
  「前台 / 模式 / 弹窗是否确认 / 是否命中 / 当前状态锚点命中列表 / 判定 ID」，输出「**上一枪的验证结论** +
  **本轮动作**」两部分——验证与动作会落在同一轮里（上一枪没打成，这一轮就必须再打）；
- 本类**不产生点击**：只给出 `ClickRequest`，下发一律经 `ClickDispatch.submit`（红线 6，多锚点时取产物声明顺序的第一个）；
- **锚点窗口必须并入采集层灰度转换**（T2-3b 的既有警告：漏掉会让锚点在"只转了标志窗口"的帧上读到全零像素而静默失败）
  → 采集层纳入「上一轮状态锚点窗口 ∪ 活动弹窗锚点窗口」：只纳入上一轮状态的话，弹窗**刚确认的那一轮**锚点读到的是
  未转换像素，白等一轮（真机 200ms~1s）；
- 阶段口径不变：弹窗只出现在「启动页 → 进入大厅」段，非该阶段 `hits` 里不含弹窗 → 本闭环自然不动作。

**风险 / 待验证（2026-09-13 用户纠正后追加）**：弹窗**不是每次登录只出现一个**，而是**可能有多个活动、点完一个出现下一个，
全部关闭后本次登录才不再出现**（FR-04 第 6 步原文即"0 个 ~ 多个，逐个关闭"）。
本闭环的"验证"判据是「点击后下一轮**仍命中活动弹窗** = 未通过」，因此存在一个边界：
若两个弹窗之间**无缝衔接**（没有"无弹窗"的中间帧），弹窗 B 会被误判为"弹窗 A 没关掉" → 连续 3 次即放弃并停点。
- **待验证**：两弹窗之间是否存在"无弹窗"的中间帧（节流 200ms / 1000ms + 弹窗弹出与关闭动画，很可能存在）。
- 若存在 → 本闭环天然正确（关掉 A → 下一轮不命中 = 判成功并复位 → B 出现走一遍新的识别 → 确认 → 点击 → 验证）。
- 若无缝衔接 → 需改判据；候选：把"失败"从"弹窗仍命中"改为"**画面无变化**"（弹窗区域灰度摘要比对）；
  注意"按命中了哪条 popup 记录区分"**不可用**——三条 × 记录在同一弹窗上同时命中 0.99~1.00（T2-2 已实测）。
- **取证位置**：T2-3e 实点段重点观察日志是否出现「验证未通过 ×3 → 放弃」序列。

**实现状态（2026-09-13）**：`action/PopupCloseController` + 采集层接线（锚点定位器、锚点窗口并入灰度转换、
FR-01 日志）已完成；`PopupCloseControllerTest` 12 例，全量 284 例 0 失败 + `assembleDebug` 通过。
**待做**：真机演练（零下发，`MM-Capture` 应出现 `FR-01 判定: 活动弹窗已确认，关闭控件「…」点击点 (x, y)（运行帧坐标），
第 1 次尝试 → 演练模式：只识别不下发（判定 fr01-N）`）→ 实点一次核对 `MM-Click`（门禁 + 300ms 间隔，即 T2-3e）；
首次实点前需用户真机标定一条「弹窗关闭」锚点。

> 设计口径（本次定稿）：① 锚点的**点击点 = 模板匹配位置的中心**，不再单独标定坐标（ADR-002「坐标一律来自识别判定结果」）；
> ② 锚点的「归属状态」= 它被启用时的当前状态（FR-01 关闭控件 → 活动弹窗；FR-04 各步骤锚点 → 对应步骤界面）；
> ③ 同一元素既要判状态又要点击时写**两条记录**（一条标志、一条锚点），与 §2.1「并列的独立角色」一致；
> ④ 标定页手势三区（T2-3f 定稿）：**框内 = 整体移动 / 选框外一环 = 拉伸缩放 / 更远 = 新建选框**，
> 手柄一律不画（白线本身就是抓手）——"不画"≠"不能用"；
> ⑤ 角色的「多选」（T2-3g）只决定**一次写入几条记录**：一条记录仍只有一个角色（§2.1），产物结构不变（仍 v2）。

### T2-5 唤醒集收窄为「活动弹窗」+ 负背景误命中验证——**已完成（2026-09-13）**

目标：守护待命期的期望集合由 {启动页} ∪ {活动弹窗} 收窄为 **{活动弹窗}**（去掉大窗口信号 `launch_start`），
并在**农场 / 好友列表 / 好友农场**背景上验证 3 条 × 记录**不误命中**。

依据（需求已于 2026-09-13 改定）：FR-01 触发行改「**任何阶段**每轮最高优先级……常驻守护待命期间同样检查」；
§2.1 活动弹窗行改「任何阶段」；FR-04 旧注「进入大厅后不再出现弹窗」**删除**，改为「弹窗处理贯穿整个登录环节；
弹窗的出现**不以保证"已进入大厅"为前提**」。
用户口径（本卡立论）：弹窗**物理上一定出现在大厅之后**，但程序**可能未第一时间识别到大厅**（大厅一闪即被弹窗盖住）
→ 判定"弹窗该不该管"的依据只能是"**弹窗自己出现了没有**"，不能依赖"是否已识别到大厅"。

验收：
1. 守护待命期每轮搜索集合 = 活动弹窗的全部标志记录（3 条），**不含** `launch_start` / `hall` / `server_select`；
2. **待命期与弹窗期的集合相同** → `PopupPhaseExpectedSignals` 的两阶段推进（含 `onRound` 用原始命中切阶段）随之退化
   为固定集合源：那个"命中大厅即永久收回窗口"的脆弱点在结构上被消除；
3. **零误命中**（离线）：农场 / 好友面板 / 好友农场帧上，3 条 × 记录结论全部为**未命中**；
   **命中对照**：「大厅 + 活动弹窗」帧（含「月度福利礼册」）上至少 1 条命中且判定**可信**（红线 7：多处高分按不可信处理）；
4. 既有能力不回退：`PopupCloseController` 12 例、`PopupPhaseExpectedSignalsTest`、`ActiveSignalSelector` 相关全量单测 0 失败；
5. 成本与代价**如实记录**：待命期 4 条（≈490ms，其中 `launch_start` ≈283ms）→ 3 条（≈207ms）；
   代价 = 守护期待命不再认识启动页 / 大厅 → 状态恒显「未知」（日志与悬浮窗可读性下降）。

证据方式：
- 离线：`CalibrationReviewProbeTest` 逐帧打印每条信号「结论 / 最高分 / 位置 / 竞争分」，样本取
  `set-20260912-03`（104 帧，含自己农场 / 好友面板 / 好友农场 / 大厅+多种弹窗）；报告镜像进 `docs/verification/m2/t2-5/`；
- 单测：`:app:test -PfastTests`；
- 真机：待命期日志出现「本轮搜索集合变化: 「popup_close、popup_close2、popup_close5」」，弹窗出现当轮即命中（演练零点击）。

实现要点：
- `CalibrationData.toLoop()` 默认注入源改为**固定集合**（活动弹窗全部标志记录）；`ExpectedSignals.ALL`（演练 / 离线回归）
  与 `FixedExpectedSignals` 保留；
- `standbyNames` / `popupNames` / `onRound` 阶段推进删除（收窄后二者集合相同、阶段无语义）；
- 若将来需要"悬浮窗实时显示当前界面"（**显示**需求，非流程需求），另立卡片用**低频全扫**实现（如每 N 轮），
  **不因此扩大唤醒集**。

**实现状态（2026-09-13）**：
- `decision/PopupWatchExpectedSignals` 取代 `PopupPhaseExpectedSignals`（阶段枚举 / `onRound` 推进 / `standbyNames` /
  `popupNames` 全部删除）；`CalibrationData.toLoop()` 默认注入随之更换。`ExpectedSignals.ALL` / `NONE` /
  `FixedExpectedSignals` 保留不变。
- 单测：删除 `PopupPhaseExpectedSignalsTest`（10 例，口径已废），新增 `PopupWatchExpectedSignalsTest`（6 例：
  搜全部弹窗记录 / 不含 launch_start 与其他状态 / 集合不随命中变化 / 命中大厅后仍搜弹窗 / 未标定得空集 / ALL·NONE 行为）。
  `:app:test -PfastTests` 通过（8s）；改动文件 0 lint 错误。
- **负背景验证（验收 1 / 3）**：新增探针 `recognition/PopupWatchProbeTest`，用生产默认口径跑 `set-20260912-03` 全批 104 帧：

  | 分组 | 弹窗记录命中数 / 帧数 |
  | --- | --- |
  | 农场 / 好友农场 | **0 / 15** |
  | 好友面板（列表 / 滚动） | **0 / 7** |
  | 大厅（无弹窗） | **0 / 14** |
  | 大厅 + 活动弹窗（命中对照） | 36 / 15 |

  每轮搜索条数恒为 **3**（口径生效）。证据：`docs/verification/m2/t2-5/`（逐帧报告 + 说明）。
  **局限如实记录**：负样本合计 36 帧 < 需求 §5 的 60 帧下限 → 按上界表述，真实误报率上界 ≈ 8.0%，**不得声称"零误报"**。
- **真机演练已通过（2026-09-13 21:08，用户操作设备 / AI 核对日志）**：集合行**只出现一次**变化且恒为
  「popup_close、popup_close2、popup_close5」；统计行全为「**单信号 0 轮** / 多信号 N 轮 / 不搜 0 轮」，
  且匹配次数 = 轮数 × 3；`未知 -> 活动弹窗（连续 2 次命中）` **仅此一次**转移、此后稳定保持
  （旧口径"命中大厅即永久收回搜索"在真机上不复现）；锚点 `popup_close_anchor` 命中、点击点 (1219, 1348)；
  **全程零真实点击**（MM-Click 记录全为演练拒绝）。
  证据：`docs/verification/m2/t2-5/t2-5-02-device-log.txt`（220 行业务日志）+ `t2-5-03-drill-verification.txt`（判据对照表）。
  **两项观察留待后续决定**：① 演练模式下判定行每轮重复（fr01-1 ~ fr01-20+）——因演练不推进重试计数，属 T2-4 验收 6 的
  有意设计，实点会收敛为「3 次后放弃」；若嫌日志噪音可改为"同一原因只记一次"；② 出现一条
  「不动作: 未命中关闭控件锚点（不猜位置）」——锚点在弹窗不同帧上时而未命中（弹窗内容 / 动画），
  保护按设计生效（零猜测、零点击），但**实点成功率**受其影响 → 实点段（T2-3e / B5）观察是否需补第二条锚点记录。

### T2-6 实点开关（B5 的用户入口）——**已完成（2026-09-13）**

目标：给用户一个**显式开启实点**的入口。现状：`ClickDispatch.enableLive()` 已存在但**没有任何 UI 入口**——
授权页「运行方式」卡片是 M1 的静态只读展示（源码注释："点击能力在 M2 首次开放后，本卡改为展示用户选择的运行方式"），
因此 **B5 实点段无法在真机上执行**。

验收：
1. 授权页「运行方式」卡片由静态展示改为**可切换**：显示当前模式（演练 / 实点）并提供开启入口；
2. **开启实点必须二次确认**：弹深色 AlertDialog，正文含风险说明（将对游戏界面产生真实点击）；
3. 切换即时生效于 `ClickDispatch.mode`，且 UI **实时反映**（模式以 `StateFlow` 暴露，不依赖 Compose 重组时机）；
4. **安全默认的边界**（三条都成立才算通过）：
   a. 进程启动 = 演练；
   b. 无障碍服务断开 / 被中断 / 销毁（`uninstall`）= 回演练（已有行为，需单测锁定）；
   c. **采集会话建立（INACTIVE → ACTIVE）= 回演练** —— 落实「每次会话开始前由用户明确指示『这次实点』」，避免切走/重授权后仍在实点；
5. 对话框「取消」不改变模式（默认拒绝）；
6. 本卡**不新增任何点击来源**（红线 2 仍为三类）；只改模式开关与安全边界。

证据方式：`ClickDispatchTest`（JVM：切换 / uninstall 重置 / 会话建立重置 / 默认值）+ 授权页真机核对（用户操作：
看到开关 → 尝试开启 → 二次确认 → 悬浮窗/日志显示实点）；实点链路核对随 T2-3e（`MM-Click` 门禁 + 300ms 间隔）。

实现要点：
- `ClickDispatch.mode` 由 `@Volatile var`（私有 set）升级为 **`StateFlow<ClickMode>`**，保留 `mode` 只读访问；
- `CaptureService` 会话状态转为 ACTIVE 时调用 `ClickDispatch.enableDrill()`；
- `strings.xml` 补「实点」档文案与确认弹窗正文；说明文字必须如实区分演练 / 实点，不得暗示"已能自动点击"。

**实现状态（2026-09-13）**：
- `ClickDispatch.mode` 由 `@Volatile var` 升级为 **`StateFlow<ClickMode>`**：新增 `modeFlow` 供 UI 观察
  （模式还会被非 UI 路径改变 —— 服务断开自动回演练 —— 所以不能靠"用户点了开关"这一次事件刷新）；
  `mode` 保留为只读 getter，门禁与既有调用方无感。
- `CaptureService` 会话转为 ACTIVE 时调用 `ClickDispatch.enableDrill()` —— **边界 c 落地**。
- 授权页 `RunModeCard`：静态展示 → **可切换**（Switch + 当前模式与说明文字随模式变化；实点档用 `error` 色标注）；
  **开启实点走二次确认 AlertDialog**（标题「开启实点？」+ 风险正文 + 「开启实点 / 取消」，**取消 = 保持演练**）。
- `strings.xml` 补 8 条文案（实点档 / 两档说明 / 开关标签 / 确认弹窗三段），并删除 M1 时期那句
  "自动点击能力将在后续版本开放"的过期表述。
- 单测 `ClickDispatchTest`（5 例）：切换 + 流同步 / 回演练 / **uninstall 回演练并卸载注入通道**（边界 b）/
  **会话建立重置**（边界 c 的效果）/ 无通道时拒绝且原因可审计（ADR-002）。`:app:test -PfastTests` 通过（6s），改动文件 0 lint。
- **真机核对通过（2026-09-13，用户操作）**：① 开关与当前模式可见；② 开启出现确认弹窗；③「取消」保持演练；
  ④「开启实点」切换到实点；⑤ 停止采集后重新授权自动回到演练。
  日志侧旁证：核对期间**零点击记录**（全量 `MM-` 日志无 `MM-Click` 行）＝ UI 操作未引发任何真实点击；
  会话于 21:16:44 / 21:17:21 两次正常建立（集合均为 3 条弹窗记录）。
  至此 **B5 具备实点执行条件**；实点链路核对进行 T2-3e（`MM-Click` 门禁 + 300ms 间隔 + 动作后验证）。
- **实点操作顺序（重要，边界 c 的含义）**：必须**先建立采集会话、再开启实点**——反过来会被"会话建立回演练"重置。
  即：授予采集权限并建立会话 → 在授权页开启实点（确认）→ 切回游戏触发弹窗。

### T2-7 业务日志落盘（取证通道）——**实现完成 + 落盘验证通过，待实点取证（2026-09-13）**

目标：把 `MM-*` 业务日志**双写**到应用私有文件，使取证不再依赖 logcat。

依据（2026-09-13 实测）：设备上有个应用每几毫秒吐一条 `HWUI BlurEffectManager` 日志，把 logcat `main`
缓冲在**数秒内**刷满——677 行快照里 `MM-` 记录 **0 条**，事后抓取拿不到任何证据（当天实点段因此无法取证）。
M5 的全流程点击审计（长流程、多账号）同样不能依赖 logcat。

验收：
1. `MM-*` 各类日志（点击审计 / FR-01 判定 / 状态与集合 / 会话 / 前台 / 悬浮窗 / 标定）同时进 logcat 与文件；
2. 落盘位置 `files/logs/mm-log-YYYYMMDD.txt`（按天分文件），每行带 `MM-dd HH:mm:ss.SSS` 时间戳（可与 logcat 逐行对照）；
3. **不阻塞调用方**：写入走单线程队列，帧线程 / 主线程只入队；
4. **有界**：单文件超过上限自动保留后半（按行切、不切半行），不无限增长；
5. 未注入 Context（JVM / 单测）时只写 logcat，不抛异常；写文件失败只告警，**不得递归调用自身**；
6. 格式与滚动逻辑为**纯函数**并覆盖单测；`:app:test -PfastTests` 与 `assembleDebug` 通过；
7. 本卡**不引入任何点击 / 识别行为变更**（红线 1 / 2 不受影响）。

证据方式：`MmLogFormatTest`（行格式 / 滚动切分）+ 真机取文件
（`adb -t <tid> shell run-as com.example.mastermechanic cat files/logs/mm-log-<yyyyMMdd>.txt`）→ 归档 `docs/verification/m2/t2-7/`。

实现要点：
- 新增 `log/MmLog`（门面 + 落盘，方法签名与 `android.util.Log` 对齐：`d/i/w/e`，便于整体替换既有调用点）；
- 新增 `log/MmLogFormat`（纯函数：时间戳 / 行格式 / 超限截断）；
- 新增 `MasterMechanicApp : Application`（`onCreate` 只做 `MmLog.install`）+ Manifest 注册 `android:name`；
- 7 个文件的 `Log.*` → `MmLog.*`（TAG 不变，仍是 `MM-*` 同族）。

### T2-8 实点段修复：忽略单次成败判定 + 采集模式提示——**已完成（2026-09-13）**

> **口径修正（同日，用户质疑驱动）**：本卡最初写的是"点击后宽限期"（假设"点击有效、只是画面反映慢"）。
> 用户指出该假设**没有依据**——日志里只有「活动弹窗是否命中」，而**「关掉 A + 立刻冒出 B」与「A 根本没关掉」完全同形**，
> 无法区分；用户实测观察是"挨个点掉、点掉一个马上又出现一个"，即**点击有效，问题在判据本身**。
> 宽限期只是把误判推迟、不解决问题，**已回退**。
> **改为（用户拍板）**：**撤销「单次点击是否奏效」的判定**——弹窗还在就继续点；唯一停止条件是
> **同一段弹窗内点击次数上限 12 次**（只兜底"锚点匹配到非关闭控件"，宁可漏关不可错点）；弹窗真的消失即复位，
> 下一段弹窗重新计数（多弹窗因此能逐个关掉）。`requirements.md` FR-01 的「成功判据 / 失败处理」两行已同步改写
> （按需求维护规则，条文中不出现具体参数值）。

依据（T2-3e 实点段真机，用户对照实验驱动；证据 `docs/verification/m2/t2-7/`）：

1. **全屏共享采集时无障碍手势注入无效**：用户对照实验（全屏 ✗ / **单应用 ✓** / 再全屏 ✗），日志支持——
   两次全屏会话点击后弹窗**始终不消失**，单应用会话点击后弹窗**消失**。两次的采集画布几何也不同
   （全屏 → 1440×3168 竖画布，单应用 → 横画布）。与 ADR-001「单应用采集口径」一致 →
   **使用口径必须是「共享一个应用」**，需在授权引导中明示。
2. **验证时机过短 → 误判"未通过"→ 3 次后放弃**（代码缺陷）：单应用那次点击其实**有效**——
   `22:24:42.286 点击 fr01-3` → `22:24:43.560 验证未通过（第 3 次）→ 放弃` → **`22:24:44.450 状态转移: 活动弹窗 -> 未知`**
   （弹窗在最后一次点击后 **2.16s** 才消失，而程序在 **1.27s** 就判失败）。三次验证间隔实测 452 / 1264 / 1274 ms。
   **后果**：一旦"放弃"，程序在弹窗消失前不再点 → **多弹窗场景只会点掉第一个**（即用户最初"没点掉"的另一半原因）。

验收：
1. `PopupCloseController`：点击后宽限期内的"弹窗仍命中"**不计失败**（记为"验证中"），超出宽限期才累计连续失败；
2. 宽限期取值有真机依据（实测关闭延迟 2.16s → 取 2500ms），日志可区分「验证中（宽限期内）」与「验证未通过」；
3. 单测覆盖：宽限期内不计数 / 宽限期外照常计数 / 宽限期内弹窗消失仍判成功 / 宽限期不改变"演练不计推进"语义；
4. 授权页采集卡片补提示：**必须选「共享一个应用」**（选全屏共享会导致点击无效）；
5. 既有能力不回退（`:app:test -PfastTests` + `assembleDebug` 通过）。

证据方式：`PopupCloseControllerTest` 新增用例 + 真机复验（单应用共享下**多弹窗逐个关闭**的完整落盘日志）。

实现要点：
- `PopupRoundInput` 增 `nowMs: Long`（帧轮时间戳，由调用方如实传入）；
- `PopupCloseController` 内部记录上次点击时刻，验证时算差值：`StillPresent` 且差值 < 宽限期 → 返回"验证中"（**不推进 attempt**）；
- 调用方（`CaptureService`）用已有的轮内时间戳填入，不新增时间源。

## 移交自 M1 的未结事项

| 事项 | 出处 | 处置 |
| --- | --- | --- |
| NFR-01 未达标（真机子集 P95 298.3ms / 全扫轮 ≈790ms） | M1 A9、`verification/t1-13/` | 移交 M5 最终验收；候选手段「收紧 4 个大窗口信号（≈2×）/ 多尺度粗定位」登记为 M5 性能任务候选 |
| T1-12a 判据「①判定明细 ⑤子集生效」未取证 | `verification/t1-12/` | T1-13 已补日志（转移轮判定明细 / 统计行实际搜索信号），可随时补测闭环 |
| 帧流静默停止检测（换区 / 游戏重启无回调） | T1-9 发现 2 | 建议随 FR-08 加固纳入后续里程碑 |
| R-2 如何确认「已进入目标好友的农场」（`requirements.md` §7 第 1 项） | `requirements.md` §7 | M4 前置，按研究任务继续 |
| 退出登录入口完整路径 / 服务器列表滚动处理 / 同一区服多好友（§7 第 2~4 项） | `requirements.md` §7 | M3 / M4 实现时按当前假设执行 |
| T1-8c 巡查状态标定（大厅 / 农场 / 好友列表 / 好友的农场） | `plans/m1-recognition.md` T1-8c | M4 前置，随 M1 流转并入后续计划 |

## 已确认口径（2026-09-13 用户定稿）

1. **弹窗只出现在「启动页 → 进入大厅」之间**：活动弹窗不会在农场 / 好友列表 / 好友的农场出现
   → 该阶段之外（含常驻守护待命期间）**不做弹窗检查**。`requirements.md` FR-01 触发行与 §2.1 活动弹窗行已同步
   （原「常驻守护下常驻每轮检查」已删除）。
   实现含义：守护待命期的预期集合为空 = 不搜；进入该阶段的判断由 T2-1 设计时给出方案（不得引入全扫兜底）。
2. **先演练、后实点**：真实点击**由用户执行**——用户操作设备并逐步告知当前界面状态，我负责核对识别结果、
   动作序列与判定证据；AI 不代用户点真实界面。
3. **复现账号已就绪**：**小号先完成一局对局即可消除新手引导 / 新手大厅**（2026-09-13 用户实测口径）；
   服务器列表显示账号等级，可据此确认所用小号是否为未过引导的新号。
4. **性能手段留 M5**：M2 不做窗口收紧 / 多尺度粗定位等优化；若某轮耗时影响动作节奏，如实记录并交由 M5。
5. **FR-02 移除**：新手引导 / 新手大厅的处理逻辑**不做**（`requirements.md` §1.1、§1.2、§1.3、§2.1、§3.1、§4 已同步，2026-09-13）；
   理由 = 账号打完一局后这两个界面不再出现，投入使用的账号可先手动过掉；恢复条件 = 后续出现无法先打完一局的账号时重新评估。
6. **弹窗阶段入口 = 命中「启动页」**（2026-09-13 用户拍板，落实第 1 条遗留的"进入该阶段怎么判断"）：
   守护待命的期望集合**不是空的**——只搜「启动页」（不搜它就没有任何办法知道游戏进入了「启动页 → 进入大厅」这一段；
   成本 = 一个小窗口信号，真机 7~22ms）；**命中启动页才注入弹窗期集合**
   {启动页 ∪ 服务器列表 ∪ 活动弹窗 ∪ 大厅}，命中大厅即收回待命。弹窗期带上「启动页」是因为它可能再次出现
   （返回启动页 / 换区），且滞回确认要走完 2 次命中。阶段推进用**本轮原始命中**，不等滞回结论
   （等 2 轮会让刚弹出的弹窗在本轮与下一轮都搜不到）。
   副作用（如实记录）：① 待命期停在农场等界面时状态显示为「未知」——不搜即不认识，这是"预期集合驱动、不兜底"的必然结果；
   ② 节流档位判据未改（仍为"命中当前状态才算稳定轮"），故待命期以 1s 档运行，性能优化留 M5（见第 4 条）。

## 演练 → 实点的推进方式（工作约定）

| 阶段 | 谁操作设备 | 我做什么 | 证据 |
| --- | --- | --- | --- |
| 演练（离线） | 我 | 改代码 + 单测 + 固定批次回放 | 回放报表镜像进 `docs/verification/m2/` |
| 真机演练（只识别不点击） | 用户走界面并告知状态 | 装包、读日志核对命中与判定 | 日志 + 截图 |
| 真机实点 | 用户走界面并逐步告知状态 | 核对「识别 → 点击 → 验证」闭环、节流与失败处理 | 日志 + 截图 |

- 实点必须在演练段全部通过（B2 ~ B4）后开启，且**每次会话开始前由用户明确指示「这次实点」**。
- 实点期间任一判据不成立（如动作后未验证、点击间隔不足）→ 立即停止并回到演练段定位。
