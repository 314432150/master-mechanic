# M1 计划：识别闭环

| 项 | 内容 |
| --- | --- |
| 状态 | 已完成（2026-09-13 收尾，口径 A：A1–A8 通过 + A9 如实记录不勾选） |
| 目标 | 「只识别、不点击」的闭环可用，且识别能力按 §5 口径可验证 |
| 前提 | M0 已交付（授权 / 前台 / 服务 / 悬浮窗壳，零点击基线）；ADR-003 先行落地 |
| 对应需求 | §2（界面状态与稳定性）、§4（不可点击约束延续）、§5（识别能力）、FR-09（会话区分）、NFR-01、NFR-02、附录「推进顺序」第 1 步 |

## 范围边界

- **做**：采集层（采集会话生命周期）、识别层（标志检测与判定记录）、决策层（状态机滞回）、标定、回放与指标工具、演练模式（只识别、不点击）、悬浮窗状态文字随识别更新。
- **不做**：任何点击（本里程碑仍全程零点击）；FR-01 / FR-02 的动作下发（M2）；巡查（M4）；悬浮窗完整交互（M3）。

## 验收清单（全部需真机核对）

- [x] A1 采集会话生命周期完整：建立 / 维持 / 终止均可见且正确；切换其他应用不终止会话，会话本身终止才停止循环（FR-09 会话区分）
- [x] A2 识别层可复现：同一批画面数据 + 同一套参数重复执行，结果完全一致（§5-4）
- [x] A3 判定记录完整：命中的标志、位置、置信度、同画面最强竞争位置四项齐全（§5-1）
- [x] A4 不可信处理：多处高分、或最高分与次高分差距过小时，按未识别处理（§5-2）
- [x] A5 状态机滞回：进入连续 2 次命中、离开连续 3 次未命中、连续 3 次未命中任何标志 → 未知；非前台期间不消耗连续计数（§2.2、FR-09）
- [x] A6 设备自标定：识别能力在目标设备上建立，代码零设备绑定参数；更换设备可重建（§5-5）
- [x] A7 误报率口径达标：负样本 ≥60；三类干扰分别统计；结论以上界公式表述（§5）
- [x] A8 演练模式：只识别不点击地运行；悬浮窗文字随识别状态更新
- [ ] A9 性能基线：单帧处理 < 20ms（P95，连续 100 帧）；检测间隔自适应（变化中 0.5 ~ 1s，稳定时 3 ~ 5s）（NFR-01、NFR-02 的 M1 基线，最终验收在 M5）
      **未勾选（2026-09-13 定稿，如实记录）**：间隔项**已真机核对通过**——T1-13 改为 200ms / 1000ms
      （间隔语义 = 「两轮之间的休息时间」），实测稳定档周期 = 1000ms 休息 + 单轮耗时（7~8 轮 / 10s），
      快档切换判据与预期一致；耗时项未达标——真机实测子集轮 P95 **298.3ms**、全扫轮单轮 ≈**790ms**
      （由未知态 2 轮 1.973s 反推），距 20ms 阈值仍差一个量级，瓶颈为大窗口信号（启动页 / 选择服务器）与 7 信号全扫；
      差距与后续方向见 `recognition/research/research-t1-10f-cost-profile.md` 与
      `research-t1-10g-subset.md`；证据 `docs/verification/t1-13/t1-13a-device-expected-driven.md`；
      NFR-01 / NFR-02 最终验收在 M5（T1-9 / T1-10 任务卡既有备注）
      **M1 收尾口径（2026-09-13 用户定稿，口径 A）**：M1 以「A1–A8 通过 + A9 如实记录不勾选」收尾并流转 M2；
      A9 的差距、候选手段（收紧大窗口搜索窗口 / 多尺度粗定位）与非验收项任务卡（T1-8 标定补充）
      随 `plans/m2-auto-handling.md`「移交自 M1 的未结事项」登记

## 任务卡

### T1-1 决策前置：落地 ADR-003（识别技术路线）

- 目标：确定标志检测技术途径，覆盖 §5 全部要求（判定记录产出、不可信处理、设备自标定衔接）
- 验收：ADR-003 状态为「接受」；`architecture.md` 开放决策表同步
- 证据：ADR 文件链接
- 备注：先于 T1-3；识别层保持「纯逻辑、可离线回放」的分层约束

### T1-2 采集层：采集会话

- 目标：采集会话的创建、维持与终止，向识别循环供给帧数据
- 验收：A1
- 证据：真机会话生命周期记录（日志 + 界面状态）
- 备注：M0-T0-4 顺延项；「切走不终止、会话终止才停」按 FR-09 区分；采集期间保持零点击

### T1-3 识别引擎（纯逻辑）

- 目标：标志检测 → 判定记录（命中、位置、置信度、竞争位置）→ 可信性判定；输入帧数据，输出判定
- 验收：A2、A3、A4
- 证据：JVM 单测 + 回放比对报告
- 备注：不含任何设备绑定常量（§5-5）；不依赖安卓框架，可在 JVM 离线重跑

### T1-4 状态机（滞回）

- 目标：§2.1 全状态 + §2.2 滞回 + 非前台冻结计数；识别循环接入采集帧管线（帧 → 判定 → 状态机 → 日志）
- 验收：A5
- 证据：滞回时序单测 + 真机状态变化日志（识别循环运行与冻结核对记录）
- 备注：状态变化必须输出日志（判定审计的一部分）；未命中任何标志达到阈值 → 未知；未知不操作；识别循环接入为 T1-2 帧管线既定承诺，NFR-02 自适应节流初版随接入落地（变化 / 稳定两档）；真实标志驱动的状态切换真机观察依赖标定产物（T1-5），届时补充核对

### T1-5 标定

- 目标：在目标设备上自建识别能力（样本采集 → 参数建立 → 落盘），全程无开发设备参数
- 验收：A6
- 证据：标定流程真机记录 + 产物审查（零设备绑定）
- 实现切片：
  - T1-5a 产物模型与编解码（纯逻辑）：产物 = 信号（模板 + 搜索窗口）+ 状态映射 + 匹配参数 + 标定帧尺寸；文本格式编解码；模板提取复用运行时同源灰度路径
  - T1-5b 标定采集与标注（应用侧）：从采集会话录制帧样本（落盘）；应用内挑帧、框选、命名、设参、保存产物；采集会话建立时加载产物接入识别循环
  - T1-5c 真机标定与核对：在目标设备完成真实界面标志标定（弹窗 / 新手等不可控界面留待后续补充），核对真实标志驱动的状态变化（补 T1-4 备注事项）
  - T1-5d 标注交互增强（真机标定反馈）：帧预览支持双指缩放 / 平移（横屏内容在竖屏帧画布中过小，需放大后框选）；选框支持拖动框内整体移动、拖动边 / 角拉伸（图片工具交互）；框编辑与视图变换为纯运算并单测覆盖，选框比例坐标语义不变（零设备绑定不破）
  - T1-5e 标注交互第二轮（真机标定反馈）：归属状态绑定默认信号名（选中状态自动填入名称框，免手输）；「新手大厅 / 新手引导」拆分为两个独立状态（§2.1 与术语表同步）；框选改为全屏画布（弹出式操作，「完成」退出返回，放大后不再遮挡页内其他区域）；选框左上角拉伸锚点改为关闭按钮（点按清除当前选框、可立即重新框选）；关闭命中为纯运算并单测覆盖，选框比例坐标语义不变（零设备绑定不破）
  - T1-5f 标定页布局聚焦（真机标定反馈）：缩略图列表由大图条改为矮方块条（裁切预览）、「框选标志」操作说明移入点击 ⓘ 弹窗展示、预览大图限制最大高度——预览大图与缩略图列表同屏可见，挑帧无需来回滚动
  - T1-5g 信号名录入移除（真机标定反馈）：信号名完全由归属状态的默认名决定，标定页不再展示名称输入框（使用者无需关心命名）
  - T1-5h 预览交互增强（真机标定反馈）：预览图支持左右滑动切换样本帧（缩略图列表选中随动）；双击预览图进入全屏框选；移除「全屏框选」按钮，改为预览画布内正下方的全屏图标（深色衬底 + 白色图标，与画面背景保持对比）
  - T1-5i 预览切换防闪烁（真机标定反馈）：预览画布按最近解码帧比例预留高度（≤260dp），切换样本帧时布局不塌缩；加载 / 失败态在占位画布内展示
  - T1-5j 缩略图联动时差修正（真机标定反馈）：滑页与缩略图联动由「定格后同步」改为「过半即同步」——拖动过半缩略图高亮立即随动，松手回弹不触发
  - T1-5k 选框锚点外置与视觉修正（真机标定反馈，2026-09-12）：框选改为「**外层操作框 + 内层真实选区**」——拉伸手柄与关闭钮挂在外层框上，内层框才是真实选区（解决真机反馈的「锚点过大压住边缘、看不清边缘」）；外层框白色细线、外扩收窄至 16dp、锚点缩小（角 5dp / 边 3.5dp / 关闭钮半径 8dp）；选框与锚点之间的带状区域按整体移动处理（想抓锚点差几像素不会清掉选框）；`hitCloseButton` 增「选区内一律不命中」保护（窄边距下热区侵入选区时，拖动选框内部不会误清选框）；最大放大倍率 8× → 16×。验收：① 锚点与关闭钮的绘制与命中都在选区之外（单测：锚点中心不落在选区内 / 内层框的角不再是手柄 / 选区内不触发关闭钮）；② 拉伸与移动语义与 T1-5e 一致（拉伸只动对应边或角、框内拖动移动）；③ 选框比例坐标语义不变（零设备绑定不破）；④ 真机框选时边缘与角可见、可放大到逐像素对齐。证据：JVM 单测 `FrameEditTest`（30 例）+ 真机框选截图 `verification/t1-5/t1-5-03-anchor-outside-selection.png`
  - T1-5l 标定流程重构（真机反馈定稿，2026-09-12 用户确认口径）：① **锚点移除**——框选只保留内层红色选框与左上角关闭钮（锚点影响视觉，全部去掉；拉伸能力一并取消，不满意就 ✕ 重框）；② **废除草稿模型**（`CalibrationDraft` / `DraftSignal` 删除）：产物即唯一数据源，框选 + 归属状态确定后**直接写入产物**（同名信号覆盖），不再有「提取为模板」「保存产物」两步；③ 操作收进**全屏工作台**：缩略图挑帧 / 框选 / 归属状态同页完成，取消页内预览大图与页内缩略图条；④ 产物**可读取、可删除**：页内列出产物信号（状态 / 模板尺寸 / 搜索窗口），可查看模板详情、删除单条信号、删除整个产物；⑤ 参数属产物级设置，编辑合法即写入（无产物时随首个信号写入）；⑥ 画布几何与产物不一致时拒绝写入并提示（避免产物混几何）。验收：① 框选 + 选归属状态即落盘（同名覆盖、无中间步骤），已保存列表同步可见；② 删除单条信号后产物立即更新，删空则产物文件删除（回到「无产物」）；③ 产物信号可查看（模板灰度图 + 尺寸 + 窗口帧像素）并可删除；④ 无锚点后选框边缘与角完全无遮挡，命中区仅「框内移动 / 框外新建 / 左上关闭」三类；⑤ 几何不一致的写入被拒绝且提示明确；⑥ 零设备绑定不破（模板 / 窗口 / 参数仍全部来自产物）。证据：JVM 单测（`CalibrationSignalsTest` / `FrameEditTest` / `CalibrationCodecTest`）+ 真机工作台截图 + 产物文件对照
    - 进展（2026-09-12）：**实现 + 离线验证 + 真机核对全部完成**（真机记录见 [t1-5l-12](../verification/t1-5l/t1-5l-12-real-device-verification.txt)：标定页 / 工作台 / 框选 / 覆盖写入 / 模板详情 / 删除单条 / 删除整个产物逐步截图 + 产物文件逐字段对照 + 核对前后 md5 一致；期间**发现并修复**「参数编辑写入落后一次编辑」缺陷——组合期快照值改为实时读取 + 现读盘上产物，复测通过）。① 纯逻辑：新增 `CalibrationSignals`（`upsert` 覆盖写入 / `remove` 删空返回 null / `withParams` / `geometryError` / `parseParams` / `defaultParams`），`SelectionWindow` 由被删的 `CalibrationDraft.kt` 迁入（语义不变）；`FrameEdit` 删除 `FrameHandle` / `resize` / `outerFrame` / `handleCenters`，命中只剩「框内移动 / 框外新建」，保留左上角外置关闭钮（外扩 16dp / 半径 8dp / 热区 20dp）。② 界面：标定页改为「采集卡 + 产物清单（逐条：名称→状态 / 模板 WxH / 窗口帧像素，可查看灰度图详情、可单条删除、可整体删除）+ 产物级参数（编辑合法即写盘）」；新增**全屏工作台**（画布 + 缩略图条 + 归属状态 chip，点 chip 即写产物）；删除页内预览大图、缩略图条、滑页预览与草稿卡。③ 写入路径：`writeSignalFromSelection`（解码 → 选区裁剪模板 → 窗口 → 几何校验 → 覆盖写入 → 存盘），几何不一致拒绝写盘并提示「先删除产物后重标或在与产物同方向画布下建会话」。④ 验证：单测 27 套件 **198 例零失败**（`CalibrationSignalsTest` 21 例新增、`FrameEditTest` 按新 API 重写 21 例、`CalibrationDraftTest` 8 例随草稿模型删除）；`:app:assembleDebug` 通过；证据 [t1-5l/t1-5l-01-offline-verification.txt](../verification/t1-5l/t1-5l-01-offline-verification.txt)。**余项：真机工作台截图 + 产物文件对照（核对前须先备份设备现有 7 信号产物、核对后还原）。**
  - T1-5m 工作台交互定稿（真机反馈，2026-09-12 晚，用户参考系统相册大图浏览与涂鸦编辑给出）：① 「打开标定工作台」入口移入「标定产物」卡，文案去括号备注；② 工作台大图占满全屏（黑底等比居中），缩略图条与工具栏**绝对定位悬浮**于屏幕底部；③ 浏览模式左右滑动大图切换帧，缩略图「过半即同步」（T1-5h/j 口径回填工作台）；④ 点工具栏「框选」进入**框选模式**：禁用滑页、隐藏缩略图条，工具栏左 ✕（放弃本次框选：清选框并回浏览）/ 右 ✓（确认写入：需已框选 + 已选归属状态，未选状态提示不写盘），工具栏上方出现归属状态滑动条（单选可切换）；点 ✓ 按选中状态写入产物（同名覆盖），成功后回浏览模式；⑤ 恢复选框**外层白色细线**（外扩 16dp，仅视觉；锚点仍移除，命中区仍「框内移动 / 框外新建 / 左上 ✕」三类）；⑥ 双指缩放 / 平移保留。**口径修订**：写入时机由 T1-5l 的「点归属状态即落盘」改为「✓ 确认写入」（用户定稿）。验收：① 入口在产物卡内、文案无括号；② 工作台全屏大图 + 底部悬浮条；③ 浏览模式滑页缩略图随动；④ 框选模式禁滑页 / 隐缩略图 / ✕ 退出 / ✓ 写入（守卫生效）；⑤ 白色外框可见且不遮挡选区边缘；⑥ 零设备绑定不破、产物格式与识别口径不变。证据：JVM 单测（`FrameEditTest` 外框几何）+ 真机截图 + 产物文件对照
    - 进展（2026-09-12 晚）：**实现 + 真机核对完成**——①入口移入产物卡（文案「打开标定工作台」）；②工作台全屏大图（黑底等比居中），底部缩略图条与工具栏深色衬底悬浮；③浏览模式滑页切帧、缩略图过半同步（T1-5h/j 口径回填）；④「框选」进入框选模式（禁滑页 / 隐缩略图，工具栏 ✕ ｜ 选区信息 ｜ ✓ 写入，上方归属状态单选滑动条，✓ 需已框选且未选状态给提示，成功后回浏览并显示覆盖消息）；⑤白色外层细线恢复（外扩 16dp 仅视觉，`FrameEditMath.outerFrame` 回归 + `FrameEditTest` 23 例）；⑥双指缩放保留。真机证据：[t1-5m-01](../verification/t1-5l/t1-5m-01-verification.txt) + 截图 t1-5m-02 ~ t1-5m-08；产物文件对照（仅覆盖信号变化、params/几何不变）+ 核对后 md5 与基线一致。补充（用户反馈，同日）：底部工具栏「框选」按钮改为**图标在上 + 文字在下**（新增矢量图标 `ic_frame_select.xml`，`ToolIconButton` 复用样式，禁用态降透明度），构建 + 单测（27 套件 200 例零失败）后装机交付；**用户实机自测结论：符合预期**（最终验证：产物 md5 与基线一致，自测未污染产物）。核对期间一次测试侧输入污染（差距线被误注入 0.17，界面与产物一致、非代码缺陷）当场纠正并复跑写入链路排除；产物最终已还原（md5 `a30a6422…`）。
- 备注：标定口径与 T1-1 的 ADR-003 衔接；样本索引归入 `docs/recognition/`

### T1-6 回放与指标工具

- 目标：回放样本集管理、同数据重跑比对、误报率统计（负样本 ≥60、三类干扰分列、上界公式）
- 验收：A7
- 证据：指标报告（含样本量 n 与上界）
- 实现切片：
  - T1-6a 样本集结构与加载（纯逻辑）：固定批次样本集 = 清单（样本文件名、类别标签〔正样本 / 负样本，负样本含三类干扰分列：半透明遮罩 / 动背景 / 相似多目标〕、正样本目标标志与位置、备注）+ 画面文件；清单解析与样本模型纯逻辑并单测覆盖；格式遵循 `docs/recognition/README.md` 版本化约定（set-YYYYMMDD-NN）
  - T1-6b 误报指标计算（纯逻辑）：按类别分列统计（样本量 n / 误报数 / 点估计）；「零误报」仅以上界 `1 − 0.05^(1/n)` 表述，n < 60 时输出「样本不足、不得声明零误报」标记；三类干扰不得合并统计；单测覆盖 0 误报 / 有误报 / n=60 边界 / 样本不足
  - T1-6c 同数据重跑比对（纯逻辑）：同一数据集 + 同一套参数多次运行 → 判定记录逐字段比对（沿用 T1-3 口径，扩展至真实样本集与参数版本）；差异明细定位到「样本 + 字段」；单测覆盖
  - T1-6d 指标报告模板与渲染（模板先于样本收集）：`docs/recognition/reports/report-template.md` 定义结构（口径与公式 / 样本量 / 参数集版本 / 三类分列 / 可复现命令）；渲染器与模板字段一致（纯逻辑 + 单测）；JVM 回放工具的 IO 适配（读样本、生成报告至 `docs/recognition/reports/`）由测试源集承载
  - T1-6e 固定批次采集与首份指标报告（A7 证据）：真机采集固定批次（含三类干扰与 ≥60 负样本）→ 清单标注 → JVM 回放生成首份报告；样本索引同步 `docs/recognition/samples-index.md`
  - T1-6e 备注（2026-09-12 会话方向复核，替代此前「横持重标定」前置）：画布方向由建会话瞬间的系统显示方向决定——日常配置（旋转锁定竖屏 + 辅助 App 竖屏前台）建会话 → 竖画布 1440×3168；游戏前台时游戏自声明横屏（应用请求优先于用户锁定），横屏内容旋转嵌入画布即真实使用形态；物理持机姿态不进入该链路。采集与运行保持同一建会话条件即可，无需横持重标定。
- 备注：回放样本集为固定一批（§5-4）；指标报告模板先于样本收集定义

### T1-7 演练模式与状态展示

- 目标：只识别不点击的运行方式 + 悬浮窗文字随识别状态更新
- 验收：A8
- 证据：真机演练记录（识别日志 + 悬浮窗截图）
- 实现切片：
  - T1-7a 界面状态信号：识别结论以独立信号对外输出（变化时日志含判据、订阅通知；初始与不可用情形一律「未知」）；采集会话重建 / 终止时重置「未知」
  - T1-7b 悬浮窗状态展示：收起态手柄文字随信号更新（「状态：<识别结论>」，FR-07 展示口径的 M1 部分）；重建时以信号当前值初始化；帧线程更新编组回主线程
  - T1-7c 演练模式明示：前台界面展示「运行方式：演练（只识别、不点击）」
  - T1-7d 真机演练核对（A8 证据）：标定产物加载 → 真实标志驱动的状态变化（「未知 ↔ 启动页」）中悬浮窗文字逐态核对 + 零点击审计
- 备注：M1 期满后点击能力仍待在 M2 首次开放（执行层点击转发器）；演练模式长期保留（回归验证用）

### T1-8 标定补充（移交后续里程碑前置，非 M1 验收项）

- 目标：为 §2.1 其余状态建立标志标定产物（当前产物仅启动页 `launch_start` 一个信号）；标定机制与标定页已就绪，本卡只做「采集样本 → 框选标定 → 产物更新 → 真机复演」，预期零代码改动
- 验收：目标状态在标定产物中有标志信号，且真机复演中滞回进入可见（演练日志 + 悬浮窗文字截图）；活动弹窗需覆盖多种样式（FR-01）
- 证据：标定记录（样本量 / 框选对象 / 产物审查）+ 真机状态转移日志，归档 `docs/verification/`（形式同 T1-5）
- 实现切片（按功能依赖分批）：
  - T1-8a 选择服务器（`server_select`）：标志建议「选择服务器」标题文字为主、左上角返回图标为辅（同一状态多模板）；设备在手时可任意时间先行完成，复演「启动页 ↔ 选择服务器」转移
  - T1-8b FR-01 / FR-02 状态（活动弹窗多样式、新手引导、新手大厅）：M2 前置；标志线索见 `docs/recognition/samples-index.md`（set-20260912-02）
  - T1-8c 巡查状态（大厅 / 农场 / 好友列表 / 好友的农场）：M4 前置；各步骤途经中间界面按 §7-3 随实现补充
- 备注：M1 完成定义（A1–A9）不受本卡影响；M1 流转时将本卡并入后续里程碑计划。标定模板必须从设备采集帧同源灰度裁剪，外部图片素材仅作「框选对象」指示，不得直塞产物（红线 4）

### T1-9 性能基线（A9）

- 目标：单帧处理耗时 < 20ms（P95，连续 100 帧）可测量、可取证；检测间隔自适应（变化中 0.5 ~ 1s，稳定时 3 ~ 5s）真机核对
- 现状前提：节流两档已落地（ACTIVE 1000ms / STABLE 3000ms，均落在需求区间内），稳定度切换逻辑已接线（T1-4）；缺「单帧处理耗时」测量手段与取证口径
- 验收：A9
- 证据：真机演练日志——连续 ≥100 帧的「单帧处理耗时」统计输出（P95 < 20ms）+ 节流档切换记录（变化 / 稳定两场景对照）+ 帧管线统计节奏，归档 `docs/verification/`
- 实现切片：
  - T1-9a 耗时统计器（纯逻辑）：逐帧记录处理耗时，连续 100 帧为一窗（满窗输出 P95 / 平均 / 最大并重置）；P95 取 nearest-rank（第 ⌈0.95n⌉ 位）；单测覆盖窗口边界 / P95 取值 / 满窗重置
  - T1-9b 帧管线接入与日志：`processFrame` 内测量「灰度转换 + 识别循环（含滞回）」耗时（转换失败轮不计入样本）；满窗输出一行统计日志；节流目标档位实际变化时输出切换日志（含前后间隔与判据），供 A9 间隔证据直接取读
  - T1-9c 真机核对（A9 证据）：演练模式下长跑，覆盖「稳定（无状态变化）」与「变化（状态转移前后）」两场景，收集 P95 统计输出与档位切换日志
- 备注：NFR-01 / NFR-02 最终验收在 M5（本卡只交 M1 基线：测量手段 + 首轮真机数据）；间隔参数**已由 T1-13 调整**（1000 / 3000 → 200 / 1000，且间隔语义改为「两轮之间的休息时间」，见 T1-13「人速口径」）；耗时口径不含节流判定、标定旁路与统计日志本身
- 进展（2026-09-12）：T1-9a/b 完成（耗时统计器 `FrameTimingStats` + 帧管线分段接入 + 节流切换日志）；T1-9c 真机首轮完成——**P95 2287.6ms（灰度 52.7ms / 识别 2241.2ms），未达标，如实记录**；瓶颈为朴素 NCC 全窗滑动（识别段占 98%）；整改移交 T1-10。证据见 `docs/verification/t1-9/`。

### T1-10 性能达标优化（A9 整改）

- 目标：单帧处理耗时 P95 < 20ms（真机连续 100 帧统计）；判定语义不变（固定批次新旧判定记录回归通过）
- 背景：T1-9 首轮真机数据 P95 2287.6ms（灰度 52.7ms / 识别 2241.2ms）——瓶颈为朴素 NCC 全窗滑动（约 9.6e7 次乘加 / 帧）；桌面同源探针 45ms（超标 2.25 倍，设备环境再放大约 50x）
- 验收：A9（达标）
- 证据：优化前后真机 100 帧统计对照 + 固定批次回归（判定记录逐字段比对 + A7 指标复跑）+ 单测
- 实现切片：
  - T1-10a 匹配算法优化（纯逻辑）：帧侧搜索窗口前缀和（整数积分图，位置统计量零误差）+ 采样粗搜（模板 2×2 抽取，网格步进）+ 候选网格点全精度精搜（精确覆盖网格间隙）；最终候选分数全部取全精度（阈值与差距判定输入不变）；单测覆盖「粗搜 + 精搜与穷举全精度一致」「边界」「确定性」
  - T1-10b 帧管线裁剪与调度（平台层）：灰度仅转换「各信号窗口行并集」（原始帧拷贝量按行裁剪，灰度数组全帧尺寸、窗口外留零）；帧线程处理时提升至显示优先级
  - T1-10c 离线回归与提速取证（JVM）：固定批次「优化前 / 后」判定记录逐帧比对 + 加速比探针（桌面对照）+ A7 指标复跑（三次一致性）
  - T1-10d 真机核对（A9 达标证据）：连续 100 帧 P95 < 20ms；节流两档切换节奏保持（变化 / 稳定对照）
  - T1-10e 后续手段评估（3×3 采样 / 部分和剪枝，2026-09-12 用户口径启动）：目标——量化两种手段在固定批次
    （104 帧 + calib-v1）上的**耗时收益**与**判定一致性**，给出是否采纳的结论与对 NFR-01 的影响判断。
    验收：① 探针可复跑，输出「基线 / 3×3 采样 / 部分和剪枝 / 二者组合」四档的 P50·P95·平均耗时与提速比，
    并先以「复刻基线 ≡ 生产实现（逐字段一致）」自校验探针正确性；② 各变体与基线判定记录逐字段比对
    （峰值分数 / 位置 / 峰值序列），漂移如实记录；③ 结论文档给出是否采纳 + 理由 + 对真机 P95 的推算影响，
    以及若不达标的结构性建议（窗口面积 / 信号数 / 帧降采样等，留后续里程碑）；④ 采纳则改生产代码并补单测
    （判定语义不变由探针 + 单测双重把关）。证据：探针输出 `app/build/replay-work/out/t1-10e-speedup.txt`
    + 结论文档 `docs/recognition/research/research-t1-10e-speedup-evaluation.md`
    - 进展（2026-09-12）：**评估完成**。探针 `SpeedupProbeTest`（测试源集，可复跑）先做
      「复刻基线 ≡ 生产实现」自校验（不一致 0 帧），再在固定批次 51 有效帧上量化：
      3×3 采样 ×1.39（**best 位置 7/51 帧漂移**、结论不变、best 分数最大差 0.0011）；
      部分和剪枝 ×1.06（**语义零漂移**：结论 0、位置 0、best 分数差 0）；二者组合 K32 ×1.70（漂移同 3×3）。
      剪枝首版曾因漏掉剩余行均值修正项误剪（9/51 帧结论改变），修正后消失（教训入结论文档）。
      **结论：两种手段均不采纳**——收益仅 1.1~1.7×，最优组合推算真机 P95 由 ≈313ms 降至 ≈184ms，
      距 20ms 仍差约 9 倍；且 3×3 引入位置漂移。剪枝收益低的机理：精搜候选本就是高分邻域，缺少可剪的
      「明显不匹配」样本。**A9 判定：M1 内不勾选，如实记录**（NFR-01 最终验收在 M5）；结构性建议
      （按状态启用信号子集 / 收紧搜索窗口 / 多尺度粗定位 / 算法替换 / 需求口径确认）写入结论文档待排期。
  - T1-10f 多信号成本与分段耗时剖析（2026-09-12，用户追问「是否只测了『开始游戏』」+ 要求继续找方案）：
    已完成——① **既有真机数据确实只有单信号**（T1-9 / T1-10d / T1-10e 全部基于 calib-v1 单信号
    `launch_start`，设备产物后扩到 7 信号但从未做过 7 信号真机测量）；② 桌面多信号：
    7 信号 38.04ms vs 1 信号 9.88ms（≈3.85×，单信号均值 9.88→5.43ms），按 T1-10d 单信号识别段
    ≈294–308ms 粗推 **7 信号真机或达 ~1.1s/帧（待实测）**；③ 分段耗时：**粗搜 ≈79% / 精搜 ≈20% /
    前缀和 ≈1%**——推翻「精搜是瓶颈」假设，也解释了剪枝仅 ×1.06（只作用于精搜那 20%）。
    证据：结论文档 `docs/recognition/research/research-t1-10f-cost-profile.md` + 探针
    `SpeedupProbeTest.profileMultiSignalCost` + 输出 `app/build/replay-work/out/t1-10f-cost-profile.txt`。
    新方案清单（按状态启用子集 ≈3.5–7× / 收紧窗口 ≈2× / 多尺度粗搜 ≈2–3× / 工程侧降分配（GC 与带宽，
    设备×桌面 ×27 提示瓶颈或不在乘加）/ 网格与采样 1.3–1.4× 已否决）见文档第 3 节，待排期。
  - T1-10g 按状态启用信号子集（2026-09-12 用户口径批准启动）：目标——识别循环每轮只匹配「当前状态所需的信号子集」，
    把 7 信号全扫（推算真机 ≈1.0s/帧）降到单 / 双信号量级（≈270ms）。背景（用户指出）：实际运行时
    大多数场景同一时间只需搜一个信号，只有「点击登录 → 进入大厅前」需同时搜新手引导 / 新手大厅 / 活动弹窗，
    其余信号按流程步骤在指定阶段搜索；而当前实现每帧全查全部信号。
    验收（先写后做）：① 稳定在已知状态时只匹配子集信号（子集外信号不产生判定记录）；
    ② **未搜索的信号不计入「未命中」连续计数**（与 FR-09 冻结同语义，不得因未搜索而误判离开）；
    ③ 兜底全扫：状态未知 / 上一轮当前状态未命中（疑似转移，立即全扫）/ 每 N 轮一次；
    ④ **判定与状态序列不变**：同一批设备帧上「子集模式」与「全扫模式」的状态序列逐帧一致（差异如实记录）；
    ⑤ 桌面测量给出子集模式耗时与收益（并换算真机）；⑥ 子集映射集中一处配置，单测覆盖
    「每个状态至少有一个信号进入子集」。证据：单测 + 探针 `SpeedupProbeTest.measureSubsetSavings`
    输出 `app/build/replay-work/out/t1-10g-subset-savings.txt` + 结论文档。
    - 进展（2026-09-12 深夜）：**实现 + 单测 + 桌面实测完成**。新增纯逻辑 `ActiveSignalSelector`
      （未知 / 转移探测 / 周期兜底 → 全扫；稳定态 → 自身信号 + 附加集），`SignalDetector.detect(names)`
      支持子集（子集外信号**不产生判定记录**，不是记为未命中），`RecognitionLoop` 接线并加入
      **按需补扫**（当轮当前状态未命中 → 同轮补扫其余信号，消除转移检测延迟、状态序列与全扫零差异），
      `CalibrationData.toLoop()` 默认启用。实测（设备 67 帧 + 7 信号产物，生产路径）：
      **状态序列差异 0/67**（语义等价）；**搜 1 个信号 2.31ms vs 搜 7 个 36.80ms（≈1/16）**；
      本批采集图整体 ×1.29（76% 帧触发全扫：30% 未知 + 补扫 + 周期兜底）——真实运行停留在已知状态时
      单帧 ≈62ms（真机推算，对比全扫 ≈993ms，约 16×）。全量单测 **214 例零失败**（新增 10 例）。
      结论文档 `docs/recognition/research/research-t1-10g-subset.md`。
      **附加集结论（2026-09-13 用户确认，不启用）**：弹窗只在「点击登录后」出现一次（遮挡大厅→关闭→进大厅），
      农场 / 好友列表 / 好友农场无弹窗；弹窗出现时当前状态必然未命中 → **按需补扫**同轮发现 `popup_close`，
      附加集多余（且要在稳态多付 ≈156ms/帧）。数据佐证：采集图 7 帧 `ACTIVITY_POPUP` 在子集模式下与全扫
      状态完全一致（差异 0/67）。产物暂无 `tutorial_*` 信号（T1-8a 未标定），后续需要时在
      `fromRules(attached = ...)` 加一行即可。实测三配置：全扫 36.85ms / 子集 28.70ms（×1.28，稳态搜 1 个
      2.31ms ≈62ms 真机）/ 子集+附加 29.88ms（×1.23，稳态搜 2 个 8.08ms ≈218ms 真机）。
      **待办**：真机长跑复核；period 可调；与"收紧窗口"组合。
    **下一步：先补 7 信号真机基线（装机 + 用户长跑），再定优化实施顺序。**
- 备注：采样与步进为算法固定特性（与设备无关，红线 4 不破）；对判定起作用的分数（最高 / 竞争 / 阈值比较）全部为全精度值，采样仅用于粗筛——语义不破由回归与单测双重把关；若 2×2 采样不足以达标，再评估 3×3 采样 / 部分和剪枝（另行记录）
- 进展（2026-09-12）：T1-10a/b/c 完成——固定批次（104 帧）判定零漂移（结论 0 不一致、≥0.5 分档差 0、位置漂移 0；尾部最大差 0.0007 / 0.0276 如实记录）；桌面识别耗时 P50 74→10ms / P95 79→11ms（≈7.4×）；A7 指标复跑逐字段一致；单测全量 170 例 0 失败（24 套件，临时探针文件删除后复跑）。证据见 `docs/verification/t1-10/`。**T1-10d 真机核对已执行（2026-09-12，vivo V2463A）：三条满窗一致——总 P95 312.8–327.8ms（灰度 20.6–27.6ms / 识别 294.3–308.0ms），未达标，如实记录**（较 T1-9 基线提速 ≈7.0–7.3×；设备/桌面识别倍差 ≈27–28×）；节流两档切换（1s↔3s）与「未知 → 启动页」命中两会话成对复现；真机取证与截图见 `docs/verification/t1-10/`（t1-10-04 ~ t1-10-06）。**后续手段评估（3×3 采样 / 部分和剪枝）待启动**；A9 维持未勾选。

### T1-11 画布方向动态适配（研究先行）

- 背景（2026-09-12 用户口径，重大局限确认）：识别产物以「标定帧 1440×3168（竖画布）」为唯一基准；运行画布方向由建会话瞬间的系统显示方向决定——横画布（3168×1440）下帧尺寸与产物不一致，识别全失效（应用如实告警后状态持续「未知」；T1-10d 准备期实录 1 次，见 `verification/t1-10/` 环境事实节；另 T1-5 / T1-7 同类记录）。现有保障仅为「授权时 MM 竖屏前台」的流程约定，隐含「旋转锁定竖屏」等环境前提（T1-6e 备注），对真实用户不可控：锁定关闭 / 会话在游戏前台下重建等场景均可能落到横画布。目标：不依赖用户设置与操作环境，横竖画布下识别均正确。
- 目标：同一标定产物在竖画布与横画布两种运行会话下均正确识别（以启动页信号为先导验证；其余状态随标定补充自然适用）
- 验收（先写后做）：
  1. 真机双会话复演：竖画布与横画布会话均出现「未知 -> 启动页」命中，且无尺寸不一致告警；
  2. 判定一致性：同一界面内容在两种方向的帧上判定一致（结论 / 位置；固定批次 + 方向变体样本 JVM 回放，样本量如实记录）；
  3. 零设备绑定不破（红线 4）：适配逻辑不含开发设备分辨率 / 坐标常量；判定语义不变（对判定起作用的分数仍全精度）
- 证据：两种方向的样本帧与几何映射结论（归档 `docs/recognition/`）+ 双方向真机运行日志（归档 `docs/verification/`）
- 实现切片：
  - T1-11a 样本与几何对照（研究先行）：同界面（启动页）分别在竖 / 横画布会话下采集帧；测量两向帧内容的映射关系（旋转角度 / 是否缩放 / 偏移，给出精确结论）
    - 进展（2026-09-12）：**离线测量完成**——结论：**无旋转，等比缩放 + 居中平移**；比例 = 两画布宽之比（1440/3168 = 0.4545），竖画布内容带高 655（期望 654.55）、上留白 1256 / 下留白 1257（左右无留白），与「横屏内容等比缩放居中嵌入」模型一致；归一化后锚点（开始游戏）命中 0.9749 @651,1745 对竖画布原生 0.9989 @651,1744（残差 0,1 px），其余三个旋转角锚点分数 ≤0.28。结论与原始输出：[research-canvas-orientation.md](../recognition/research/research-canvas-orientation.md)；目视对照 [t1-11a-band-compare.png](../recognition/research/t1-11a-band-compare.png)；测量工具 `CanvasOrientationProbeTest`（可复跑）。
    - 待补项已闭合（2026-09-12）：真机横画布会话样本 37 帧（3168×1440）已采并归档（`recognition/samples-index.md` set-20260912-05）；探针按其代表帧复跑——旋转 0° 唯一成立（缩放 2.2、带内相关 0.7419、锚点残差 0,1px），结论复核一致。
  - T1-11b 方案定稿：候选方向——运行时归一（帧或产物坐标按方向旋转归一后进入现有匹配链路）/ 产物多方向变体（加载时生成）等；选定并记录取舍（必要时补 ADR / `architecture.md` 条目）
    - 定稿（2026-09-12）：**选用候选 A「按画面区的运行时归一」**（不改采集画布，识别前把运行帧摆回标定几何）；取舍与备选方案见 [ADR-007](../decisions/ADR-007-画布方向适配.md)，`architecture.md` 开放决策表与不变量已同步。
    - 取舍输入（2026-09-12，来自 T1-11a 实测）：①无需旋转，方案可降级为「等比缩放 + 平移」；②失效机理 = 产物搜索窗口按整幅比例换算后纵向偏约 247px（横画布下窗口 y 比例 0.556 对应内容带内 0.773）→ 换算必须经「内容带」中介；③候选 A 运行时归一（本次已间接验证：归一化帧 + 原生模板锚点 0.9749，远高于命中线 0.85）、B 产物多方向变体（模板重采样方向未测）、C 只换算窗口（B 的轻量版）；判据 = 判定语义不变 / 零设备绑定 / 增量成本。④候选 D（在采集层把画布固定为横屏尺寸）已评估否决：违红线 4（或退化为依赖「游戏恒横屏」假设，与目标相反）+ 识别成本约 ×23（内容满幅后模板 / 窗口像素面积 ×4.84）+ 需重标定；详见研究文档 §6。⑤边界形态（2026-09-12 用户观察 + 档案复核）：会话首帧可能是「旋转满幅」的画布过渡帧（画布尺寸不变、内容被 90° 旋转铺满，见研究文档 §9）→ 实现须显式跳过（帧尺寸同标定帧但内容带≈整幅 → 判未识别），且不得被「多方向变体」当成合法方向；滞回对单帧异常天然过滤，不构成误报风险。
  - T1-11c 实现与单测：按方案落地；新增用例覆盖方向归一正确性与判定一致性
    - 进展（2026-09-12）：**实现与单测完成**——`CanvasGeometry`（画面区 contain 模型 + 标定 ↔ 运行帧换算，含采样外扩与确定性取整）、`FrameNormalizer`（按窗口区域重采样归一；画面区一致时零拷贝快路径）、`RecognitionLoop` 检测前归一、采集层窗口区域改为「标定窗口映射到运行帧」、`CalibrationData.toLoop()` 带入几何、运行期日志由「尺寸不一致，识别可能不可用」改为「方向不同，已归一并继续识别」。单测新增 12 例（`CanvasGeometryTest` 7 + `FrameNormalizerTest` 5，含恒等快路径 / 真机实测值锚定 / 归一后判定一致 / 过渡帧不误报 / 确定性），探针新增真实帧生产路径核对 1 例：**横屏取证帧 3168×1440 经生产路径归一后 `MATCHED 0.9538 @651,1744`，对竖画布原生 `0.9989 @651,1744` 位置残差 0px**，两会话均进入「启动页」；全量 27 套件 184 例零失败。
  - T1-11d 真机核对：双方向会话复演 + 竖画布回归
    - 完成（2026-09-12，vivo V2463A）：三条会话——A 竖画布 1440×3168（回归，无方向提示）`未知 -> 启动页` 17:57:08；B「共享一个应用」时系统先回 MM → 仍竖画布（环境事实）；**C 横画布 3168×1440**（`wm size` 临时置横屏建会话，事后已还原）→ 日志「方向不同…已按画面区几何归一后继续识别」+ `未知 -> 启动页` 18:05:58，全程无降级告警；同批录制横画布样本 37 帧。证据：`docs/verification/t1-11d/`（t1-11d-11 真机取证 + t1-11d-12 运行日志 + 双会话截图）。
- 备注：非 A1–A9 验收项（真实使用可用性修复）；排期先于 T1-10 后续手段评估（3×3 采样 / 部分和剪枝）——2026-09-12 用户口径

### T1-12 标定产物逐信号真机复演（非 A1–A9 验收项）

- 背景：设备侧 7 信号产物已标定完成（T1-8a/b/c），但**从未逐信号在真机上验证识别效果**；
  交互流程（M2 / M4）尚未实现，故按「用户操作设备、AI 验收归档」的方式逐个界面核对。
- 目标：每个信号在其归属界面上能被正确识别（进入 → 稳定 → 可离开），并留下可回溯证据。
- 验收（每个信号逐项，**先写后做**）：
  ① 判定记录四项齐全（命中标志 / 位置 / 置信度 / 最强竞争位置，§5-1）——日志取证；
  ② 状态转移正确：连续 2 次命中 → 进入该状态（日志含判据），悬浮窗文字同步；
  ③ 稳定：在该界面静置 ≥30s（稳定档约 10 轮）状态持续保持、不抖动；
  ④ 可离开：切到其他界面后连续 3 次未命中 → 回到未知（不卡死）；
  ⑤ 子集模式生效（T1-10g）：稳定态只搜该状态自身的信号；
  ⑥ 全程零点击（应用侧，红线 1 / 2）。
- 切片：T1-12a 启动页（信号 `launch_start`，状态 `LAUNCH_PAGE`）→ T1-12b 选择服务器 → T1-12c …（逐个补齐）
- **T1-12a 结果（2026-09-13 真机，进程 19758）**：②③④⑥ **通过**（三条转移均正确且带判据：未知→启动页 /
  启动页→选择服务器 / 选择服务器→启动页；启动页稳定 ~90s 与 ~140s 无抖动）；①⑤ **未能取证**——运行日志
  只输出「状态变化 + 10s 统计」，无判定明细、无 `searched`（统计行「信号 7 个」是标定**总数**口径，
  不反映本轮搜索数）。**用户反馈"切换慢"已量化归因：状态转移需 2 轮，而轮 A（未命中 + 补扫发现候选、
  `candidateStreak=1`）因 `transition == null` 被判为"稳定轮"仍按 3000ms 排下一轮 → 总延迟 3~6s**
  （实测 未知→启动页 6.2s、启动页→选择服务器 3.2s、选择服务器→启动页 3.4s）。建议优化：降档判据改为
  「无转移 **且无候选累积**」→ 总延迟 1~4s（稳定档仍 3000ms，不破 NFR-02）。证据
  `docs/verification/t1-12/t1-12a-launch-page.md` + `runtime-log-19758.txt`。
  **待用户定**：是否实施节流降档优化 + 补齐"状态转移轮输出判定明细 / searched"日志后再复测。
- 取证：应用运行日志（`adb logcat`）+ 悬浮窗 / 界面截图；证据归档 `docs/verification/t1-12/`
- 分工：用户操作设备，AI 负责验收、核对与归档（只读 adb 取证，不注入点击）

### T1-13 识别口径转为「预期驱动验证式」（取消发现式全扫）

- 背景（2026-09-13 用户口径，实际使用场景为**巡查**）：流程每一步都有**明确的预期状态**，识别只需回答"预期的那一个在不在"，
  不需要"随时判定当前处于哪个界面"。用户给出的完整链路（口径依据，均为**步骤级**）：
  农场（用户经悬浮窗二级菜单发起「切换小号」，起点必然在农场）→[退出农场 + 确认]→ 预期**大厅** →
  [系统设置 → 退出登录]→ 预期**启动页** →[换区]→ 预期**选择服务器** →[点配置指定的区服]→ 预期**启动页** →
  [开始游戏]→ 预期**活动弹窗或大厅**（弹窗 0 个 → 直接进大厅；N 个 → 逐个关闭后进大厅；**进大厅后不再有弹窗**）→
  [大厅内农场入口]→ 预期**农场** →[好友列表]→ 预期**好友列表** →[配置指定的好友]→ 预期**好友的农场** → 本项结束。
- 需求依据（**不新增需求，现状实现与既有条款不符**）：FR-04「每一步都必须在执行动作后验证结果」+ 硬性要求 #1
  「确认画面已变成**预期状态**」；§2.1 状态表本就按「常驻守护下 / 巡查流程下」分栏；§2.2 滞回与「未知」定义不变。
- 现状（T1-10g）为「自动发现式」：未知态全扫 + **按需补扫** + 每 10 轮兜底全扫——三处都在为"发现其他状态"服务，
  既是用户不需要的开销，也是节流器不敢用快档的原因（怕撞全扫尖峰）。
- 目标：每轮只判定「当前预期状态的信号集合」（通常 1 个；「开始游戏 → 进大厅」这一步为 `{大厅, 活动弹窗}` 共 2 个），
  不再为"发现其他状态"扩大搜索。
- 验收（**先写后做**）：
  ① 稳态每轮只判定预期集合内信号；集合外信号**不产生判定记录**（沿用 T1-10g 语义，不记为未命中）；
  ② **取消按需补扫**：预期未命中时不再补搜其余信号（补扫属"发现"，本口径不需要）；
  ③ **取消周期兜底全扫**（原每 10 轮一次）；
  ④ 保留「无预期 / 状态未知 → 全扫」，作为唯一需要"找"的时刻（启动对齐、守护待命、会话重建）；
  ⑤ 节流降档判据改为"**确实稳定**"（当前状态命中 ∧ 无候选累积 ∧ 无转移）→ 未达预期 / 候选累积期间用**快档**，
  只有确实稳定才降到稳定档（两档数值与语义见下「人速口径」）；
  ⑥ 运行日志可自证：状态转移轮与 10s 统计行输出**本轮实际搜索的信号名**（补齐 T1-12 判据①⑤的取证缺口）；
  ⑦ 真机复测（T1-12a 重跑）：被动切换启动页 ↔ 选择服务器 时，状态序列为
  「启动页 →（连续 3 次未命中）→ 未知 →（全扫）→ 选择服务器」，静置期每轮只搜 1 个（日志可证），全程零点击。
  **✅ 已通过（2026-09-13，用户装机实测）**：逐条验收与原始日志见
  `docs/verification/t1-13/t1-13a-device-expected-driven.md`——实测静置 7~8 轮 / 10s（全扫 0 轮）、
  未知态全扫轮 ≈0.99s、子集轮 P95 298.3ms、被动切换总延迟 ≈3.5s（取消发现式搜索的预期代价）。
- 说明：⑦ 中被动切换变慢（先回未知）是本口径的**预期行为**——真实巡查由流程驱动（有明确预期）。
- **取消补扫的连带影响（必须写清）**：T1-10g 结论文档第 4 节曾以"补扫天然覆盖弹窗"为由不启用附加集；
  补扫取消后，**弹窗 / 画面变化的发现路径改为**「连续 3 次未命中 → 转未知 → 全扫」（滞回语义，功能不丢），
  代价是慢几轮。M2 流程编排把"本步预期集合"经 `ActiveSignalSelector.attached` 注入后即可当轮发现
  （例：「点击开始游戏后」预期集合 = {大厅, 活动弹窗}）。
- 证据：单测（子集 / 补扫取消 / 降档判据 / 日志字段）+ 桌面回放固定批次状态序列复跑 +
  真机日志与截图归档 `docs/verification/t1-12/`。
- 需求同步（2026-09-13 用户确认，已落 `requirements.md`）：FR-04 #6 补写活动弹窗「**0 个 ~ 多个**」；FR-04 #10 改为
  「到达好友的农场即完成，**不做任何返回动作**」（原 §7-1 关闭）；FR-01 触发限缩为"**可能出现弹窗的阶段**"
  （守护模式仍常驻每轮）；§2.1 弹窗行同步。
- 起点与终点（2026-09-13 用户确认）：本项终点 = **好友的农场**（之后「祝福」由用户手动完成，直到用户自行发起下一次切换小号）；
  下一次起点 = 自己的农场或好友的农场，**两者换号路径一致**；大厅发起 = 农场发起流程的**子集**（跳过「退出农场」一步，
  自「退出登录」起与农场发起完全重合；需求 FR-04 已同步）。
- **人速口径（2026-09-13 用户要求「调整到真实人类完成机械化流程的速度」，已实施）**：
  - 节流取值：`ACTIVE_INTERVAL_MS` 1000 → **200ms**、`STABLE_INTERVAL_MS` 3000 → **1000ms**；
  - 间隔语义：由「从本轮开始计时」改为「**两轮之间的休息时间**」——新增 `FrameThrottle.markProcessedEnd`
    在每轮结束时回填基准；否则间隔小于单轮耗时（大窗口信号真机 ≈270ms）时会退化为连续满负荷；
  - 降档判据：`RoundResult.stable`（transition == null）→ 新增 `RoundResult.settled`
    （预期命中 ∧ 无候选累积 ∧ 无转移），由 `UiStateMachine.hasForeignCandidate` 支撑；
  - 预期效果：一轮周期 = 休息 + 处理 ≈ 262ms（小窗口信号）/ 470ms（大窗口信号）；
    步骤延迟 ≈ **0.3 ~ 0.5s**（小窗口）/ **0.5 ~ 0.9s**（大窗口），对比改造前 1 ~ 6s；
  - 需求同步：NFR-02 改为「需要响应时 ≤ 0.2s，确实稳定时 ≤ 1s；间隔为两轮之间的休息时间」（已落 `requirements.md`）。
- 进展（2026-09-13）：**实现 + 单测 + 全量回归完成，真机复测待执行**——
  ① 取消按需补扫（`detectWithFallback` → `detectRound`，去除 `probing` / `scanAllThisRound`）；
  ② 周期兜底默认关闭（`ActiveSignalSelector.NO_FALLBACK`，能力保留、可显式启用）；
  ③ 降档判据新增 `settled`（配套 `hasForeignCandidate`）；
  ④ 转移轮与 10s 统计行输出本轮搜索的信号名 / 窗口内「搜索次数 + 全扫轮数」；
  ⑤ 节流提速与人速口径（见上一条）。
  单测：`ActiveSignalSelectorTest` 按新口径重写（含「取消补扫后经未知再识别」「未命中不补扫」）、
  `RecognitionLoopTest` 补 `settled` 断言、`FrameThrottleTest` 补「处理结束回填」用例；
  `:app:testDebugUnitTest` 全量通过（2026-09-13）。

## 并行研究任务

- R-1、R-2 延续推进（见 `m0-skeleton.md`）；其样本收集与 M1 标定共用真机采集流程。

## 完成定义（DoD）

1. A1–A8 全部勾选，证据齐备并链接到 `docs/verification/`；A9 **不勾选、但结论与差距如实记录**
   （2026-09-13 定稿口径 A：间隔项达标、耗时项未达标，NFR-01 最终验收移交 M5）；
2. `docs/recognition/` 有指标报告与样本索引；
3. `docs/progress.md` 已更新；
4. 本文件状态改为「已完成」（2026-09-13 已置）；
5. 非验收项与未结事项已登记去向：T1-8 标定补充（a/b/c）→ `plans/m2-auto-handling.md`（M2 / M4 前置）；
   T1-5l / T1-5m / T1-10 / T1-11 / T1-12 / T1-13 / T1-13b 均已在 M1 内归案。
