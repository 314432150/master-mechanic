# AGENTS.md — MasterMechanic 协作入口

> 本文件是 AI 助手与维护者的会话入口：开新会话先读本文件，再按「单任务工作流」执行。
> 一切验收口径以 `docs/requirements.md` 为准；本文件不复制需求，只约束"怎么干活"。

## 项目一句话

非 root 的 Android 自动化应用：替玩家自动处理《王者荣耀》王者农场的活动弹窗与新手引导（常驻守护），并按配置一键完成多区服巡查（换区 → 登录 → 进农场 → 拜访指定好友）。

## 文档地图

| 想了解 | 去哪里 |
| --- | --- |
| 需求与验收口径（唯一权威） | `docs/requirements.md` |
| 术语定义（禁止另造词） | `docs/glossary.md` |
| 架构与模块边界 | `docs/architecture.md` |
| 当前进度 / 下一步 | `docs/progress.md` |
| 里程碑计划与任务卡 | `docs/plans/` |
| 不可逆决策记录（ADR） | `docs/decisions/` |
| FR/NFR 验收证据对照 | `docs/verification/` |
| 识别指标与样本证据 | `docs/recognition/` |

## 七条红线（不可协商）

1. **游戏不前台 / 状态未知 → 零点击。** 任何代码路径都不得在这两种情况下产生点击。
2. **点击只有四类合法来源**：FR-01、FR-02、巡查主流程步骤、用户经悬浮窗菜单发起的操作。除此之外不产生任何点击。
3. **名称定位必须唯一。** 区服 / 好友名称匹配不唯一或找不到 → 停下并提示，禁止选"最像的"。
4. **视觉识别零硬编码。** 代码中不得出现开发设备的分辨率、密度、坐标、阈值；一切基于目标设备自标定。
5. **动作后未验证，不得执行下一步。** 禁止连续盲点。
6. **相邻点击间隔 ≥ 300ms**，在统一的点击转发层实现，不依赖调用方自觉。
7. **识别不可信 → 按未识别处理。** 多处高分、或最高分与次高分差距过小时即为不可信。

## 构建与验证命令（Windows PowerShell）

- `.\gradlew.bat :app:test` — 运行单元测试
- `.\gradlew.bat :app:assembleDebug` — 构建调试包
- `.\gradlew.bat :app:assembleRelease` — 构建发布包（包体验收用）

依赖统一走版本目录 `gradle/libs.versions.toml`；新增依赖前先说明用途并征得确认。

## 单任务工作流

任务分两类：**实现任务**（产物 = 代码 + 证据）、**研究任务**（产物 = 样本 + 结论文档）。

1. **选任务**：从 `docs/plans/` 当前里程碑中取一个最小可验证切片；§7 待确认事项优先以研究任务形式启动。
2. **读上下文**：本文件 → 术语表 → 对应计划 → 相关文档。禁止凭猜测开工。
3. **补任务卡**：在计划文件中补全"目标 / 验收 / 证据方式"，先写验收再写代码。
4. **实现**：小步 diff，每次只做一件事。
5. **验证**：单测 →（涉及画面时）离线回放 → 真机核对；证据记入对应文档。
6. **提交**：`feat(fr-01): ...` 格式，引用需求编号。
7. **更新进度**：把结果与证据链接追加到 `docs/progress.md`。

会话开场模板：`按 AGENTS.md 工作流，继续 M0 的 T0-x`。

## 需求维护规则

- `docs/requirements.md` 只回答"做什么、怎么算成了"；**禁止写入**算法名、库名、系统接口名、设备型号、分辨率/密度数值、参数值。
- 实现细节一律写入 `docs/architecture.md` 或 ADR。
- 修改需求本身必须先征得用户确认。
